# ARCHITECTURE

> open-tag's bird's-eye view + codemap. Answers two questions: **"Where does the code for X live?"** and **"What is this module I'm looking at actually doing?"**
> Rule (following matklad's *ARCHITECTURE.md* advice): write only the things that don't change often. **Name files but don't paste line-level links** (use symbol search). Don't try to stay in sync line-by-line with the code; revisit once or twice a year. Inline comments carry the detail.
> All descriptions are grounded in the actual `src/` code.

## I. Overview

**open-tag** is an open-source, self-hosted alternative to Claude Tag — a Slack-style multi-agent collaboration workspace. Humans and AI agents work as teammates in channels, threads, and DMs. Agents are persistent "employees" with memory, running on a local daemon host; they can be @-mentioned, claim tasks, collaborate with other agents, and wake up on events. Data stays entirely within your own network.

> **Core mental model**: an agent is an **employee** — single-threaded like a person (one thing at a time, has an inbox, goes idle, picks up via memory). **Scale out by adding more agents, not by making one agent multi-threaded.** Collaboration happens via messages (channels / DM / tasks). This one design decision explains every downstream choice: single-threaded main loop, deliver-debounce batching, idle-sleep, external wake, multi-agent parallelism.

Three planes that never cross-contaminate, each with its own auth domain:

```
Human/Web plane    React SPA  → REST /api/*       (Bearer JWT + header x-server-id), socket.io real-time
Control plane      Server ↔ Daemon: WS /daemon/connect?key=  (always the backbone; S→D commands / D→S status)
Agent data plane   Agent process → HTTP /agent-api/*  (Bearer per-agent token sk_agent_* + header x-agent-id), agent uses `open-tag` CLI
```

**Agent lifecycle**: `start → active →(idle timeout) sleep (kill process) → start again (with archived sessionId) → resume`.
- **No separate wake operation**: wake = server re-sends `agent:start` with the saved `sessionId`; the runtime resumes via `--resume` (claude), `thread/resume` (codex), `--session-id` (copilot, idempotent create-or-resume), `--session` (opencode, captured `ses_…` id), `-r` (kimi, captured `session_…` id), `--session` (pi, captured UUID), or `--resume` (cursor, captured UUID).
- **Idle-sleep is implemented** in daemon `agentManager.ts`: `IDLE_MS` default 10 minutes, overridable via `OPEN_TAG_IDLE_MS`; every activity event calls `resetIdle`; timeout kills the process to stop token burn.
- cwd = `~/.open-tag/agents/<agentId>/` (persistent workspace + `MEMORY.md`; after compaction the agent re-reads `MEMORY.md` to reconstruct context).
- **Status is two-dimensional** (daemon `agentManager` emits; `ws.ts` writes `agents.status/activity` to DB):
  - `status` = lifecycle: `active | sleeping | inactive`
  - `activity` = current state: `online | working | thinking | sleeping | offline | error`
  - Running: `active / working·thinking·online`
  - Idle-sleeping: `sleeping / sleeping` (idle timeout or process exit; @-mention triggers auto-resume via `--resume`)
  - Stopped / never started: `inactive / offline` (explicit stop or just created)
  - Machine offline → its agents offline: a machine that is **confirmed down** (`status=offline` **and** `lastHeartbeat` stale) cannot host a live agent, so the heartbeat sweeper (`machineLiveness.ts reconcileOfflineMachineAgents`) forces every still-live agent (status `active` **or** `sleeping`) on it to `inactive / offline` and publishes the change. Clean daemon WS close also calls `markMachineAgentsOffline(machineId)` immediately, so a crashed/disconnected daemon cannot leave agents stuck in `working` until the next sweep. `sessionId` is left intact so the next wake still `--resumes`; the stale-heartbeat gate keeps a brief WS blip (offline for seconds, then reconnects) from being mistaken for a dead host, and an agent idle-`sleeping` on a *live* host stays `sleeping` (resumable). `machines.status` itself (what the frontend shows) is reconciled three ways so it never lies: WS connect/close (`ws.ts`, guarded so a stale socket close cannot offline a newer connection for the same machine), a **boot pass** that flips all machines offline before listening (a fresh instance has zero daemons), and the **heartbeat sweeper** that offlines machines whose `lastHeartbeat` (bumped on every `pong`) goes stale (daemon killed without a clean close) — all in `machineLiveness.ts`.
  - Crashed: `sleeping / error` (non-zero exit code; shown in red in UI but still resumable)
  - Key distinction: `sleeping` (can be @-woken) ≠ `offline/inactive` (needs manual intervention). Frontend `Members.tsx statusOf()` unifies sidebar / overview / Roster rendering; CSS `.dot.*` colours (green=running / yellow=busy / blue=sleeping / grey=stopped / red=crashed).

## II. Codemap (what each file/directory does)

### Control plane + data plane (`src/server/`, TypeScript — the production implementation)

- `index.ts` — HTTP entry point: route order `/agent-api` → `/api` → `/docs/` (`docs-site/dist`, with `/_astro/*` asset support) → static `web/dist` (SPA fallback only for known client routes from `staticRoutes.ts`, so scanner/file paths 404 instead of getting the app shell); mounts socket.io (human real-time), bare WebSocket (daemon control plane), reminder scheduler, and the durable Conversation Turn scheduler. `/docs` redirects to `/docs/`, so self-hosted installs get in-app docs at their own origin while the marketing site points to `https://docs.getopentag.com/`.
- `routes-api/` — **Human-side REST `/api/*`** (directory: a thin `index.ts` orchestrator runs the three auth gates [public → +userId → +serverId], builds a per-gate `ctx` [`ctx.ts`], and dispatches to per-domain handlers — `auth`/`servers`/`agents`/`channels`/`messages`/`tasks`/`reminders`/`attachments`, shared helpers in `shared.ts`; **gate order + gate-2 dispatch order are security-load-bearing** — see header comment in `index.ts`). Endpoints by group: auth (dev-login / register / login + `invite-info` / `accept-invite` via join-link), servers (`POST /servers` creates workspace [core.createServer: server + owner + default #all] — returns `id`/`slug`/`role`/`capabilities` so the client activates the new workspace optimistically without a re-fetch + `POST /servers/:id/avatar` + `GET /servers/:sid/members/:uid/profile` + join-links CRUD [manageMembers] + `PATCH/DELETE /servers/:id/members/:uid` role management/removal [changeMemberRoles/manageMembers; constraints: not self + preserve last owner]; `GET /servers` returns role + capabilities per item) / members (with description) / machines (incl. `POST …/machines/:id/reconnect` [manageMachines] — rotate the connection key on an existing **offline** row so a machine whose one-time key was lost can rejoin without a duplicate; refuses an online machine; `PATCH …/machines/:id { name }` [manageMachines, tenant-isolated] — rename a machine's friendly display name) / notification-settings / sidebar-order, agents (start/stop/reset/workspace-files/activity-log/scopes/skills/personality — plus skills write/delete `PUT/DELETE /api/agents/:id/skills/:name`; lifecycle endpoints wait for `agent-control-ack-v1`, return 503 on daemon failure, and abort later restart phases), channels (inbox/unread/saved/threads/dm/files), `GET /mentions` (message-grained @-of-me activity stream, read or unread; `limit`/`offset` + `hasMore` load-more paging), messages (`GET messages/channel/:id` keyset-paginated: `before=<seq>` cursor → older page + `hasMore`)/sync/search/`GET messages/:id` (single message, channel-read-gated — declared after /sync+/search so the bare-id route never shadows them; used by the channel's "jump to unread thread" bar to open a thread whose parent isn't in the loaded page), tasks, `actions/:id/mark-executed` (action card human-in-the-loop submit), attachments, reminders, announcements. **`GET /api/servers/:sid/machines/:mid/runtime-models/:runtime`** (in `servers.ts`) — model list for a given machine + runtime: for `DYNAMIC_RUNTIMES` (opencode / cursor / pi / claude / codex) it calls `getDynamicModels(machineId, runtime)` (`runtimeModels.ts`) — after a tenant check that the machine belongs to this server — which checks an in-memory cache (~60s, keyed `machineId:runtime`) then fires a `probe-models` WS-RPC to that machine's daemon; on success returns the daemon-discovered models (default first); on probe failure / timeout / cross-tenant id / `machineId="none"` it falls back to the static `MODELS` table. copilot / kimi always return the static list.
- `routes-agent.ts` — **Agent data plane `/agent-api/*`**: full agent operation surface — message (check/**decide**/send/react/read/resolve), server/info (channels — public + agent-joined; `dm` and `thread` types are always excluded so internal thread-xxxx channel names never pollute agent context — + agents + humans, members include `description` field so agents understand teammates), channel (join/members/leave), task (list/claim/**assign**/update/new/unclaim — handoff reassigns an existing task to another agent and points the assigner back to the task thread), thread (reply/read/unfollow), search, attachment (upload/view — view resolves full uuids *and* 6+-hex short-id prefixes, mirroring `resolveMessageId`, since agents cite the 8-char ids they see in message text), profile (show/update), reminder (schedule/list/cancel/snooze), `action/prepare` (send B-mode quick-commit card, scope `action:prepare`; variants: channel:create / agent:create). `message check` hides collecting/ready/dispatching Turn members, returns later stable Turns across an earlier cursor gap once, records each `(message, agent)` observation independently, and renders canonical trigger/attention/decision/grant metadata. Ambient rows remain readable for relevance judgment; addressed `direct|dm|assigned` rows stay hidden until this recipient has `deliveryAdmittedAt`. The send-time freshness check reuses the same admission/cursor classifier but has a distinct read purpose: unseen ambient collecting context may hold a stale draft, while a direct queued Turn cannot interrupt the current reply; only the contiguous admitted prefix advances `lastReadSeq`, and per-draft reviewed ids prevent repeated holds without consuming the formal observation. A daemon without `delivery-admission-v2` receives no stable Turn and a fail-loud warning. `message decide`, trigger-bound `message send`, and legacy thread reply enforce the same recipient admission fence before reserving publication. Capability-paused sentinel Turns always remain blocked, while a durably admitted recipient may finish after an unbound topology change. For a coordinated Task, only the active primary coordinator may claim, assign, or update the parent; directed contributors publish scoped results in the Task thread. Human-only `agent_activity_receipt` rows are excluded from agent check/read/thread-read queries and never advance the collaboration loop. **Showcase** is a **front-end static page** (`web/src/views/Showcase.tsx` + `web/src/showcaseData.ts`, assets under `web/public/showcase/`) — there is **no `type="showcase"` channel and no prop agents on the backend**. The former DB-backed read-only `#showcase` channel was removed: `showcaseSeed.ts` / `showcaseAssets.ts` deleted, the `seedShowcase()` calls dropped from `createServer()` / `seed.ts`, and the `type="showcase"` read passthroughs + write intercepts removed from `channelAccess.ts` / `core.ts canAgentReadChannel` / `socketio.ts canReadChannel` / `routes-api/{messages,channels,tasks}.ts` / `routes-agent.ts`. Existing showcase rows + the per-workspace `creatorType="system"` prop agents are cleaned up by the one-off `scripts/drop-showcase.mjs` migration. (The `creatorType="system"` roster/`workspaceMembers`/`visibleAgents` filters from #116 are retained as harmless no-ops — see `docs/tech-debt-tracker.md`.)
- `conversationTurnPolicy.ts` / `conversationTurns.ts` / `conversationTurnDispatch.ts` / `conversationTurnRecovery.ts` — **Conversation Turn admission, dispatch, and version-gated recovery**: partitions bursts by concrete `(server, channel/thread/DM, sender type, sender id)`, so Alice never extends Bob's window and an agent never joins a human Turn. Ambient and same-sender DM messages use trailing settle windows capped by a first-message max wait; agent DM replies merge only with the same reply root and causal depth. Explicit mentions remain boundaries, and task/action/system boundaries dispatch immediately. A collecting Turn reserves responsibility but cannot be checked or answered; dispatch atomically activates the Turn and grants before any wake, chooses one sticky/least-loaded owner (including reserved load) for human ambient work, capability-preflights every explicit recipient before starting any, then fans them out concurrently as independent directed work. `dispatchAttempts` fences activate/renew/finish/retry. Partial directed NACKs keep the Turn and all explicit grants active while same-id retries settle. A capable reconnect re-schedules both capability-paused sentinel Turns and terminal delivery-blocked Turns with an unadmitted active grant; unbound recovery still requires exactly one capable daemon. Agent-authored ambient wakeups remain suppressed; agent-to-agent DMs are directed causal edges even without a literal `@`, same-parent retries reuse the accepted edge, and later repeated pairs are suppressed.
- `replyCoordinationPolicy.ts` / `replyCoordination.ts` — **Reply side-effect authority**: the pure policy validates structured intent (`ownership/better_fit/handoff/correction/blocker/new_evidence/unique_expertise`) and decides primary/supplemental eligibility; the DB-backed module assigns the first explicit mention primary and every later explicit mention an independent directed grant, records observation/decisions, privately transfers/releases primary ownership, derives the canonical publication target (task thread vs source channel), waits through the bounded reply-settlement window (`OPEN_TAG_REPLY_SETTLE_MS`, default 5000 ms), reserves publication, and links the consumed grant to the reply. Partial unique indexes enforce one primary + one supplemental; `(replyToMessageId, senderId)` makes all grant kinds one-shot per agent. Confirmed delivery failure releases unconsumed grants; message visibility is never removed to suppress replies.
- `core.ts` — **Message core**: seq allocation, `@mention` parsing (stored structurally in `message_mentions`), persistence, human-side broadcast, Turn attachment, target resolution, and task lifecycle mutations (`convert` / `claim` / `assign` / `unclaim` / `status`). Human- and agent-authored explicit work mentions may Slack-style auto-join reachable non-members; the reach pool still prevents private/DM outsiders. `assignTask()` reassigns an existing task to another agent, records the handoff in the task thread, ensures the new assignee is a thread member, and creates an immediate assigned Turn boundary. Agent delivery and responsibility selection live in `conversationTurnDispatch.ts`; all message persistence still converges here.
- `agentConfig.ts` / `agentDeliveryAck.ts` / `agentDeliveryAdmission.ts` — small server boundaries shared by live dispatch/reconnect: normalize runtime config; own the deterministic delivery waiter; and validate/commit the two-phase recipient admission. `agent:deliver:pending` only renews transport liveness. `agent:deliver:ready` is checked against the authenticated current machine, tenant, bound agent (or sole-daemon unbound topology), Turn, recipient, and sequence; only after `deliveryAdmittedAt` commits does the server answer `agent:deliver:admitted`. Final ACK must match that committed connection record. NACK/disconnect releases an unpublished in-flight admission; successful delivery ids remain idempotent across daemon replacement.
- `agentActivity.ts` — Shared run-Activity persistence and ownership boundary: normalize/prune activity-log rows, start a stream, claim unassigned rows for a public-message segment, retain an active stream across back-to-back messages, and assign log rows to their durable message/receipt owner. The per-agent log remains capped at 500; message snapshots are the durable chat rendering source.
- `agentStartGuard.ts` — Pure start-gating decision for agent lifecycle/wake paths: distinguishes no daemon online, missing selected machine, selected machine offline, and selected machine missing the requested runtime; unbound agents are allowed through for the legacy broadcast fallback. Kept side-effect-free so runtime/machine eligibility is unit-tested without booting the server stack.
- `auth.ts` — **Two auth stacks**: human JWT (`verifyUser`, scrypt password storage) + agent (`resolveAgent`: Bearer = per-agent token, SHA-256 compared against `agents.agentTokenHash` + header `x-agent-id`; token must belong to that agentId → prevents cross-agent / cross-server impersonation; no master key). ⚠️ Daemon WS `?key=` handshake auth (machine key) is **inline in `ws.ts`**, completely separate from agent credentials.
- `scopes.ts` — Agent permissions: 14 scopes + `agentHasScope` / `effectiveScopes` (null = grant all by default).
- `capabilities.ts` — **Human role permissions** (separate system from scopes: scopes = agent data plane, capabilities = human management rights): 8 capabilities × {owner/admin/member} mapping + `can(role,cap)` / `requireCap(serverId,userId,cap)` / `capabilitiesFor(role)`. All write endpoints (create/delete channels, agents, server settings, avatars, member management) enforce these.
- `realtime.ts` — Global publish entry point `publish()`, maps to named events and fans out to server rooms; seq comes from Redis.
- `socketio.ts` — Human-side real-time (socket.io, `/socket.io/`); `emitMapped` sends `message:new` (full message object) / `message:updated` / `task:created` / `task:updated` / `agent:activity` (workspace status + profile trajectory) / `agent:reply` (channel-scoped Activity preview `start`/`activity`/`done`/`error`, keyed by `streamId`) / `thread:updated` (parent message id + parent channel id + reply count + sender id for self-unread filtering) / `agent:created|deleted` / `machine:status`. **Room model**: content events (message/task/agent Activity preview) fan out to per-channel rooms `channel:<id>` (isolation — private channels never leak); metadata events (agent/machine/thread:updated) fan out to `server:<serverId>`. A socket joins its member-channel rooms at connect, and **view-driven** joins any channel/thread it opens via `join:channel` (the server allows joining a channel the user can read — member / public / thread of a readable channel — and refuses private non-members), so realtime tracks what the user is actually viewing.
- `ws.ts` — **Daemon control plane** (bare WS, `/daemon/connect?key=`). Attaches its frame listener before asynchronous machine-key authentication; `wsFrameGate.ts` queues frames received during authentication and drains them in arrival order. Handles D→S `ready`, admission pending/ready/ACK/NACK, status/activity/session/trajectory/reply, heartbeat, and RPC responses. The ready/admitted handler persists admission before the daemon can notify a runtime and keeps per-connection in-flight records so stale, wrong-machine, or mismatched ACK/NACK frames cannot settle another agent's work. `ready.capabilities` is registered before machine indexing/catch-up; a newly current capable connection resumes paused bound Turns and, when it is the server's sole capable daemon, legacy unbound Turns. `ready.runningAgents` reconciles stale/reported live states without rotating tokens. Contextual Activity frames are persisted and forwarded to the matching channel; done/error finalizes the last segment or receipt. Authentication, heartbeat, reconnect catch-up, stale-connection cleanup, and fatal `4001` handling retain their existing contracts.
- `machineLiveness.ts` — Keeps `machines.status` honest **and** drags down-machine agents offline: `reconcileMachinesOnBoot()` (flip all online→offline before the server listens) + `startMachineSweeper()` (periodic — offlines machines whose `lastHeartbeat` is stale, then calls `reconcileOfflineMachineAgents(cutoff)` to force every still-live agent — `active` **or** `sleeping` — on a confirmed-down machine to `inactive / offline`, `sessionId` kept) + `markMachineAgentsOffline(machineId)` (clean WS close path, immediate offline reconciliation for that machine). Thresholds via `OPEN_TAG_MACHINE_SWEEP_MS` / `OPEN_TAG_MACHINE_STALE_MS`.
- `reconnectCatchup.ts` — **Agent-side reconnect catch-up**, the symmetric counterpart to human reconnect sync. Called from `ws.ts` after live-state reconciliation, machine indexing, and durable Turn recovery. Running agents receive durable work only from the canonical dispatcher, never a second legacy body-free notice; legacy non-Turn backlog may still receive one. Dead processes receive `agent:start`, whose startup nudge may pull already-admitted durable backlog. This separation preserves the v2 commit barrier and prevents reconnect from duplicating recipient selection or runtime input. A per-machine cooldown (`OPEN_TAG_CATCHUP_COOLDOWN_MS`) guards flapping links. Agents with `machineId = null` still have no machine-specific reconnect event.
- `daemonHub.ts` — server→daemon routing plus WS-RPC. It tracks server membership, the current `machineId→ws`, and each ready frame's capabilities. `conversationTurnDeliveryBlockReason` requires a bound agent's current daemon to advertise `delivery-admission-v2`; an unbound agent requires exactly one connected capable daemon. Agent lifecycle RPC requires `agent-control-ack-v1`. Connection replacement evicts the old socket and its capability state so stale close cannot erase the current gate. Durable Turn work and settled lifecycle controls fail closed on version skew.
- `runtimeModels.ts` — **Dynamic model cache + probe**: `getDynamicModels(machineId, runtime)` — checks an in-memory cache (TTL ~60s, key = `machineId:runtime`), on miss fires a `probe-models` WS-RPC via `requestDaemonByMachine` (`daemonHub`) and awaits the daemon's `models` reply, then sorts with the default model first. `DYNAMIC_RUNTIMES` = `{opencode, cursor, pi, claude, codex}`. Errors / timeouts fall back gracefully (callers use the static `MODELS` table). Cache is intentionally in-process (not Redis) — staleness of 60s is acceptable for model lists; a per-machine-per-runtime TTL means the cache auto-refreshes across runtimes independently.
- `storage.ts` — Pluggable object storage boundary: default local disk, switchable to any S3-compatible backend (MinIO/Garage/OSS, `OPEN_TAG_S3_*`); `storageKey` is driver-agnostic. `s3Config()` reads + validates env on each call (fails loud, naming any missing `OPEN_TAG_S3_*`).
- `attachments.ts` / `reminders.ts` / `util.ts` — busboy streaming upload (≤25MB / 10 files; each per-file save self-catches and drains its stream so a failed `saveObject` surfaces as a rejected upload, never an unhandled rejection that crashes the process) / reminder fire → posts system message `@owner reminder:...` in anchor channel (passive trigger via @mention polling, not direct process wake) / `sendJson` + `sendErr`.
- `ratelimit.ts` — in-memory fixed-window per-IP rate limiting (registration) + `clientIp()`: prefers `X-Real-IP`, falls back to the **first** `X-Forwarded-For` hop, both only under `TRUST_PROXY=true`; socket address otherwise (see AGENTS.md transport flags for the proxy trust model).

### Local daemon (`src/daemon/`)

- `index.ts` — Daemon entry point: connects to server control-plane WS, dispatches `agent:start / stop / sleep / reset / profile / deliver / workspace:list / workspace:read / workspace:write / workspace:delete / skills:list / ping / probe-models`. Turn delivery forwards `turnId`, `turnMessageCount`, `attention`, deterministic `deliveryId`, and sequence. While queued it emits `agent:deliver:pending` heartbeats. At the FIFO head it repeatedly sends `agent:deliver:ready` and waits for server `admitted` before allowing `agentManager` to write the runtime notice/startup nudge; adapter acceptance then produces final ACK, rejection produces NACK. Resource-pressure queueing remains pending. Start/stop/reset operations carrying `requestId` return `rpc:ack/nack` only after the serialized lifecycle operation settles. On `agent:start` it injects the agent's `OPEN_TAG_SERVER_URL` = **the URL this daemon connected with**, overriding the server-reported `config.serverUrl`.
- `connection.ts` — Control-plane WS client (exponential backoff reconnect 1s→30s; ping/pong dispatched in `index.ts`, this class manages reconnection + a server-frame watchdog). Backoff resets **only after the server accepts the connection** (first frame received) — not on raw socket `open`, since a rejected key also briefly opens before the server closes it. After acceptance, every server frame arms a watchdog (`OPEN_TAG_DAEMON_SERVER_STALE_MS`, default 90s); if the daemon stops receiving server frames, it closes the socket so normal reconnect runs instead of leaving a half-open process while the server marks the machine offline. A `4001` close (unknown/removed key) is treated as fatal: jump to the 30s cap + log an actionable "re-issue the connect command" error instead of hammering once a second. Socket creation goes through an injectable factory so the backoff/reject/watchdog state machine is unit-tested (`connection.test.ts`) without a real server.
- `agentManager.ts` / `deliveryAdmissionStore.ts` — **Agent lifecycle and delivery admission**: start / sleep / stop / reset / deliver; two-dimensional status `status(inactive/active/sleeping) × activity(online/working/thinking/sleeping/offline/error)` (non-zero exit → `sleeping/error`, see §I state machine). Wake = `start()` with archived sessionId. **Includes idle-sleep** (`IDLE_MS`). `onSession(sid)` immediately pushes `agent:session` to server for persistence. **Personality override**: on `start()` and `syncProfile()`, reads `personality.md` from the agent workspace; if present and non-empty, substitutes it for the agent's `description` field in the system prompt and MEMORY.md `## Role` section (via `applyProfileToMemory`). Legacy non-Turn delivery retains its 3s debounce. Durable Turns bypass that debounce, preserve arrival order through serialized ledger preparation, and execute one FIFO item at a time. Only the queue head may pass the server commit barrier and then start an Activity preview/runtime input; a rejected barrier produces neither runtime input nor phantom Activity. Cold startup absorbs one head Turn into its wake nudge and leaves the rest queued. `error` finalizes that preview but does not advance until the adapter's terminal `online` event, preventing an `error -> online` sequence from stealing the next stream. Successful ids persist in the bounded `.delivery-admissions.json` ledger; failed/dequeued/stopped ids release the fence, and same-id retries re-commit server admission without repeating ordinary runtime work. Per-agent lifecycle controls are serialized; start waits for initial protocol admission, stop/reset wait for that session's exit, and delivery epochs cancel stale async preparation.
- `runtime.ts` — Runtime adapter **interface** (pure types: `Runtime{start()}` / `RuntimeSession{deliver,stop}` / `RuntimeCallbacks` / `StartOpts` [includes `runtimeConfig`] / `TrajectoryEntry`). `RuntimeCallbacks.onInitialTurnAdmission` is an exactly-once boundary: adapters accept only after their initial input is admitted (`turn/start`, stdin write, or argv process spawn), and reject pre-admission failures. `spawnSafe.ts` preserves that boundary on Windows by resolving the real executable through `cwd` / `PATH` / `PATHEXT` before using `cross-spawn`; an unresolved CLI uses native spawn so ENOENT is emitted before any misleading `cmd.exe` spawn event.
- `runtimes.ts` — Runtime **registry** (`REG` / `getRuntime()`) + `detectRuntimes()` probes which runtimes are installed locally (`claude` / `codex` / `copilot` / `kimi` / `opencode` / `pi` / `cursor` via `cursor-agent` / `hermes`). `REG` implements all eight adapters.
- `claudeRuntime.ts` — Claude `-p stream-json` adapter: writes `.claude-system-prompt.md` on spawn via `--append-system-prompt-file`; parses `thinking` / `text` / `tool_use` blocks into trajectory entries. Spawn failures such as a missing `claude` binary surface as offline/error activity instead of an unhandled daemon exception, so one misconfigured agent cannot kill the daemon process. **No MCP**; the agent communicates via the `open-tag` CLI.
- `codexRuntime.ts` — Codex `app-server` JSON-RPC adapter (experimental; handles both legacy and raw event schemas; forwards `runtimeConfig.reasoningEffort` to thread/turn; auto-approves exec/patch/permissions/elicitation; raw v2 de-noises token deltas, maps completed item / tool_start to text/thinking/tool trajectory entries; spawn failures such as a missing `codex` binary surface as offline activity instead of an unhandled daemon exception).
- `hermesRuntime.ts` — Hermes profile adapter (experimental; **one-shot per turn**). Each wake spawns `hermes chat -q <prompt> -Q --source open-tag`, selects either `default` (no `HERMES_HOME`) or a discovered profile, captures native `session_id:` from stderr for resume, and retries fresh when Hermes reports a missing resumed session. Its final-response bridge binds stdout to the checked trigger; an ambient response first requests a reply grant and stays silent when denied, while a directed active grant can publish normally. Hermes owns provider credentials and profile configuration.
- `copilotRuntime.ts` — Copilot CLI adapter (experimental; **one-shot per turn**, unlike the persistent claude/codex processes). Each turn spawns `copilot -p --output-format json --allow-all --no-ask-user --session-id <uuid>` and parses the JSONL event stream (`assistant.message`/`reasoning`/`turn_start` → text/thinking/tool trajectory; `result` → sessionId). Multi-turn + wake-resume chain via a **self-assigned `--session-id`** (idempotent create-or-resume). System prompt is injected via `{cwd}/AGENTS.md` (Copilot reads it natively — no system-prompt flag). Model/launch errors are surfaced from **stderr** on non-zero exit (they are not JSON events). `forwards runtimeConfig.reasoningEffort` via `--effort`. Verified against Copilot CLI 1.0.61.
- `opencodeRuntime.ts` — OpenCode CLI adapter (experimental; **one-shot per turn**, like copilot). Each turn spawns `opencode run --format json --dangerously-skip-permissions --dir <cwd> [--model provider/model] [--variant <effort>] [--session <id>] <msg>` and parses the `{type, part}` event stream (`text` → text, `tool_use` → tool, `reasoning` → thinking; every event carries `sessionID`). Multi-turn + wake-resume chain via the captured `ses_…` id passed to `--session`. System prompt via `{cwd}/AGENTS.md`. **Two load-bearing gotchas (verified vs 1.15.5): stdin MUST be `ignore`** (a piped stdin makes `opencode run` block forever) and **`NODE_OPTIONS` is stripped** from the child env (a proxy flag can stop the CLI starting); `PWD` is pinned to cwd for project discovery.
- `kimiRuntime.ts` — Kimi Code adapter (experimental; **one-shot per turn**, like copilot/opencode). Each turn spawns `kimi -p --output-format stream-json [-m <alias>] [-r <session_…>] <msg>` (stdin `ignore`) and parses OpenAI-chat-message lines (`{role:assistant,content}` → text, `{role:assistant,tool_calls}` → tool; `{role:meta,type:session.resume_hint,session_id}` → resume id). System prompt via `{cwd}/AGENTS.md`. **Auth is config-only**: kimi-code reads its provider + `default_model` from `~/.kimi-code/config.toml`, NOT env — the host must configure it (e.g. an OpenAI-compatible gateway with a kimi-k2 model). Verified against kimi-code 0.19.2 (the maintained successor to the deprecated `kimi-cli`).
- `piRuntime.ts` — Pi Coding Agent adapter (experimental; **one-shot per turn**, like copilot/opencode/kimi). Each turn spawns `pi -p --mode json --append-system-prompt <file> --session-dir <cwd> [--model provider/id] [--session <id>] <msg>` (stdin `ignore`) and parses the JSON event stream — a `session` event carries `.id`; `message_end.message.content[]` blocks map `{type:text}` → text and `{type:toolCall,name,arguments}` → tool (we read `message_end` only; `agent_end` repeats it). System prompt via Pi's native `--append-system-prompt` file. Provider/model come from Pi's own config (`pi --list-models`) — the host configures the provider (e.g. a custom OpenAI-compatible gateway). Verified against pi 0.73.1.
- `cursorRuntime.ts` — Cursor Agent adapter (experimental; **one-shot per turn**, like copilot/opencode/kimi/pi). Each turn spawns `cursor-agent -p --output-format stream-json -f [--model <m>] [--resume <id>] <msg>` (stdin `ignore`) and parses Cursor's Claude-style stream-json: every event carries `session_id`; tools arrive as separate `tool_call` events (`<kind>ToolCall.args` → tool); the terminal `result` event carries the final text + `is_error` (the streamed `assistant` text chunks are skipped to avoid one row per token). System prompt via `{cwd}/AGENTS.md`. **`NODE_OPTIONS` is stripped** from the child env (cursor-agent's bundled node rejects proxy flags). Runs on the user's Cursor account (`cursor-agent login`). Verified against cursor-agent 2025.09.17.
- `listModels.ts` — **Dynamic model discovery**: shells out to each runtime's discovery command and parses the result. opencode (`opencode models --verbose`), cursor (`cursor-agent --list-models`), pi (`pi --list-models`) → return a full **model list**; Hermes enumerates `default` + profile directories from `HERMES_PROFILE_DIR` and `~/.hermes/profiles`. claude (`claude --help`, parses `--effort`) and codex (`codex debug models`, parses `supported_reasoning_levels` from JSON) → model catalog stays static but per-model **thinking/reasoning-effort levels** are probed. copilot and kimi are **not supported** (their discovery requires an ACP / JSON-RPC-over-stdio handshake that the daemon's current adapter model doesn't implement). Called by `src/daemon/index.ts` in response to a `probe-models` S→D message.
- `prompt.ts` — Assembles the runtime-agnostic agent system prompt: identity + runtime context + **full `open-tag` CLI command reference** + reply coordination (`check → decide → send only when granted`) + message format + task flow (`todo → in_progress → in_review → done`) + etiquette / credential hygiene + startup sequence + **MEMORY.md re-read & compaction self-rescue**.
- `workspace.ts` — Workspace file tree / file read / write / delete + personality.md + list skills. **Skills are resolved per the agent's runtime** (`skillRootsFor`, ported from multica's `localSkillRootsForProvider`): the `agent:skills:list` WS-RPC carries the agent's `runtime`, and each runtime reads its own provider dir (claude → `~/.claude/skills`, codex → `~/.codex/skills`, opencode → `~/.config/opencode/skills`, cursor → `~/.cursor/skills`, pi → `~/.pi/agent/skills`, copilot → `~/.copilot/skills`) plus the universal `~/.agents/skills`; runtimes with no established dir (kimi) get the universal one only. (Agents run with the operator's real `$HOME`, so the home provider dir is the one they actually load.)
- `resourceLimit.ts` — **Per-platform memory pressure enforcement**. Every agent CLI process spawned via `spawnSafe.ts` is assigned to a Windows Job Object (via `koffi` FFI `kernel32.dll`) or Linux cgroup (v2 or v1) — no per-agent limits are set initially. A pressure monitor in `agentManager.ts` runs every 10s: when free memory drops below `OPEN_TAG_PRESSURE_MEM_MB` (default 500), new agents are queued and each running agent gets a `ProcessMemoryLimit` at `current RSS + ceil(400 / N)` MB via `SetInformationJobObject` + working set trimmed via `SetProcessWorkingSetSizeEx` (Windows) or `memory.max` (Linux). macOS support was removed.
- `openTagBin.ts` — **Generates** the `~/.open-tag/bin/open-tag` wrapper script for daemon PATH injection. Two modes: **repo** (runs `src/cli/index.ts` via local `tsx`) and **bundled** (when a sibling `agent-cli.mjs` is found next to the running daemon — i.e. launched from the `@fancyboi999/open-tag-daemon` npm package — runs that bundle via `node`).

### Distributable daemon package (`packages/daemon/`, `scripts/build-daemon-pkg.mjs`)

Lets any machine join a server without cloning the repo: `npx @fancyboi999/open-tag-daemon@latest --server-url <url> --api-key sk_machine_…` (the connect-a-computer UI generates the same `@latest`-pinned command, so a stale npx cache can't serve an outdated daemon). The daemon talks to the server purely over WS (no DB), so it bundles cleanly.

- `scripts/build-daemon-pkg.mjs` — esbuild bundles **two** self-contained ESM files into `packages/daemon/dist/`: `cli.mjs` (the `open-tag-daemon` bin = `src/daemon/index.ts`) and `agent-cli.mjs` (the agent CLI = `src/cli/index.ts`, which `openTagBin.ts` injects in bundled mode). ESM output (so `import.meta.url` works) + a `createRequire` banner (so bundled CJS deps `ws`/`commander` and node builtins resolve); ws's optional native accelerators stay external. Run via `npm run pkg:daemon:build`.
- `packages/daemon/package.json` — the publishable `@fancyboi999/open-tag-daemon` (bin `open-tag-daemon`, `files: [dist, README.md]`, zero runtime deps — everything bundled). `dist/` is gitignored and rebuilt in CI before publish.
- CI: `.github/workflows/ci.yml` runs typecheck, tests, security audits for the root/web/docs lockfiles, web + docs builds, and the daemon bundle build on **both Ubuntu and Windows** for every push/PR; `.github/workflows/publish-daemon.yml` publishes to npm on GitHub Release (via OIDC Trusted Publishing — no long-lived token); `.github/workflows/docs-deploy.yml` builds `docs-site/` at the custom-domain root and deploys it to GitHub Pages (`https://docs.getopentag.com/`) on push to `main` when `docs-site/**` changes.

### Data / shared (`src/`)

- `db/schema.ts` — **Data model** (Drizzle). Canonical field truth; change data structures here first.
- `db/index.ts` / `db/seed.ts` — DB handle (drizzle + postgres) / seed data.
- `redis.ts` — `nextSeq` (INCR `seq:{server}`) / `nextTaskNumber(serverId, channel?)` (INCR a scope key from `taskNumberKey`: `tasknum:dm:{channelId}` for DMs, `tasknum:{server}` otherwise) / `reconcileCounters` (boot-time durability guard: advances seq + every task-number scope to the live Postgres max) / `publishEvent` (pub/sub real-time fan-out, SSE subscribes `events:{server}`) / `pokeAgent` (RPUSH `wake:{agentId}`, wakes agent's BLPOP long-poll).
- `env.ts` — Loads root `.env` (**must be the first import** in any module that reads `process.env`; does not override variables already set in the shell environment).
- `log.ts` — Unified logger writing JSONL to `~/.open-tag/logs/` and human-readable console lines to stdout/stderr: non-error server/daemon logs go to stdout so platform loggers do not classify debug/info as errors, true errors go to stderr, and CLI logs stay on stderr so agent-readable command stdout remains clean.
- `daemonProtocol.ts` — Shared daemon ↔ server WS control-plane constants, including fatal machine rejection `4001` and `DELIVERY_ADMISSION_CAPABILITY`, imported by both planes so the wire contract has one source of truth.
- `cli/index.ts` — **`open-tag` CLI**: subcommand tree mirroring `/agent-api` (message / channel / task / thread / profile / reminder / attachment / search / server / action), directly `fetch(OPEN_TAG_SERVER_URL + /agent-api/...)` with Bearer `OPEN_TAG_AGENT_TOKEN` (per-agent token injected by daemon) + `x-agent-id`. `message check` is non-blocking and records the checked trigger for one-shot fallback; `message decide` submits structured intent/transfer; `message send --reply-to` binds a public reply to its grant. There is **no** `receive` command.

### Frontend (`web/`, React + Vite, standalone package — fully functional, not a demo)

- `main.tsx` / `store.tsx` — Router entry / global state Context (dev-login → JWT, socket.io real-time subscription, REST wrappers; multi-server: **the URL `/s/:slug` is the source of truth for the active workspace** — a one-time auth bootstrap resolves token + server list, then a separate *activation* effect [keyed on the active server id] resets per-workspace state, loads that workspace's data, and opens its socket; it re-runs on every **client-side** workspace switch — `WorkspaceRoute` bridges a known-but-not-active URL slug to `switchServer`, so create/switch never do a full-page reload. Exposes `servers` / `capabilities` / `createServer` / `switchServer`). While bootstrapping or switching (`ready=false`) route guards render a **three-column loading skeleton** (`views/Skeleton.tsx`) rather than reserving an observability column. Public routes and auth-routing decisions retain the contracts documented in `web/src/routing.ts`. The store keeps agent status/profile Activity realtime state but no channel-wide trajectory buffer; `openAgentPanel(id)` / `agentPanelReq` still opens Agent Profile on its Activity tab from the sidebar LiveAgentBar.
- `ServerSwitcher.tsx` — Top-left brand = workspace switcher (lists `GET /servers`, switch, create).
- `views/Auth.tsx` — Register/login page (`/login` · `/register`) + invite landing page (`/join/:token`); standalone from StoreProvider bootstrap, self-fetches `/api/auth/*`, on success stores `open-tag.token` and hard-navigates into the main app. Bootstrap prefers `open-tag.token`; falls back to dev-login (`?as=`).
- `views/Chat.tsx` / `AgentActivity.tsx` — Main group-chat view plus the shared per-message Activity disclosure. Both channel rows and thread rows consume the same channel-scoped `agent:reply` events, persist/merge `message:updated` tails, render actual public messages only after message API persistence, and render no-message receipts without body/actions. Live placeholders opt into automatic expansion; persisted messages and receipts default to collapsed even if more events arrive. Header run state is derived from the message snapshot rather than the agent's workspace-global presence. The fourth grid column opens only for a thread or member profile; Agent Profile Activity remains the global diagnostic surface.
- `views/Composer.tsx` — Shared message composer for channels, DMs, and threads: text + attachment upload (button / paste / drag-drop, with progress) + @mention autocomplete + send. `allowAsTask` gates the As Task toggle (channels/DMs only; a thread reply is never a task). Renders a **reachability/wake hint** above the input for the agents a message will reach — the DM peer (`dmAgent` prop) + any @-mentioned in the draft — when their machine is offline (copy: the message is saved and handled automatically once the machine reconnects, via reconnect catch-up), or, for a DM, when the sole peer is sleeping. Centralized here (replacing the former DM-only `topSlot` hint) so channels/DMs/threads share one path.
- `toast.tsx` — Lightweight global toast notifications (`ToastProvider` / `useToast()`, mirrors `ConfirmModal`'s context shape, no third-party lib; mounted in `main.tsx`). One-shot operation feedback where an inline banner doesn't fit — e.g. start/restart an agent whose machine is offline (503), or create an agent that couldn't start (`started:false`).
- `alerts.tsx` — **System alert center**: `useSystemAlerts()` derives *standing* system warnings from live store state (an online machine whose `daemonVersion` ≠ `latestDaemonVersion` → outdated daemon; an offline machine still hosting agents → those agents can't run), and `NotificationCenter` renders them in a fixed, rail-anchored popover. The rail's warning button (above Settings in `Layout.tsx`) shows only when there are undismissed alerts. Distinct from `toast.tsx`'s one-shot feedback — alerts recompute from state and are individually dismissable (session-scoped). `latestDaemonVersion` is read from `packages/daemon/package.json` by the `/api/servers/:id/machines` route (`routes-api/servers.ts`).
- `views/Members.tsx` — Members directory: the sidebar groups agents by machine + a humans section; the default roster lists agents and humans as two labelled sections of clickable cards, each navigating into that member's profile. AgentProfile seven-tab panel (overview / activity / workspace / permissions / apps / DM / reminders) + HumanProfile + create-agent modal.
- `views/ChatSidebar.tsx` — channel/DM sidebar shared by the Chat and Saved views (channel list, DM list, unread badges; hosts `LiveAgentBar` at its bottom).
- `views/ConnectComputerWizard.tsx` — multi-step connect-a-computer modal behind all three entry points (`onboard` auto-show / Computers `add` / offline-machine `reconnect` with key rotation).
- `QuickSwitcher.tsx` — Cmd+K global jump palette (channels / DMs / members).
- `messageRender.tsx` — markdown message renderer: mention links resolved from the message's own structured `mentions[]`, raw HTML downgraded to literal text (`remarkHtmlAsText` — no raw-HTML/XSS surface; see §III).
- `views/LiveAgentBar.tsx` — Live agent activity bar pinned to the bottom of the sidebar (rendered inside `ChatSidebar`, so it shows in both Chat and Saved views): a workspace-wide pulse of agents currently `working`/`thinking` (status-guarded against stale `activity`), most-active first with a `+N` popover for the rest. Clicking an agent calls the store's `openAgentPanel` signal → `Chat.tsx` opens that agent's profile on the Activity tab in the right column (no page nav, no DM). Complements the per-DM status dots; does not replace them.
- `views/misc.tsx` — Tasks / Inbox / Computers / Search / Settings.
- `views/Landing.tsx` / `views/Features.tsx` / `views/ProductMock.tsx` + `landing/` — Public marketing pages: landing (`/`) plus features (`/features`) share the warm-editorial skin (tokens all scoped to `.lp-*`, strictly isolated from app editorial skin — no cross-contamination). Landing covers hero / three-pillar / capability cards × 9 / engine / architecture / CTA / footer; features is a static public case showcase with five channel→task→thread demos and a clickable thread panel. Both pages reuse `ProductMock` so the promo surface mirrors the real app shell (rail, channel sidebar, chat, task pill, thread, agent profile, existing avatar system), with no API or live-agent calls. "Enter workspace" navigates to `/s/:slug` when logged in, otherwise to `/login`. Copy and selling points are grounded in verified capabilities.
- `TaskBoard.tsx` — 5-column kanban (todo / in_progress / in_review / done / closed) + List toggle + **Board layout toggle** (horizontal columns ↔ vertical stack, default horizontal, persisted in `localStorage["open-tag.tasks.boardLayout"]`; horizontal uses min-width columns + horizontal scroll so the narrow embedded channel tasks tab degrades gracefully) + filters + **move interaction** (drag-and-drop *or* click the status pill's menu — interactive controls inside a card `stopPropagation` the `pointerdown` so dnd-kit's sensor doesn't swallow the click; the board fills the pane (`.board-scroll` flex column) and columns are **full-height equal lanes** (`align-items:stretch`, body `flex:1` + internal `overflow-y`), so the *whole* column is the drop target, not just the cards; a `.drop-slot` indicator is appended after the cards instead of overlapping them; moves are optimistic and the card FLIP-animates from its old column to its new one, honoring `prefers-reduced-motion`) + real-time subscription.
- `Layout/Avatar/Select/PaneEmpty/Lightbox/icons.tsx`, `styles.css` — Shell / avatar / dropdown / centered editorial empty-state (`<PaneEmpty>`, for primary full content panes — secondary sub-section empties keep the inline `.empty`) / shared image lightbox (`Lightbox.tsx`: portal to `document.body`, scroll-zoom + drag-pan + Esc/backdrop-to-close + focus-trap; reused by `views/Chat.tsx` message attachments and the static `views/Showcase.tsx` demo so an image preview opens a floating dialog in place instead of navigating to the raw asset) / icons + UI tokens (EB Garamond + Inter, drawn from DESIGN.md token subset). `iconMotion.css` — pure-CSS icon micro-interactions (two-class API: `im` trigger on the button + `im-*` effect on the icon; shared overshoot curve; hover-capable + `prefers-reduced-motion`-gated; principles in `docs/icon-motion.md`).
- `dist/` — App build output, statically served by the server `index.ts`.

### Documentation (`docs/`, `DESIGN.md`, root files)

- `ARCHITECTURE.md` (this file) · `docs/` (**project-authored knowledge**: core-beliefs / generated/db-schema / tech-debt-tracker + tech-debt-archive / PLANS / MISSION / authorization / code-quality; `docs/exec-plans/` holds active + completed execution plans; `docs/superpowers/` is a read-only archive of earlier plan/spec documents).
- `docs-site/` — One-page public documentation site built from `DESIGN.md`; the Pages workflow builds it at the `https://docs.getopentag.com/` custom-domain root, while the normal build leaves the base unset so the app image can serve the same output at `/docs/`.
- Root `README.md` (how to run — kept current) · `DESIGN.md` (**visual system / frontend design spec**: design tokens + component specs, adopts an editorial design language as the project's own design system; `web/styles.css` implements the relevant subset) · `AGENTS.md` (agent onboarding guide) · `FEATURES.md` (feature **status** checklist — checkbox + short note; this file's §II stays the authoritative route/API surface).

### Running the project (`package.json` scripts)

```
npm run infra        # start postgres + redis via docker compose
npm run db:push      # push schema
npm run seed         # seed data
npm run server       # tsx watch src/server
npm run daemon       # daemon process
npm run typecheck    # tsc --noEmit, root + web (run before committing)
npm run wt:add -- <name>   # isolated parallel-dev worktree (see AGENTS.md); wt:rm tears down
npm run dev:e2e:up   # isolated live E2E stack with a seeded @dev-bot (dev:e2e:down to stop)
npm run prod:up      # local prod deploy: web build → db:push:prod → server (prod:down stops)
npm run pkg:daemon:build  # bundle the publishable @fancyboi999/open-tag-daemon package → packages/daemon/dist
cd web && npm run dev  # Vite HMR dev server
```

## III. Architecture invariants (violation = bug)

- **Three-plane auth never crosses**: human = JWT + `x-server-id`; agent = per-agent token SHA-256 vs `agentTokenHash` (+ `x-agent-id`); daemon = WS `?key=` handshake (machine key). The three credential sets are not interchangeable; using the wrong auth domain in any route is a defect. **Full access-control model — role/capability + agent scopes, the four invariants (tenant isolation, resource-access checks, channel visibility), and the hardening roadmap — is [`docs/authorization.md`](docs/authorization.md).**
- **Observation does not imply reply permission**: every message exposed by `message check` gets an idempotent `(messageId, agentId)` decision row and `observedAt`, including ambient agent chatter encountered during a broader check. A public agent reply must name its trigger and atomically reserve that agent's active grant; freshness drafts and the older thread-reply route cannot bypass this. The first explicit mention gets primary, later explicit mentions get independent directed grants, and unmentioned observers compete for at most one evidence-bearing supplemental (including unique expertise). A one-to-one DM has no competing recipient, so its active primary is immediately `accepted` with `dm_auto_authorized`; every `message check` also upgrades that agent's legacy active/pending DM rows, including already-read messages. This is explicit reply permission, not an obligation, and the agent may still choose `no_action`. Publishing any other active addressed `direct|dm|assigned` grant atomically records an implicit accept, so ordinary replies do not depend on a separate model-mediated command; explicit accept remains available as an early ownership signal, while no-action, transfer, and ambient grant requests remain explicit. `(replyToMessageId, senderId)` permits at most one publication per trigger/agent. A suspected typo stays pending as `better_fit`; the primary owner must accept, delegate, or abstain. Task replies are authorized only in `task.threadId`, and an active primary coordinator reserves the single parent-task claim while directed contributors publish scoped work without claiming. In the standing prompt, an early Task `accept` is an acknowledgement and the one-shot public grant is reserved for the completed result or a concrete blocker; the server enforces quantity and target, while this semantic text classification remains model-mediated. Primary publication waits through a bounded concurrent-decision window and is rejected while transfer is pending. Confirmed delivery failure releases an unconsumed grant; silent observers stop blocking after the window.
- **Control-plane WS is always the backbone**: all daemon ↔ server commands and status travel over `/daemon/connect`; none go over HTTP.
- **Seq is globally monotonically increasing and unique** (Redis INCR per server): drives unread tracking (`channel_members.lastReadSeq`), the channel-history keyset cursor (`GET /api/messages/channel/:id?before=<seq>` → older page + `hasMore`, hitting the `(channelId, seq)` index), and offline catch-up endpoint `/api/messages/sync?since=`. Thread replies keep their own thread-channel cursor, but user-facing channel badges aggregate unread thread replies onto the parent channel id; raw thread channel ids are not exposed as top-level sidebar unread keys. **Each source clears independently**: reading the channel advances only the channel-own cursor, opening a thread advances only that thread's cursor — so `POST /api/channels/:id/read` returns `{channelId:<parent>, unread:<remaining aggregated>}` (a thread read rolls onto its parent) and the client renders that exact value rather than blindly zeroing the badge (blind-zeroing hid still-unopened thread replies, which then "resurrected" on the next unread refetch). Self-authored messages do not count as unread, including task lifecycle `system` audit messages because they carry the initiating actor's `senderId`. ⚠️ In the steady-state, human-side real-time uses socket.io with full `message:new` payloads (carries the entire message object); the web client does **not** currently consume the sync endpoint for incremental catch-up.
- **An agent joins a channel "caught up" at the watermark, not from seq 0**: when an agent becomes a `channel_members` row — created → `#all` (`routes-api/agents.ts`), CLI `channel/join` (`routes-agent.ts`), admin add-member / channel-create-with-members (`routes-api/channels.ts`), or `@`-mention auto-join (`core.autoJoinMentioned`) — its `lastReadSeq` is initialized to the channel's current max seq via `core.addChannelMembers` (→ `core.channelMaxSeq`), so the agent's first `message check` surfaces only **post-join** messages instead of the channel's entire pre-join backlog (which it can still pull on demand via `message read`). The one exception is **`@`-mention auto-join**, where the watermark is `triggeringSeq-1` so the `@` that pulled the agent in stays unread (the agent must see it). **Users keep `lastReadSeq=0`** — the human UI intentionally shows channel history as unread on join. This is purely an *initial-value* change: the unread predicate (`seq > lastReadSeq`) and the reconnect catch-up idempotency are unchanged. ⚠️ Thread-membership inserts deliberately stay at `0` (task-assignee → thread, thread auto-follow on reply) so the assignment / system message that triggered the join stays visible.
- **A task always carries a number, scoped to its channel** — `taskStatus IS NOT NULL ⟺ taskNumber IS NOT NULL`. The only two paths that promote a message into a task (`createMessage({asTask})`, `convertMessageToTask`) mint the number; the status mutators (`claimTask` / `unclaimTask` / `setTaskStatus`) all carry an `isNotNull(taskStatus)` guard so they can never turn a plain message into a numberless task. Agent `task/claim`·`task/update` on a non-task message convert it first (canonical mint), so the agent CLI keeps its "grab a message as work" gesture without ever producing a `#null`. **Numbering scope is per-channel-type**: `dm` channels number per-DM (`tasknum:dm:<channelId>` — each 1:1 conversation has its own #1), every other type shares the per-server counter (`tasknum:<serverId>`); `reconcileCounters` advances both scopes to the live DB max on boot. ⚠️ A bare `task #N` is unique only **within its scope** (a DM #1 and a channel #1 coexist) — see tech-debt-tracker.
- **@mentions are stored as structured `message_mentions` rows** and are the single source of truth: the frontend linkifies `@name` from each message's own `mentions[]` (`messageRender.processMessageContent`), so an `@` the server never recorded (e.g. a non-member in a private channel / DM) renders as plain text rather than a fake clickable mention. Two read paths, do not conflate: the **channel-aggregated inbox** (`/api/channels/inbox`) computes `hasMention` only inside `if (unreadCount > 0)`, so it surfaces an `@` only while its channel still has unread — an already-read `@` drops off (by design, it's a triage list). The **Mentions activity stream** (`/api/mentions`, `mentions_target_idx`) is message-grained and read-or-unread — the canonical "who pinged me" history powering the Inbox Mentions tab.
- **A non-empty message body never renders as an empty bubble**: `messageRender` renders markdown but **never raw HTML** (no `rehype-raw` — no arbitrary-HTML/XSS surface). Raw HTML in a body is downgraded to **literal escaped text** (`remarkHtmlAsText`), so an all-HTML message shows its source instead of being silently dropped. Rendering must degrade visibly, never to nothing.
- **Mention auto-join (Slack-style) — a thread inherits its parent channel's @-reach**: `@`-mentioning someone not yet in the channel adds them to `channel_members` before the mention is recorded (`core.autoJoinMentioned`, candidate pool from `core.mentionAutoJoinPool`), so the mention actually delivers (wake / inbox) instead of being silently dropped. The pool = who already has access to the space, so adding them leaks nothing: a **public `channel`** → the whole workspace; a **`thread`** → resolves to its **parent channel** and reuses *its* reach (the same parent-channel inheritance `socketio.canReadChannel` uses for read access — public-parent thread → workspace, private-parent thread → its parent's members only); **`private` / `dm`** (and threads under them) → current members only, so an `@` to a non-member stays a no-op (never pulls in an outsider → no leak). ⚠️ Before this fix `thread` was excluded alongside `private`/`dm`, so `@`-ing a teammate who hadn't replied in a thread yet was silently dropped — mention unrecorded, agent never woken (see tech-debt I35).
- **Threads anchor only on real (user/agent) messages**: `resolveTarget` (`core.ts`) creates a thread off the parent message a `#channel:<shortid>` resolves to; a `shortid` that lands on a **system message** ("X created task / claimed / moved …") is rejected (`return null` → `/agent-api` `404 TARGET_FAILED`). System messages have no "open thread" affordance in the UI, so a reply threaded onto one would be delivered + persisted but **unreachable** — the same silent-loss failure mode as an empty bubble.
- **Thread targets must round-trip through `resolveTarget`** — the stable thread identity is the **parent message id prefix**, never the thread *channel* id. The agent plane accepts both `#channel:shortid` (caller-relative, good when the agent can already resolve the parent channel/DM) and `thread:shortid` (stable across public/private/DM and cross-agent handoff). The message header (`fmt` in `routes-agent.ts`) still renders a thread-anchor message as `#chan:<m.id8>` (its own id = the thread's parent), matching `addressableTarget`'s historical convention. Emitting the thread channel id instead made the shown target unresolvable (404), so agents reusing it silently failed to reply into the thread (`test/threadTargetRoundtrip.integration.ts`). Handoff / follow-up paths should prefer `thread:shortid`, because a new assignee may be a member of the thread but not of the parent private channel or original DM.
- **`name` is the @mention handle, validated as a machine-safe identifier on creation** (`^[A-Za-z][A-Za-z0-9_-]*$`, ≤`MAX_AGENT_NAME` (64) chars since the column is unbounded `text`, `core.invalidAgentName`, enforced in `POST /api/agents` + frontend `CreateAgentModal`). It is used verbatim as `@<name>` (`parseMentions`, CLI, web) and as the `dm:@<name>` lookup key, so spaces / punctuation / emoji / leading digits would break mention parsing and DM resolution. Display-friendly text (Chinese, spaces, emoji) belongs in `displayName`, which is unconstrained. `users.name` carries a DB `UNIQUE` constraint; `agents.name` now has a **per-server partial unique index** `agents_name_uniq` `(serverId, name) WHERE deleted_at IS NULL` — unique among **live** agents only, so a soft-deleted name frees up for reuse; `POST /api/agents` rejects a duplicate live name with **409** via `ON CONFLICT` against this index (race-proof — no SELECT-then-INSERT gap), closing the `resolveTarget` silent first-match blind spot. See tech-debt I26.
- **`POST /api/agents` requires a valid, same-tenant `machineId`** — the web create modal marks Computer required and disables Create until one is picked; the route independently rejects a missing/empty `machineId` with **400** and a `machineId` that doesn't resolve to a `machines` row for this `serverId` with **404** (existence-hiding, consistent with this repo's other ownership pre-checks). `machineId ?? null` is gone from the insert. This closes the *create-time* half of tech-debt I77 (an agent can no longer be created dead-on-arrival with zero possible way to run); the *rebind* half is still open — `PATCH /api/agents/:id` still cannot change `machineId` after creation, and pre-existing unbound rows (e.g. `seed-dev.ts`'s `@dev-bot`, which inserts directly via `db.insert` and bypasses this API) still rely on the legacy `broadcastToDaemons` fallback described above.
- **Credential hygiene**: plain-text API keys / tokens only in DMs or private channels, **never in public channels**.
- **`storageKey` is driver-agnostic**: switching local ↔ S3 requires no changes to business logic.
- **Agent communication is unified through the `open-tag` CLI** (generated at `~/.open-tag/bin`, injected into the agent's PATH). Claude agents do **not** use MCP. What the agent cannot see in its context effectively does not exist — knowledge is persisted to the workspace `MEMORY.md`.
- **Agents are single-threaded / one agent = one session (intentional design, not a limitation)**: one agent = one process, one session; durable Turns execute in strict FIFO. While busy, a later Turn remains server-persisted but recipient-invisible, has no Activity preview, and is not written to runtime input until the current adapter returns to terminal `online`. Legacy non-Turn notices may still debounce. **Parallelism lives at the team level (multiple agents collaborating), not inside a single agent** — "employee mental model" (see §I): scale by hiring more people, not by making one person multi-thread. `Task` and sub-agents are **not blocked** (inherited from the host machine's tooling), but the standing prompt does not encourage their use → **do not build logic that depends on which sub-agents happen to be installed, and do not add `/fork`-style intra-agent parallelism**.
- **Single file ≤ 1000 lines** (split if exceeded). The former 1093-line `routes-api.ts` is now the `routes-api/` directory; the largest single file is currently `src/server/core.ts` — check with `wc -l` rather than trusting a written count (recorded numbers here drifted stale twice).

## IV. Boundaries (made explicit because they are easy to miss)

- `/api` (human) ↔ `/agent-api` (agent) ↔ `/daemon` WS (control) — three independent entry points demultiplexed in order in `index.ts`.
- `replyCoordination.ts` is the sole reply-grant state transition boundary; prompts and runtime adapters may request/consume through `/agent-api`, but cannot authorize publication themselves.
- `conversationTurns.ts` is the sole burst-admission/Turn-lifecycle and attempt-fencing boundary; `conversationTurnDispatch.ts` is the sole responsibility-selection and wake-delivery boundary.
- `realtime.publish()` — the **sole fan-out point** for all human-side real-time events.
- `storage.saveObject/readObject` — the **sole boundary** for all object I/O.
- `runtime.ts` interface — all differences between claude / codex / copilot / opencode / kimi / pi / cursor (incl. persistent-process vs one-shot-per-turn lifecycles) are contained behind adapters; the upper-layer `agentManager` is runtime-agnostic.
- Daemon ↔ server protocol (message `type`) — D→S defined in `ws.ts`; S→D defined in `core.ts` / `conversationTurnDispatch.ts` / `daemonHub`. This is the control-plane contract. **Turn delivery fence (`delivery-admission-v2`, daemon 0.13.0+)**: S→D `agent:deliver` carries `{turnId, turnMessageCount, attention, deliveryId, streamId, seq}`. D→S `agent:deliver:pending` renews the waiter without accepting work. At its FIFO head the daemon sends `agent:deliver:ready`; the server validates the authenticated machine/tenant/agent/Turn/recipient/sequence, commits `deliveryAdmittedAt`, then replies S→D `agent:deliver:admitted`. Only then may the daemon inject runtime input. D→S final ACK confirms adapter acceptance; NACK/disconnect releases an unpublished in-flight commit. Retry reuses `deliveryId`; the daemon ledger suppresses repeated runtime work while re-running the server commit handshake after ACK loss/reconnect. **Lifecycle control**: S→D start/stop/reset carries `requestId`; daemon 0.12.0+ advertises `agent-control-ack-v1` and replies `rpc:ack/nack` after the operation settles. **Inline Activity extension**: D→S `agent:reply` carries `{agentId, channelId, streamId, op:start|activity|done|error, entries?}`. `activity` entries are contextual status/thinking/tool events for an ephemeral per-message disclosure; they are never provisional public-message text. A persisted agent message absorbs the current segment, while `done/error` finalizes the last segment or creates a no-message receipt. **Model-discovery extension**: S→D `{type:"probe-models", runtime, requestId}` (sent by `runtimeModels.ts` via `requestDaemonByMachine`); D→S `{type:"models", requestId, runtime, models, error?}` (replied by `listModels.ts` via `index.ts`). Uses the existing `requestId` / pending WS-RPC mechanism. **Version-skew NACK**: for any S→D message carrying a `requestId` whose `type` the daemon doesn't know, the daemon replies D→S `{type:"rpc:nack", requestId, error}` (daemon ≥0.10.1) so the server surfaces "daemon too old" instead of a generic RPC timeout.

## V. Cross-cutting concerns

- `env.ts` must be imported before any module that reads `process.env`.
- `log.ts` spans all three planes; JSONL logs land in `~/.open-tag/logs/`, while console streams are split so platform logs and CLI stdout semantics stay usable.
- `redis.ts` seq / task-number / pub-sub / wake queue are shared infrastructure for real-time and sync.
- `auth.ts` (human/agent) + `ws.ts` (daemon key) + `scopes.ts` (agent permissions) + `capabilities.ts` (human role permissions) are enforced across all routes.

## VI. Maintenance notes

- This file is intentionally short. **Do not keep it in sync line-by-line with the code.** Come back only when a module's purpose, boundary, or invariant changes.
- `CLAUDE.md` acts only as a **directory** pointing here and to `docs/`; architecture detail does not belong in `CLAUDE.md`.
- When changing `src/db/schema.ts`, regenerate `docs/generated/db-schema.md`. When adding or removing routes or CLI subcommands, update the Codemap in §II. When completing a feature, tick it off in `FEATURES.md`.
