# open-tag Feature Checklist

> Strategy: **breadth-first** — ship each feature working end-to-end, polish later. Checked = implemented and self-tested.
> Scope: this file tracks feature **status** (checkbox + a short note; long verification narratives belong in the PR).
> The authoritative route/API surface is `ARCHITECTURE.md` §II — do not treat this list as an API reference.

## P0 Foundation
- [x] Project bootstrap + root `AGENTS.md` map (`CLAUDE.md` imports it; architecture detail lives in `ARCHITECTURE.md`)
- [x] Docker Compose: Postgres + Redis (`docker-compose.yml`, ports 5433/6380)
- [x] Database schema (`src/db/schema.ts`: users/servers/serverMembers/machines/agents/channels/channelMembers/messages/messageMentions/reactions/attachments/reminders/knowledge)
- [x] DB client (drizzle + postgres.js) `src/db/index.ts`; `db:push` migrations (13 tables created)
- [x] Redis client + helpers: `seq:{serverId}` INCR, SSE pub/sub `src/redis.ts`
- [x] Bootstrap seed: default `open-tag` workspace + owner + #all only; machines and agents are created explicitly through onboarding

## P1 Control Plane (server ↔ daemon) — Verified end-to-end
- [x] WS `/daemon/connect?key=` TS implementation + machine registration/heartbeat persistence + push to frontend (machine ready verified)
- [x] Message protocol: agent:start/deliver + ready/status/activity/trajectory/session (daemon↔server fully wired)
- [x] `--resume` wake-up supported; machine online status persisted
- [x] **Reconnect robustness + per-machine key rotation**: the server now rejects an unknown/removed `sk_machine_*` key with WS close `4001` (was a silent `ws.close()`), so the daemon stops the per-second reconnect storm — it backs off to the 30s cap and logs an actionable error; backoff resets only after the server accepts the connection, not on raw socket open. A new `POST /api/servers/:id/machines/:id/reconnect` rotates the key on the **same** machine row, and a **Reconnect** button on offline machines (`views/misc.tsx`) lets a lost-key machine rejoin without spawning a duplicate row. Verified: unit tests for the backoff/reject state machine (`connection.test.ts`, 3 cases), live daemon E2E (bogus/old key → `4001` + 30s backoff; rotated key → online on the same row, machine count unchanged; bootstrap happy-path regression), and browser (Reconnect button → new command modal)
- [x] **Reconnect catch-up + offline reachability (agents no longer go silently unreachable)**: when a machine reconnects, the server wakes only the agents on it that accumulated a *wakeable* backlog while offline (`reconnectCatchup.ts`) — they self-check the inbox and process every message missed offline, the symmetric counterpart to the human-side reconnect sync. The wake criterion mirrors `createMessage` (DM/@ always, ambient only with `inbox:receive`) so idle agents are never woken (no token-burn). Frontend: the Composer shows an offline reachability hint for the agents a message will reach (channel @-mention + DM peer — channels had none before), the misleading DM "queued and delivered" copy is corrected to match catch-up, and a lightweight global toast (`toast.tsx`) surfaces start/restart 503s and create-while-offline (`started:false`) that were silently swallowed. Verified: `isWakeable` unit tests (4 cases); live dev E2E (hard-offline @ → reconnect → catch-up `woke 1` → agent `message check` → reply; selective wake `scanned 2 woke 1`; ambient-no-scope agent not woken; online → no hint; start/create toasts; channel + DM offline banners). Soft-offline deliver / 30s cooldown / server-restart trigger are logic-verified (same `onReady` path), not separately E2E'd.
- [x] **Idle-sleep**: agent auto-kills process when idle (`OPEN_TAG_IDLE_MS`, default 10 min), resumes on next `--resume`/thread/resume; agent:stop/sleep linked

## P2 Agent Data Plane (bundled `open-tag` CLI) ★ — Core verified
- [x] `open-tag` CLI (commander), auth via machine key + x-agent-id, proxied to `/agent-api/*` (verified, stdout clean)
- [x] Subcommands: message check/send (stdin heredoc)/read/react/search, server info, channel join/members/leave, task list/claim/assign/update/new/unclaim, thread reply/read/unfollow, profile show/update, reminder schedule/list/cancel/snooze, attachment upload/view, action prepare (`src/cli/index.ts` mirrors `/agent-api`)
- [ ] Subcommand pending: knowledge (create/list/search — no endpoints or CLI verb yet, see P7; mentions need no verb — `@name` inside `message send` is the mechanism)
- [x] Daemon spawns agent with injected system prompt (Current Runtime Context + open-tag CLI spec) + PATH-injected open-tag wrapper (verified)
- [x] **Runtime adapters (unified Runtime interface)**: claude (stream-json) + codex (app-server JSON-RPC, resume fallback) + copilot (one-shot pipe JSONL, `--session-id` resume) + opencode (one-shot `run --format json`, `--session` resume, stdin-closed + `NODE_OPTIONS`-stripped) + kimi (Kimi Code one-shot stream-json, `-r` resume, config.toml-only auth) + pi (Pi Coding Agent one-shot `--mode json`, `--session` resume, native `--append-system-prompt`) + cursor (Cursor Agent one-shot Claude-style stream-json, `--resume`, `NODE_OPTIONS`-stripped) — **all verified on real hardware** (copilot 1.0.61 / opencode 1.15.5 / kimi-code 0.19.2 / pi 0.73.1 / cursor-agent 2025.09.17: reply / multi-turn resume / trajectory / failure surfaced, dev:e2e) + **hermes (experimental)**: one-shot `hermes chat -q` per turn (`oneShotWake`), profile selection via `HERMES_HOME`/`HERMES_PROFILE` (live-discovered from `~/.hermes/profiles`; Hermes keeps provider credentials), native `session_id:` stderr resume with missing-session fresh-retry, plus a **final-response bridge** (stdout of a turn that read messages but never ran `message send` is bridged to the channel it read — evidence-gated, noise/error-filtered, freshness-hold-aware, loud on refusal) — verified live (hermes 0.16.0, real gpt-5.4 profile: reply / resume / profile injection / bridge success + hold-block + error-refusal, dev:e2e)
- [x] **Mention routing**: only @ -mentioned agents are woken in a channel (+ DM agent); others correctly stay idle
- [ ] codex enhancement: per-agent `CODEX_HOME` isolation + auth/MCP injection; raw-v2 item/* trajectory detail

## P3 Messaging Core (human /api/* + realtime) — Backend verified (smoke tested)
- [x] REST: auth (dev-login/register/login/me) / servers / agents / channels / members / machines / messages/channel/:id / messages/sync
- [x] Auth: Bearer JWT + `x-server-id` middleware (humans); machine key + `x-agent-id` (agents)
- [x] **Seq incremental sync** (`GET /api/messages/sync?since=`, verified); human realtime is socket.io full `message:new` (`socketio.ts` — the once-planned SSE `/api/push/prompt-events` endpoint was never built and is not coming back)
- [x] channel_member + unread (lastReadSeq) + unread summary (`GET /api/channels/unread` aggregates channel-own + followed-thread unread, shared with `POST /:id/read` — see the honest-badge entry in P4)
- [x] **@mentions[] structured storage** + frontend highlight rendered from the message's own `mentions[]` (`messageRender.tsx` — a non-member `@` renders as plain text)
- [x] **Markdown rendering polish**: chat messages and Workspace `.md` preview share the same GFM rendering surface for GitHub-style alerts, task lists, tables, blockquotes, code blocks with language/copy chrome, color swatches (`#92B6FF`, `rgb(...)`, etc.), safer raw-HTML-as-text handling, and internal reference pills (`@agent`, `@human`, `#channel`, thread, task)
- [x] **Saved Messages / Bookmarks**: `saved_messages` table + GET/POST/DELETE/check endpoints + web right-click "Save message" / Saved view / sidebar count; 13 item fields (messageId/channel*/parent*/sender*). Full curl + browser end-to-end verified (right-click → save → Saved view → unsave + DB)
- [x] **Channel lifecycle**: `PATCH /channels/:id` (rename/description/visibility), `POST /channels/:id/archive`·`unarchive`, `DELETE /channels/:id` (soft-delete deletedAt); all gated on manageChannels; `GET /channels` hides archived by default (`?archived=include` to show). Frontend channel header "⋯" settings menu (rename/archive/delete, visibility-gated). curl 5-way verified + browser menu verified. DM auto-create deduplication in place; private channel visibility enforced (GET /channels only shows private to members); **join private channel gated** (`POST /channels/:id/join` → 403 invite-only for private, curl verified)
- [x] **Channel message pagination (keyset)**: `GET /api/messages/channel/:id?before=<seq>&limit=` returns a `hasMore` flag (cursor on the monotonic `seq`, hits the `(channelId, seq)` index — no offset drift; `hasMore` via a `limit+1` sentinel like the search/mentions routes, so no exact-page-boundary false positive). Web Chat loads the latest page (50) then lazy-loads older pages on scroll-to-top (viewport anchored — no jump) and caps the live-tail in-memory window at 400 (drops oldest, re-fetchable via the cursor; suppressed mid-pagination to avoid a `hasMore` race). Verified: curl (first page / `before` older page — no overlap or gap / `before=abc|0|-5` + `limit=0|-5` fall back to defaults / exact-page boundary `limit=60`→`hasMore=false` / `limit` clamp / empty channel / cross-tenant 403) + browser (initial 50, scroll-to-top 50→60 viewport-anchored, stops at `hasMore=false`). Deep-links (`?msg=`/`?thread=`) to a message outside the loaded window page older history until the target loads (load-until-found), bounded by `hasMore`; an in-flight older-page fetch is dropped if the channel switches mid-flight. Live-tail trim is unit-tested (`appendWithCap`) but not browser-E2E'd (needs 400+ realtime msgs)
- [x] **Date dividers + calendar-time timestamps**: channel/thread message lists insert a day-boundary divider (`.date-divider`, `web/src/styles.css`) between messages sent on different calendar days — "Today"/"Yesterday" for the two most recent days, else a localized weekday+date (`Intl.DateTimeFormat`, year included once it's not the current year); i18n keys `chat.dateToday`/`chat.dateYesterday` (`isSameLocalDay`/`fmtDateDivider`, `web/src/format.ts`). Per-message timestamps now always show full date+time (`fmtDateTime`) instead of the old time-only `fmtTime` (removed — misleading once messages span multiple days), consolidating the 8 former call sites (Chat.tsx main list/thread panel/action-card/attachments, misc.tsx inbox/mentions/search, Members.tsx reminders). Verified: typecheck + build clean, all 316 unit tests pass (incl. locale drift guard), and Playwright against a live server confirmed all 4 label tiers render correctly in both the main channel and thread panel (Today → Yesterday → this-year weekday+date → cross-year weekday+date+year) with zero console errors
- [x] /agent-api/*: message check/send/read, server info, channel join, task list/claim/update (full chain verified)
- [x] WS daemon control plane: connect/machine registration (persistent machine-id + ready:ack) / heartbeat / agent:start·deliver broadcast — real daemon integration tested (spawn claude/codex; agent:deliver carries inbox-notice metadata targetName/msgShort/isTask)
- [ ] Unarchive/restore UI: backend `POST /channels/:id/unarchive` exists but has no frontend surface (archived channels are hidden with no browse toggle). The rest of the old "UI gaps" line shipped: DM creation, private-channel visibility toggle, channel member add/remove, archive are all in the UI (`Chat.tsx` EditChannelModal / ChannelMembersModal)

## P4 Tasks & Threads — Implemented (slice03 evidence-driven alignment + end-to-end verified)
- [x] Task field on message: As Task / right-click convert to task; `GET /api/tasks/channel/:id`, `/api/tasks/server`, `POST .../convert-message`, `DELETE /api/tasks/:id` (delete = revert to plain message, source message preserved)
- [x] Task claim/status flow `todo→in_progress→in_review→done→closed` (claim atomically sets in_progress; non-enum values validated 400; done auto-creates thread); kanban cards link to source message; socket `task:created/updated/deleted` realtime (agent status updates also push live to kanban)
- [x] **Board layout toggle** (`TaskBoard.tsx`): Board view switches between a **horizontal Kanban** (columns side by side, min-width + horizontal scroll so the narrow embedded channel tasks tab degrades gracefully — default) and the legacy **vertical stack**; choice persisted in `localStorage["open-tag.tasks.boardLayout"]`; drag-and-drop status change works in both layouts. CSS-regression unit test (`test/tasksBoardLayout.unit.test.ts`) + browser-verified (default horizontal / toggle to stack / reload persists / drag across columns changes status / embedded narrow tab not squashed)
- [x] **Board move interaction** (`TaskBoard.tsx`): move a task between columns by **drag-and-drop _or_ clicking the status pill's menu** (no longer drag-only; the pill/claim/delete buttons `stopPropagation` the `pointerdown` so dnd-kit's sensor doesn't swallow the click — fixes "the menu only opened after I moved the mouse"). The board **fills the pane** (`.board-scroll` flex column) and columns are **full-height equal lanes** (`align-items:stretch`; body `flex:1` + internal `overflow-y` for many cards), so the **whole column** is the drop target — not just the cards or the title strip (fixes "the drop zone was only this big"). The drop indicator (`.drop-slot`) is appended after the cards instead of the old `.drop-hint` banner that **overlapped the dragged card**. Moves are **optimistic** and the card **FLIP-animates** from its old column to its new one (~220ms ease-out), no first-load flutter, with a `prefers-reduced-motion` instant fallback. CSS-regression unit test (`test/tasksBoardMoveUx.unit.test.ts`) + browser-verified (click-move opens instantly + relocates the card / full-height column drop lane — empty IN REVIEW 50px→637px / non-overlapping drop-slot / FLIP animation / reduced-motion rule present)
- [x] **DM tasks distinct from channel tasks**: DM tasks number **per-DM** (`tasknum:dm:<channelId>` — each conversation has its own #1, independent of the workspace counter); DMs get their own lightweight `chat | tasks` board (Chat.tsx); the global board labels DM tasks `@peer` (vs `#channel`) so a per-DM #N is never confused with a channel #N. **Invariant enforced**: `taskStatus ⟺ taskNumber` — `claim/unclaim/setStatus` guard `isNotNull(taskStatus)` so a plain message can't be promoted into a numberless task; agent `task/claim`·`task/update` on a non-task convert it first (canonical mint). Fixes the `#-`/`#null` task an agent produced by claiming/updating a plain DM message. Verified: real HTTP agent-api chain (per-DM #1 independent of channel; agent update/claim on a plain message → real number, not null; human set-status on a plain message → 404, not promoted) + browser (status popover renders above lower cards; DM `@peer` board; per-DM numbering) + `scripts/heal-task-numbers.ts` backfills legacy numberless tasks
- [x] **Agent-to-agent task handoff**: an agent can reassign an existing task to another agent via `open-tag task assign` / `/agent-api/task/assign` (by message id or `channel + task number`). The handoff updates the assignee, promotes `todo → in_progress` when needed, records a system audit message in the task thread, auto-adds the assignee to that thread, and wakes/delivers to the assignee on the thread target. Verified by real DB integration (`test/taskAssign.integration.ts`) and agent-route integration (`test/taskAssignAgent.integration.ts`).
- [x] Threads (parentMessageId-derived channels)
- [x] **Thread follow/done system**: `POST /channels/threads/follow`·`unfollow`·`done`·`undone` + `GET threads/followed` (filters done); **auto-follow** (reply in thread → `core.createMessage` auto-enrolls + this-thread done resets → back to Inbox); `channelMembers.threadDoneAt` (per-user done). Frontend ThreadPanel head "Mark Done / Unfollow" buttons; Inbox shows followed threads (done removes from Inbox). curl full-flow verified (follow→in inbox / done→removed / undone→restored / unfollow→removed) + browser (ThreadPanel head buttons)
- [x] **Honest unread badge (no "read message resurrects as unread") + jump-to-unread-thread bar**: a channel badge aggregates channel-own + followed-thread unread, and each source clears independently — but the client used to blind-zero the whole badge on channel-read, so a still-unopened thread reply "resurrected" the badge on the next unread refetch. Now `POST /api/channels/:id/read` returns `{channelId:<parent>, unread:<remaining aggregated>}` (shared `unreadMapForUser`, reused by `GET /channels/unread`) and `store.markRead` renders that exact value. Because the surfacing unread can live in a folded thread invisible in the main timeline, the channel now shows a pinned "💬 N unread thread replies · View" bar (`web/src/views/Chat.tsx`, fed by `GET threads/followed`); View opens the first unread thread — fetching its parent via the new `GET /api/messages/:id` when it isn't in the loaded page — marks it read, and advances to the next. Verified: integration `readReturnsRemainingUnread.integration.ts` (read channel → thread portion stays / read thread → parent badge clears / no resurrection) + `singleMessageGet.integration.ts` (happy + private-channel non-member 404 IDOR) + curl on the live server (badge 1 → read channel `unread:1` → read thread `unread:0`) + browser (badge stays 1 after channel-read, bar appears, View opens thread + clears badge — `.shots/unread-banner-{before,after}.png`)
- [x] **Inbox Mentions tab — message-grained @-activity stream**: the unified Inbox's Mentions filter is its own message-grained list (`GET /api/mentions`, backed by `mentions_target_idx`) of every message that @-mentions you, **read or unread** — each row deep-links straight to that message (`?msg=`) or opens the parent thread. Distinct from the channel-aggregated `all`/`unread` filters, which only badge an `@` while its channel still has unread (so an already-read `@` would otherwise drop off the Mentions view entirely). Verified: real send (`tester` @you → `message_mentions` row) → mark read → `/api/mentions` returns it `read:true` while the old `inbox?filter=mentions` reports `hasMention:false`; browser (Mentions tab lists the read @ + click jumps to the exact message). **Paginated** (`limit`/`offset` + `hasMore`, Saved-style "Load more" button — 50/page); verified by seeding 63 @s → first page 50 + Load more → all 63, then `hasMore:false`
- [x] **Action Cards (B-mode quick-commit human-in-the-loop)**: agent lacks channel:create/agent:create scope → `open-tag action prepare --target <ch>` (stdin action JSON, variants channel:create/agent:create) sends proposal card → human opens **pre-filled creation modal** to review/edit/confirm → creates as themselves → `POST /api/actions/:id/mark-executed` marks "executed by X". Verified curl + browser end-to-end
- [x] **Attachment enrichment**: human-side image inline + **fullscreen lightbox** (scroll-zoom/drag-pan/Esc), **video inline player** (`<video controls playsInline preload=metadata>` + codec fallback); agent-side `attachment view` **downloads bytes to local `attachments/`** (image → runtime vision). Browser verified
- [x] **Unified message composer**: channels, DMs, and threads share one `Composer` component (`web/src/views/Composer.tsx`) — text input, attachment upload (button / paste / drag-drop with progress), and @mention autocomplete behave identically everywhere. Threads previously had a stripped-down composer (no uploads, no @ menu) and now match channels; **As Task stays channel/DM-only** (a thread reply is never a task). The channel composer's state + handlers moved out of `Chat.tsx` into `Composer`, removing the two-composer drift. Browser verified (channel: send + @menu; thread: upload buttons + @menu present; typecheck green)
- [x] **Pluggable attachment storage**: `src/server/storage.ts` boundary — local disk (default, zero-config) or any **S3-compatible backend** (MinIO/Garage/SeaweedFS/OSS) via `OPEN_TAG_STORAGE=s3` + `OPEN_TAG_S3_*` (`@aws-sdk/client-s3` optional dep). Fail-loud config validation (`s3Config()` names the missing var). Bytes travel over HTTP so a remote daemon works cross-host. **Local-MinIO E2E verified**: human + agent round-trips byte-identical, missing-config → `500` + clear detail without crashing the server (regression test for the upload error-handling crash it surfaced)

## P5 Agent Collaboration — Core loop end-to-end verified (slice 01)
- [x] Agent A `open-tag message send @B` → B wakes → works → reports back — collaboration complete (e2e verified: real web send → two agents cross-runtime bidirectional)
- [x] Routing: @mentions/channel membership determines which agents wake (non-mentioned agents correctly stay idle); busy-state stdin notification (deliver 3s debounce batching + inbox-notice metadata)
- [x] **Agent Live Trace panel persistence + responsive layout (task #2)**: the right-column live-trace feed (all agents' thinking/tool calls) now lives in a **bounded ring buffer in the store** (`web/src/trajBuffer.ts`, cap 300, drop-oldest) instead of per-Chat-view state, so traces **survive channel/DM switches** (no longer wiped). Backend `agent_activity_log` is **pruned to the newest 500 rows per agent on insert** (`ws.ts pruneAgentActivityLog`), bounding table growth. CSS: long trace lines **wrap** (`overflow-wrap:anywhere`) and the panel no longer scrolls horizontally (`overflow-x:hidden`). Verified: `trajBuffer` unit 6/6, prune integration 6/6 (isolated DB), CSS no-h-scroll + wrap in a real browser. Refresh-restore deferred (per @fancy); see tech-debt I37.
- [x] **Live agent bar (sidebar bottom)**: a persistent, workspace-wide "who's running right now" strip pinned below the channel/DM list (`web/src/views/LiveAgentBar.tsx`, in `ChatSidebar`). Shows agents whose `status==='active'` **and** `activity∈{working,thinking}` (status guard hides phantom agents stuck on stale `working` after an unclean stop); most-active (working before thinking) shown with avatar + pulsing status pip + live `activityDetail`, the rest folded into a `+N` popover. Clicking an agent opens its profile on the **Activity** tab in the right column via the store's one-shot `openAgentPanel` signal (consumed by `Chat.tsx` → `setProfile` + `?agentTab=activity`, cleared on close) — **no page nav, no DM**. Editorial skin (hairline divider, reuses `.dot` colors, `prefers-reduced-motion` pulse off); rail↔sidebar divider strengthened (`--hair`→`--hair-strong`) for container separation. Resting state "No agents running". Browser-verified in an isolated worktree: resting / 1-active / +N popover / guard (online + stale-inactive hidden) / click→inline Activity panel without leaving the channel; `npm run typecheck` clean, no console errors.
- [x] **Message header status badge moved into the name/timestamp line; Live Trace panel groups fragments into message-bar-like blocks**: the agent activity badge (`online`/`working`/`thinking` `.msg-activity`) no longer sits on its own `.msg-subhead` line under the sender name — it now renders inside `.msg-head` itself, pinned to the far right via `margin-left:auto` (`web/src/views/Chat.tsx` main channel row only; the description/`msg-role` subhead is unaffected and still renders when present). The Live Trace panel (`web/src/views/Chat.tsx` traj-col + new `groupTraj()` in `web/src/trajBuffer.ts`) no longer renders one `<div>` per raw streamed fragment — consecutive `text` fragments from the same agent/turn merge into a single running block with a real agent avatar, `tool_start` fragments stay as discrete pills, and a `boundary` marker (pushed when an agent's activity leaves working/thinking, see `store.tsx` `agent:activity` handler) starts a fresh group for that agent's next turn instead of running on from a finished one. Verified: `messageHeaderLayout` unit 17/17, `trajBuffer`/`groupTraj` unit 11/11, full suite 268/268; real browser E2E against a live `claude/sonnet` dev-bot (`npm run dev:e2e:up`) confirmed both the header badge position and the grouped/avatar trace panel end-to-end.
- [x] **Fixed: a trailing agent-reply delta after absorption could spawn a phantom channel message with no row in the DB** (`web/src/lib/agentReplyPreview.ts` `applyAgentReplyPreview`): once a stream's preview is absorbed into a real persisted message and settles/swaps (past `AGENT_REPLY_FINAL_SETTLE_MS`), the array entry is no longer `AGENT_REPLY_PREVIEW_TYPE`, so a late `delta` for that same `streamId` (e.g. a claude turn that calls `message send` and then keeps talking) used to fall through and spawn a brand-new orphan preview bubble that looked exactly like a real message and never got replaced, since the agent never posts a second message for it — gone only on refresh. Only a `"start"` may now originate a preview; a stray `delta`/`done`/`error` with no matching stream is silently ignored, mirroring the `done`/`error` idx<0 guard that already existed. Found live via dogfooding (real DB query showed the phantom row never existed; server log showed the agent's last REST call ended ~3s before the phantom appeared, timing that only lines up with the WS `agent:reply` channel). Verified: `agentReplyPreview` unit 18/18 (2 new regression tests), full suite 270/270; reproduced live against a real `claude/sonnet` dev-bot both before the fix (phantom appeared) and after (gone, DB and UI match exactly).

## P6 Workspace Settings & Agent Creation & Profile
- [x] **Multi-workspace (community) + server switcher + role permissions**: a user can belong to multiple servers; `POST /api/servers {name,slug}` creates workspace (createServer: server + owner + default #all "General channel for all members"); top-left brand = switcher (ServerSwitcher.tsx: list `GET /servers` all + switch + create); bootstrap picks current server by URL `/s/:slug` (not hardcoded servers[0]). **Capability permission layer** (`src/server/capabilities.ts`, 8 caps × owner/admin/member): write endpoints (create/delete channel/agent/server settings/avatar) enforced via `requireCap`; `GET /servers` returns role + capabilities → frontend hides/shows by cap (member does not see create buttons). Browser verified (switcher list / member hidden create buttons) + curl (POST servers / member → 403 / owner → 200)
- [x] **Member invites · join-link (S2a: full backend + invite management UI)**: owner/admin generates invite link (configurable role admin/member, use count) in Settings › Invite Members → share; backend `join_links` table + `GET/POST/DELETE /servers/:id/join-links` (manageMembers, links only grant admin/member not owner) + `GET /auth/invite-info` (public) + `POST /auth/accept-invite` (join by token, idempotent, auto-adds to #all). curl full-flow verified (create link → invite-info → register carol → accept → becomes member + useCount++) + browser invite UI verified
- [x] **Register/login + invite landing page (S2b)**: real register/login pages (`/login`·`/register`, backend register/login already in place) + `/join/:token` landing page (Auth.tsx: invite-info → unregistered user register/login, logged-in user one-click join → accept-invite); bootstrap prefers `localStorage.open-tag.token` (real login), falls back to dev-login (`?as=` switch unaffected, auth routes skip auto-login). Auth failures now return stable error codes (unknown email vs wrong password vs register conflicts) and render localized, accessible form messages (`role=alert`, labeled fields, IME-safe Enter). Browser end-to-end verified (user joins via link → member with no create buttons; clean `?as=you` dev flow intact; unknown-email login shows actionable English/Chinese copy) + **logout** (Settings › Account "Log out" → `store.logout` clears open-tag.token/devuser → /login; JWT stateless so frontend clear is sufficient, no backend endpoint needed. Browser verified: logout → /login + token cleared)
- [x] **Member role management (S3)**: `PATCH /api/servers/:id/members/:uid {role}` (changeMemberRoles) + `DELETE` (manageMembers, also removes from server channels); constraints: cannot change self + **last owner cannot be demoted/removed**; HumanProfile "Member Management" card (role dropdown owner/admin/member + remove button, cap-gated). curl 5-constraint verified (owner changes other → 200 / self → 400 / last-owner demote → 400 / member changes other → 403 / remove → 200) + browser (card renders + role change member→admin reflects immediately)
- [x] **Empty-server onboarding → in-modal connect-computer wizard**: a new workspace with no machines auto-shows a multi-step wizard (`ConnectComputerWizard` in `views/ConnectComputerWizard.tsx`) that carries the user end-to-end **inside the modal** instead of bouncing them to the Computers page — intro ("Connect a computer" + a "Don't remind me again" opt-out) → generate a ready-to-run `npx @fancyboi999/open-tag-daemon@latest … --api-key …` command (`daemonConnectCommand`) + a live "Waiting for computer to connect…" pulse → on the daemon coming online (socket `machine:status`), a "Computer Connected" success card (`hostname · os · runtimes`) with an optional friendly **rename** (`PATCH /api/servers/:id/machines/:id`, cap-gated + tenant-isolated) → Done. One component backs all three entry points: `onboard` (auto-show, gated on `machines.length===0 && capabilities.manageMachines`; once-per-tab sessionStorage dismiss + permanent localStorage opt-out), `add` (Computers "+", starts at the connect step), `reconnect` (offline machine → rotates the key). Replaces the old `AddComputerModal` + `ConnectMachineModal` (both removed — one source of truth). Browser-verified end-to-end against a real daemon: auto-show → command → daemon connects → success card → rename "My Dev Mac" persisted (`status=online`, `os=darwin arm64`, 7 runtimes); `add` skips the intro; `reconnect` rotates a fresh key. curl + integration verified the rename endpoint (200 + persisted; empty→400; foreign/unknown id→404; member without `manageMachines`→403). **Persistent empty-state CTA**: the one-shot auto-wizard is easy to lose (dismiss/opt-out is remembered), which left the static Computers no-machines pane with only a tiny sidebar "+" — easy to miss. `PaneEmpty` (`web/src/PaneEmpty.tsx`) now takes an optional `action` slot, and the no-machines pane renders a prominent "+ Connect a machine" button (`.pe-cta`) there, wired to the same `add` wizard, gated on `capabilities.manageMachines`. Browser-verified (desktop + mobile).
- [x] **Workspace avatar**: Settings › Workspace Profile → upload image (owner/admin) → `POST /api/servers/:id/avatar` (image-only, stored as attachment row → `servers.avatarUrl=/api/attachments/<id>`) → sidebar rail block + settings page preview update live; persists across reload via bootstrap (`GET /api/servers` returns avatarUrl). **Also fixed existing routing bug**: `settings/:section` had no Route → all settings sub-pages (workspace/notifications/machines) bounced to RootRedirect on click; added one line to `main.tsx`. Browser verified: real upload + reload + DB triple-verified
- [x] **Create agent**: POST/GET/PATCH/DELETE /api/agents + runtime-models; frontend create modal (machine/name/description/runtime/model/fast); backend curl fully verified; each agent has UUID workspace. **Create auto-starts**: `POST /api/agents` builds then calls `await startAgent` (no daemon → `ok:false` does not block create, response body includes `started`). Browser verified (workspace create autostart-test → UI working / DB active / daemon spawns claude). **Dynamic model discovery** (`GET /api/servers/:sid/machines/:mid/runtime-models/:runtime`): for opencode / cursor / pi the daemon shells out the runtime's list command and returns real installed models (~60s in-process cache); for claude / codex it probes thinking/effort levels available on the local install; copilot / kimi remain static pending ACP probe (see tech-debt I39). Frontend model dropdown and reasoning-effort picker reflect the dynamically discovered list; falls back to static catalog on probe failure or timeout. **Model + reasoning effort are optional for claude/codex** (PR: runtime-default-model): the model picker's top entry "Use local default (recommended)" is selected by default → the agent row stores `model = NULL` → the daemon omits `--model`/`--effort` so the CLI uses its own local config (`~/.claude` / `~/.codex`). Selecting a concrete model/effort passes them through as before. This also closes #65's half-finished effort pass-through — `claudeRuntime` now sends `--effort` (it never did before; the UI picker was wired only to codex).
- [x] **Edit agent profile syncs the workspace `MEMORY.md`**: `PATCH /api/agents/:id` changing `displayName`/`description` now broadcasts `agent:profile` → the daemon surgically rewrites the workspace `MEMORY.md` H1 title + `## Role` while preserving the agent-owned `## Key Knowledge` / `## Active Context` / `notes/` (admin-wins). Previously the Role was seeded only on first start and never re-synced, so edits never reached the workspace. Unit-tested (`test/memory.unit.test.ts`, 9 cases) + daemon real-run + real HTTP PATCH end-to-end verified (title+Role updated, other sections preserved, model-only PATCH does not over-broadcast)
- [x] **Agent workspace file browser** (Profile › Workspace tab): file tree (`GET /agents/:id/workspace-files` + `/read`) + file viewer; `.md` files **Preview (rendered markdown, default) / Raw (monospace source) toggle**; top root path bar showing the daemon's real on-disk workspace dir + Copy path (1.5s "Copied" feedback) + folder click collapse/expand (caret rotation); **dotfiles hidden by default** (like real `ls`; path bar eye toggle to show = `ls -a`). Browser verified (md preview/raw + dotfile: `.claude-system-prompt.md` hidden by default, toggle shows)
- [x] **Profile fields + header alignment + per-runtime skills**: member profile now shows the workspace **join date with full calendar date** (`fmtDateTime` in `web/src/format.ts` — `toLocaleString` instead of the time-only `fmtTime`) and the member **email to any co-workspace member** (was self-only); the agent profile shows its **creation date** (`createdAt`, already returned by `GET /api/agents/:id`). The agent/human profile header is **aligned on one centered row** — fixed a CSS cascade bug where `.head{align-items:baseline}` (later in the file) overrode `.head-agent`'s `center`, leaving the avatar/name/actions misaligned and the name floating to the middle; `.head.head-agent` (higher specificity) + a growing `.head-id` name block now put avatar+name left and actions flush-right. **Skills are resolved per the agent's runtime** (`skillRootsFor`, ported from multica's `localSkillRootsForProvider`) instead of always reading Claude's `~/.claude/skills`. Verified: unit tests (`test/format.unit.test.ts`, `test/profileHeadAlign.unit.test.ts`, `test/skillRoots.unit.test.ts`) + browser (agent profile created date + aligned header; non-self human profile join date + email). Daemon change → `@fancyboi999/open-tag-daemon` bumped to 0.4.0 (republish on deploy).
- [x] **Member profile description (human profile + 3000-char limit + agent-visible via server info)**: Members left panel → click human → profile detail page (`/human/:uid`, shows description/role/joinedAt/**Created Agents**; self can edit own description); human + agent descriptions limited to **3000 chars** (frontend `maxLength` + `n/3000` counter + backend 400 "Description must be at most 3000 characters"); `open-tag server info` Humans/Agents each entry shows `— description` (pull model: standing prompt teaches agent to use server info to discover teammates, not pushed into prompt). Added `GET /api/servers/:sid/members/:uid/profile` + `agents.creatorId`. curl (profile/members/3000/agent server info) + CLI render + browser (detail page/edit/counter) end-to-end verified
- [x] **Agent profile header action bar**: agent detail page (Chat right panel + Members `/agent/:id` both modes) header action row — **DM** (everyone, openDM → navigate to DM) + **start/stop · restart · delete** (gated `capabilities.manageAgents`); Overview tab card shows only profile + edit profile (manageAgents). Backend start/stop/reset/delete endpoints already enforced requireCap manageAgents; frontend visibility aligned. Browser verified (owner sees DM/start/restart/delete + edit profile; member sees only DM)
- [x] **Avatar picker (set + upload) for agents & humans, shown everywhere**: clicking the profile-head avatar opens a picker — a **set of generated avatars to switch to** (DiceBear notionists, shuffle for a fresh batch) **+ upload-your-own image**. A picked face persists as `avatarUrl="dicebear:<seed>"` (no file); the `Avatar` component renders that scheme, while uploads persist as an attachment URL (`resolveAvatar` signs it). The **human profile head now matches the agent layout** (48px avatar + name/@handle·role column + action bar). The chosen avatar **propagates everywhere** — chat messages, sidebar/DMs, member lists, rosters, hovercards, mention autocomplete — by carrying `avatarUrl` on `GET /api/agents` + channel-members (`/members` already had it) and resolving message senders from the loaded agents/humans (no message-schema change). Permission: agent avatar = `manageAgents`, human avatar = self. Browser end-to-end verified: pick a generated face (human + agent) → persists + shows in head **and** sidebar; real PNG upload → renders in head + sidebar.
- [x] **Unified in-chat profile overlay (agent + human + self), thread-override**: clicking any avatar / name / `@mention` in a channel or thread opens that teammate's profile in the right column as an **overlay** ("click X → show X"). The old agent-only `profileAgentId` became `profile {type:"agent"|"human"; id}`; the right column is now a base layer (thread, else live trace) with a profile overlay on top (priority **profile > thread > trace**), so a profile opened while a thread is open **covers** the thread and closing it **reveals the thread again** — previously `thread` always won and the profile was rendered underneath, invisible until the thread was closed (the reported bug). Humans **including yourself** are now clickable everywhere agents were (main-chat rows + thread rows + `@human` mention) and reuse `HumanProfile` in a new panel mode mirroring `AgentProfile` (`onClose`/`onMessage`, self shows Edit profile, others show DM); the full-page `/human/:uid` + `/agent/:id` routes (Members roster, quick switcher) are unchanged. Opening a thread clears the overlay. Mobile: the profile panel renders as a fullscreen overlay (`.traj-col.profile-mode`, like the thread panel) instead of the previous `display:none` (it was invisible on mobile before). Browser-verified desktop + mobile: thread-open + click agent (main & thread) → profile covers thread; close → thread returns; click self/other human → human panel; `@mention` → panel; open-thread-while-profile clears the overlay.
- [x] **Agent restart three modes**: header "Restart" → modal three options: **Restart** (keep session + workspace, backend `restart` = stop + start) / **Reset Session & Restart** (clear session, keep workspace, `reset {restart:true}`) / **Full Reset & Restart** (clear session + delete workspace, `reset {wipeWorkspace:true,restart:true}`); all three options "& Restart" (reset then startAgent). curl three modes verified + browser modal three options verified
- [x] **Profile page seven facets**: overview / permissions / DMs (Agent DMs) / reminders / workspace (file tree) / integrations (Apps) / activity — all 7 facets have real components + API (`scopes`/`agent-dms`/`reminders`/`workspace-files`/`integrations`/`activity-log`), no placeholders; Apps display layer present ("connected apps", third-party login deferred)
- [x] **Members view: agents grouped by machine + online status** (`byMachine` grouping + `dot` activity/status)
- [x] **Capability-gated UI consistency (machine + channel-member ops) + custom role picker**: brought three frontend surfaces in line with the existing capability model — the backend already enforced all three via `requireCap` (member calls already 403'd), so this closes the **UI-exposure** gap where a member saw ops they couldn't perform. `Computers` (`views/misc.tsx`) gates connect/reconnect/delete-machine on `manageMachines`; the channel-members modal (`Chat.tsx`) gates the agent Remove button + add-agent section on `manageChannels`; the member-role picker (`Members.tsx`) swaps the unstyled native `<select>` for the design-system `<Select>`. Browser-verified both roles on a live stack: owner sees all ops + the custom role dropdown (owner/admin/member, checkmarked); member sees the members/machines but **no** Remove / connect / reconnect / delete affordances. Backend `requireCap` 403-enforcement re-confirmed by curl as a member.
- [x] **System alert center (rail notification popover)**: standing system warnings surfaced via a warning button above Settings in the rail (`web/src/alerts.tsx` — `useSystemAlerts()` + `NotificationCenter`, wired in `Layout.tsx`), shown **only when there are undismissed alerts**. Two real signals derived from live store state: an **online machine on an outdated daemon** (reported `daemonVersion` ≠ the latest published one) and an **offline machine that still hosts agents** (those agents can't run until it reconnects); benign states (idle, an offline machine with no agents) raise nothing. `latestDaemonVersion` is read from `packages/daemon/package.json` by the machines endpoint (was a hardcoded `"0.1.0"` that would have falsely flagged current `0.3.0` daemons; `Dockerfile` ships that manifest into the runtime image). Each alert offers View (→ that computer) + Dismiss (session-scoped); editorial popover style (not brutalist). Connect-a-computer command pinned to `@latest` so a stale npx cache can't serve a daemon that immediately trips the outdated alert. Online outdated machines now show an **Update daemon** guide in Computers: the browser does not pretend to self-update or recover the stored `sk_machine_*` key; saved-key users stop the old daemon and rerun `@latest`, while lost-key users stop it, wait for offline, then use the existing Reconnect flow to rotate a fresh key. Browser-verified (2 real alerts render, badge `2`, dismiss decrements `2→1→0`, button + popover disappear at `0`) + curl (`latestDaemonVersion:"0.3.0"`).
- [ ] External agent (Create External Agent — remote/external agent not attached to a local daemon): not yet implemented

## P7 Advanced
- [ ] **knowledge (agent private knowledge base / memo)**: agents **create multiple** memos (`title`+`content`), full-text **search** (PG tsvector) for self-retrieval during work — audience is **the agent itself**, not humans, **not** a read-only platform manual. Schema `knowledge` table (`agentId/title/content/searchText`) direction correct; missing endpoints + `open-tag knowledge` CLI (create/list/search) + GIN index
- [x] **messages search (human search)**: `GET /api/messages/search?q=` shipped (`routes-api/messages.ts` — scoped to the caller's readable channels, ilike + snippet + `hasMore` paging) + web Search view; **not the same as knowledge**, do not conflate
- [ ] integrations / apps, credential proxy (agents never touch plaintext keys)
- [ ] Agent wake-hints/stream (out-of-process real-time push to agents; the "migrate to real `/agent-api`" half of this old line is long done — agents talk to the real `/agent-api` via the CLI)
- [ ] Web Push (vapid)

## Public Landing Page (open-source showcase)
- [x] **Landing page (`/`)**: warm-editorial skin (open-design system), hero / three pillars / 9 capability cards / engine / self-hosted architecture / CTA / footer — 9 sections; integrated into React web (added `/` route, preserved dev-login `?as=` timing, `*` RootRedirect); CSS fully `.lp-*` scoped, isolated from app editorial skin. Copy written in project voice, all claims based on verified capabilities (no pricing / no testimonials / no star counts). Browser desktop + mobile responsive verified, app regression clean
- [x] **Public features page (`/features`)**: marketing showcase page linked from landing nav/hero/footer; five static open-tag cases (tag agent, build in task thread, catch up, monitor signals, shared human+agent workspace) rendered with the landing skin and a product UI demo. Each case shows the real product shape — channel message → tracked task → clickable reply-count pill → thread evidence panel — without backend/API/live-agent calls or copied competitor assets.

## Showcase (static demo page)
- [x] **Front-end static showcase page**: four case-study transcripts (Case 1: feature ship — PM/spec/eng/review; Case 2: product research — library sweep + adversarial deliberation on PWA vs native; Case 3: bug hunt — investigate/patch/adversarial test; Case 4: growth — weekly signal triage/follow-up/synthesis) rendered as a **purely client-side route** (`web/src/views/Showcase.tsx` + transcript data in `web/src/showcaseData.ts`, sample files under `web/public/showcase/`). **Zero DB**: no `type="showcase"` channel, **zero prop agents** (the old 13 `creatorType="system"` demo agents per workspace are gone), no per-workspace seed. One shared copy served to every visitor — no workspace data, no agent `skills` ever leak through the demo. Reached from a sidebar entry; no composer / no write surface to guard. **Replaces** the former DB-backed read-only `#showcase` channel (auto-seeded into every workspace, with five write intercepts + three world-readable read passthroughs) — that whole backend path (`showcaseSeed.ts` / `showcaseAssets.ts`, the `type="showcase"` read/write special-cases, and the `seedShowcase()` calls) was deleted, and existing showcase rows + prop agents are removed by the one-off `scripts/drop-showcase.mjs` migration.
