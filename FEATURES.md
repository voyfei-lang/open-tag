# open-tag Feature Checklist

> Strategy: **breadth-first** — ship each feature working end-to-end, polish later. Checked = implemented and self-tested.
> Scope: this file tracks feature **status** (checkbox + a short note; long verification narratives belong in the PR).
> The authoritative route/API surface is `ARCHITECTURE.md` §II — do not treat this list as an API reference.

## P0 Foundation
- [x] Project bootstrap + root `AGENTS.md` map (`CLAUDE.md` imports it; architecture detail lives in `ARCHITECTURE.md`)
- [x] Docker Compose: Postgres + Redis (`docker-compose.yml`, ports 5433/6380)
- [x] Database schema (`src/db/schema.ts`: 21 Drizzle tables; human-readable contract in `docs/generated/db-schema.md`)
- [x] DB client (drizzle + postgres.js) `src/db/index.ts`; additive schema deployment via `db:push`
- [x] Redis client + helpers: `seq:{serverId}` INCR, SSE pub/sub `src/redis.ts`
- [x] Bootstrap seed: default `open-tag` workspace + owner + #all only; machines and agents are created explicitly through onboarding

## P1 Control Plane (server ↔ daemon) — Verified end-to-end
- [x] WS `/daemon/connect?key=` TS implementation + machine registration/heartbeat persistence + push to frontend (one-shot machine ready is buffered across async authentication)
- [x] Message protocol: agent:start/deliver + ready/status/activity/trajectory/session (daemon↔server fully wired)
- [x] `--resume` wake-up supported; machine online status persisted
- [x] **Single-turn cold-start wake**: deliveries queued while an agent starts are consumed by the existing STARTUP/RESUME inbox-check nudge instead of being flushed as a second notice, so one message cannot drive duplicate replies after a cold wake; already-running agents keep the 3 s batched delivery path
- [x] **Reconnect robustness + per-machine key rotation**: an unknown/removed `sk_machine_*` key is rejected with WS close `4001`, so the daemon backs off to the 30s cap with an actionable error instead of reconnect-storming (backoff resets only after the server accepts the connection). `POST /api/servers/:id/machines/:id/reconnect` rotates the key on the **same** machine row; a **Reconnect** button on offline machines lets a lost-key machine rejoin without a duplicate row
- [x] **Reconnect catch-up + offline reachability**: on machine reconnect the server wakes only agents with a *wakeable* backlog (`reconnectCatchup.ts`; criterion mirrors `createMessage` — DM/@ always, ambient only with `inbox:receive`), so missed messages get processed without waking idle agents (no token burn). The Composer shows an offline-reachability hint for the agents a message will reach, and a global toast (`toast.tsx`) surfaces start/restart 503s and create-while-offline
- [x] **Idle-sleep**: agent auto-kills process when idle (`OPEN_TAG_IDLE_MS`, default 10 min), resumes on next wake; agent:stop/sleep linked
- [x] **Dynamic memory pressure system**: no per-agent limits until free memory drops below `OPEN_TAG_PRESSURE_MEM_MB` (default 500 MB); under pressure new agents queue and running agents are capped at current RSS + a fair-share margin (Windows Job Object / Linux cgroup)

## P2 Agent Data Plane (bundled `open-tag` CLI) ★ — Core verified
- [x] `open-tag` CLI (commander), auth via per-agent token + x-agent-id, proxied to `/agent-api/*` (stdout clean)
- [x] Subcommands: message check/decide/send (`--reply-to`, stdin heredoc)/read/react/search, server info, channel join/members/leave, task list/claim/assign/update/new/unclaim, thread reply/read/unfollow, profile show/update, reminder schedule/list/cancel/snooze, attachment upload/view, action prepare (`src/cli/index.ts` mirrors `/agent-api`)
- [ ] Subcommand pending: knowledge (create/list/search — no endpoints or CLI verb yet, see P7; mentions need no verb — `@name` inside `message send` is the mechanism)
- [x] Daemon spawns agent with injected system prompt (Current Runtime Context + open-tag CLI spec) + PATH-injected open-tag wrapper
- [x] **Runtime adapters (unified Runtime interface)**: claude (stream-json) + codex (app-server JSON-RPC, resume fallback) + copilot + opencode + kimi + pi + cursor (one-shot per turn, each with native session resume) — **all verified on real hardware** via dev:e2e — plus **hermes (experimental)**: one-shot `hermes chat -q` per turn, profile selection, native stderr session resume, and an evidence-gated final-response bridge for turns that read messages but never ran `message send`. Adapter contracts + per-CLI gotchas: `ARCHITECTURE.md` §II
- [x] **Reply coordination without information hiding**: eligible members still observe messages, but publication requires a trigger-bound grant — first explicit mention primary, later explicit mentions independent one-shot directed grants, unmentioned agents compete for at most one evidence-bearing supplemental; 1:1 DM primaries auto-accept (`dm_auto_authorized`); publishing an active addressed grant records implicit acceptance; mistaken mentions transfer via `better_fit`; Task results are authorized only in the task thread (one coordinator + scoped contributors); DB uniqueness caps publication per trigger/agent. Contract: `docs/reply-coordination.md`
- [x] **Conversation Turns (sender-scoped burst admission)**: nearby same-sender messages in one concrete channel/thread/DM form one durable Turn before any agent can check or answer them; human ambient work wakes one sticky/least-loaded owner, explicit mentions wake every named agent once, DM stays direct, agent-authored ambient chatter never wakes peers. Attempt-fenced fan-out, the `delivery-admission-v2` two-phase admission barrier, per-agent FIFO, and bounded causal depth/budget/pair dedupe prevent stale dispatch, duplicate public replies, and unbounded agent loops. Full model: `ARCHITECTURE.md` §II–IV + `docs/exec-plans/completed/conversation-turns.md`
- [ ] codex enhancement: per-agent `CODEX_HOME` isolation + auth/MCP injection; raw-v2 item/* trajectory detail

## P3 Messaging Core (human /api/* + realtime) — Backend verified (smoke tested)
- [x] REST: auth (dev-login/register/login/me) / servers / agents / channels / members / machines / messages/channel/:id / messages/sync
- [x] Auth: Bearer JWT + `x-server-id` middleware (humans); machine key + `x-agent-id` (agents)
- [x] **Seq incremental sync** (`GET /api/messages/sync?since=`); human realtime is socket.io full `message:new` (`socketio.ts` — the once-planned SSE endpoint was never built and is not coming back)
- [x] channel_member + unread (lastReadSeq) + unread summary (`GET /api/channels/unread` aggregates channel-own + followed-thread unread, shared with `POST /:id/read` — see the honest-badge entry in P4)
- [x] **@mentions[] structured storage** + frontend highlight rendered from the message's own `mentions[]` (`messageRender.tsx` — a non-member `@` renders as plain text)
- [x] **Markdown rendering polish**: chat messages and Workspace `.md` preview share one GFM rendering surface (GitHub-style alerts, task lists, tables, code blocks with language/copy chrome, color swatches, safer raw-HTML-as-text, internal reference pills); chat messages use their full responsive content column, document-style surfaces keep the narrower reading measure
- [x] **Saved Messages / Bookmarks**: `saved_messages` table + GET/POST/DELETE/check endpoints + right-click "Save message" / Saved view / sidebar count
- [x] **Channel lifecycle**: rename/description/visibility PATCH + archive/unarchive + soft-delete (all `manageChannels`-gated); archived hidden by default (`?archived=include`); channel header "⋯" settings menu; DM auto-create dedup; private-channel visibility enforced and private join invite-only (403)
- [x] **Channel message pagination (keyset)**: `GET /api/messages/channel/:id?before=<seq>&limit=` + `hasMore` sentinel on the `(channelId, seq)` index (no offset drift); web loads the latest 50, lazy-loads older pages viewport-anchored on scroll-to-top, caps the live-tail window at 400; deep-links (`?msg=`/`?thread=`) page older history until the target loads
- [x] **Date dividers + calendar-time timestamps**: day-boundary dividers ("Today"/"Yesterday"/localized weekday+date, year when relevant) in channel + thread lists; per-message timestamps always show full date+time (`fmtDateTime`, replacing the misleading time-only `fmtTime` at its 8 former call sites)
- [x] /agent-api/*: message check/decide/send/read, server info, channel join, task list/claim/update (full chain verified)
- [x] WS daemon control plane: connect/machine registration (persistent machine-id + ready:ack) / heartbeat / agent:start·deliver broadcast (agent:deliver carries inbox-notice metadata)
- [ ] Unarchive/restore UI: backend `POST /channels/:id/unarchive` exists but has no frontend surface (archived channels are hidden with no browse toggle). The rest of the old "UI gaps" line shipped: DM creation, private-channel visibility toggle, channel member add/remove, archive are all in the UI (`Chat.tsx`)

## P4 Tasks & Threads — Implemented (slice03 evidence-driven alignment + end-to-end verified)
- [x] Task field on message: As Task / right-click convert to task; channel + server task lists; convert-message; delete = revert to plain message (source message preserved)
- [x] Task claim/status flow `todo→in_progress→in_review→done→closed` (claim atomically sets in_progress; non-enum values 400; done auto-creates thread); kanban cards link to source message; socket task events realtime
- [x] **Board layout toggle** (`TaskBoard.tsx`): horizontal Kanban (default) ↔ vertical stack, persisted in `localStorage["open-tag.tasks.boardLayout"]`; drag-and-drop works in both layouts
- [x] **Board move interaction** (`TaskBoard.tsx`): move between columns by drag-and-drop *or* the status pill's click menu; full-height equal-lane columns are the whole drop target; optimistic FLIP-animated moves with a `prefers-reduced-motion` fallback
- [x] **DM tasks distinct from channel tasks**: DM tasks number per-DM (`tasknum:dm:<channelId>` — each conversation has its own #1); DMs get their own `chat | tasks` board; the global board labels DM tasks `@peer`. Invariant: `taskStatus ⟺ taskNumber` — status mutators guard `isNotNull(taskStatus)`; agent claim/update on a non-task converts it first (canonical mint), so a `#null` task is impossible; `scripts/heal-task-numbers.ts` backfills legacy rows
- [x] **Agent-to-agent task handoff**: `open-tag task assign` / `/agent-api/task/assign` (by message id or channel + task number) updates the assignee, promotes `todo → in_progress` when needed, records a system audit message in the task thread, auto-adds the assignee to the thread, and wakes them on the thread target
- [x] Threads (parentMessageId-derived channels)
- [x] **Thread follow/done system**: follow/unfollow/done/undone + `GET threads/followed`; auto-follow on reply (+ this-thread done reset); per-user `channelMembers.threadDoneAt`; ThreadPanel "Mark Done / Unfollow"; Inbox shows followed threads
- [x] **Honest unread badge + jump-to-unread-thread bar**: the channel badge aggregates channel-own + followed-thread unread and each source clears independently — `POST /api/channels/:id/read` returns the exact remaining aggregate (shared `unreadMapForUser`) instead of the client blind-zeroing, so a still-unopened thread reply can no longer "resurrect" the badge; a "💬 N unread thread replies · View" row above the message scroller opens the first unread thread (fetching its parent via `GET /api/messages/:id` when it isn't in the loaded page), marks it read, and advances to the next
- [x] **Inbox Mentions tab**: message-grained @-of-me activity stream (`GET /api/mentions`, backed by `mentions_target_idx`; read or unread; deep-links to the exact message or its thread; paginated 50/page with Load more) — distinct from the channel-aggregated all/unread filters, which drop an already-read `@`
- [x] **Action Cards (B-mode quick-commit human-in-the-loop)**: an agent without channel:create/agent:create scope sends a proposal card (`open-tag action prepare`); the human reviews in a pre-filled creation modal, creates as themselves, and `POST /api/actions/:id/mark-executed` records who executed it
- [x] **Attachment enrichment**: human-side image inline + fullscreen lightbox (scroll-zoom/drag-pan/Esc) + video inline player with codec fallback; agent-side `attachment view` downloads bytes to local `attachments/` (image → runtime vision)
- [x] **Unified message composer**: channels, DMs, and threads share one `Composer` component (text, attachment upload via button/paste/drag-drop with progress, @mention autocomplete); **As Task stays channel/DM-only** (a thread reply is never a task)
- [x] **Pluggable attachment storage**: `src/server/storage.ts` boundary — local disk (default, zero-config) or any S3-compatible backend (`OPEN_TAG_STORAGE=s3` + `OPEN_TAG_S3_*`, `@aws-sdk/client-s3` optional dep); fail-loud config validation; bytes travel over HTTP so a remote daemon works cross-host

## P5 Agent Collaboration — Core loop end-to-end verified (slice 01)
- [x] Agent A `open-tag message send @B` → B wakes → works → reports back (real web send, cross-runtime bidirectional)
- [x] Routing: @mentions/channel membership determines which agents wake (non-mentioned agents stay idle); busy-state stdin notification (deliver 3s debounce batching + inbox-notice metadata)
- [x] **Inline Agent Activity in channels, DMs, and threads**: each agent run renders as a collapsible Activity disclosure directly under the public message it produced — status, thinking/text trajectory, and tool calls stream into the disclosure, never as provisional public reply text. Before the first reply a compact inline run row shows live Activity, and the persisted reply takes that row over in place (no second pop). A run that sends multiple messages segments Activity at each message boundary; a run that sends no public message leaves a quiet handled/error receipt (excluded from agent APIs, never wakes agents). Message snapshots survive refresh and log pruning; header run state derives from that message's own Activity, not the agent's global state. The permanent Live Trace column is gone; chat disclosures and Agent Profile Activity share one timeline-row renderer (Profile Activity stays the global diagnostic history)
- [x] **Live agent bar (sidebar bottom)** (`web/src/views/LiveAgentBar.tsx`): a persistent workspace-wide "who's running right now" strip below the channel/DM list — agents with `status==='active'` **and** `activity∈{working,thinking}` (the status guard hides phantoms stuck on stale `working`), most-active first with avatar + pulsing pip + live `activityDetail`, the rest in a `+N` popover. Click opens that agent's profile on the Activity tab in the right column (no page nav, no DM)
- [x] **Fixed: a trailing agent-reply delta after absorption could spawn a phantom channel message** (`web/src/lib/agentReplyPreview.ts`): only a `start` may originate a preview; a stray `delta`/`done`/`error` with no matching stream is ignored, so a claude turn that calls `message send` and keeps talking can no longer produce an orphan bubble with no DB row

## P6 Workspace Settings & Agent Creation & Profile
- [x] **Shared app i18n coverage**: permissions scope groups/labels, personality/skills controls, avatar picker, Markdown alerts/code-copy chrome, Computers metrics, member/role labels, sender fallbacks, toasts, and locale-aware date/time formatting all route through the shared `en`/`zh` locale files (content-heavy Showcase + standalone marketing copy stay separate by design — tech-debt I89)
- [x] **Multi-workspace (community) + server switcher + role permissions**: a user can belong to multiple servers; `POST /api/servers` creates a workspace (server + owner + default #all); top-left brand = switcher (`ServerSwitcher.tsx`); the URL `/s/:slug` picks the current server. **Capability permission layer** (`src/server/capabilities.ts`, 8 caps × owner/admin/member) enforced via `requireCap` on all write endpoints; `GET /servers` returns role + capabilities so the frontend hides what the role cannot do
- [x] **Member invites · join-link (S2a)**: owner/admin generates invite links (configurable role + use count) in Settings › Invite Members; `join_links` table + CRUD (manageMembers; links grant admin/member, never owner) + public `GET /auth/invite-info` + `POST /auth/accept-invite` (idempotent, auto-adds to #all)
- [x] **Register/login + invite landing page (S2b)**: real `/login` · `/register` pages + `/join/:token` landing (register/login or one-click join); bootstrap prefers `localStorage.open-tag.token`, falls back to dev-login (`?as=` unaffected); stable auth error codes render localized accessible form messages; Settings › Account "Log out" clears the token → `/login` (JWT stateless — frontend clear suffices)
- [x] **Member role management (S3)**: `PATCH /api/servers/:id/members/:uid {role}` + `DELETE` (cap-gated; cannot change self; last owner cannot be demoted/removed); HumanProfile "Member Management" card
- [x] **Empty-server onboarding → in-modal connect-computer wizard** (`views/ConnectComputerWizard.tsx`): one component behind three entry points — `onboard` (auto-shows when `machines.length===0 && manageMachines`; once-per-tab dismiss + permanent opt-out), `add` (Computers "+", starts at the connect step), `reconnect` (offline machine → key rotation). Flow: intro → ready-to-run `npx @fancyboi999/open-tag-daemon@latest …` command + live waiting pulse → on `machine:status` a success card with optional friendly rename (`PATCH machines/:id`, cap-gated + tenant-isolated) → Done. Replaced `AddComputerModal` + `ConnectMachineModal`. The static no-machines pane also renders a prominent "+ Connect a machine" CTA (`PaneEmpty` action slot) wired to the same wizard
- [x] **Workspace avatar**: Settings › Workspace Profile image upload (owner/admin) → stored as an attachment row → sidebar rail + settings preview update live and persist via bootstrap (also fixed the missing `settings/:section` route in `main.tsx`)
- [x] **Create agent**: POST/GET/PATCH/DELETE `/api/agents` + runtime-models; create modal (machine/name/description/runtime/model); create auto-starts (`started` in the response; no daemon does not block create). **Dynamic model discovery**: opencode/cursor/pi list real installed models via the daemon (~60s cache); claude/codex probe thinking/effort levels; copilot/kimi remain static pending an ACP probe (tech-debt I39); falls back to the static catalog on probe failure. **Model + reasoning effort optional for claude/codex**: the default "Use local default (recommended)" stores `model = NULL` → the daemon omits `--model`/`--effort` so the CLI uses its own local config
- [x] **Bind an agent to an existing project directory without overwriting project instructions**: manual paths + a remote machine folder picker under daemon-local `OPEN_TAG_PROJECT_ROOTS`, with bounded marker-only discovery; the canonical path becomes runtime cwd while `MEMORY.md`/personality/notes/generated artifacts stay in `OPEN_TAG_HOME/agents/<id>`; existing `AGENTS.md`/`CLAUDE.md`/runtime config remain untouched and load natively. Mixed-version daemons fail closed (`project-directory-v2` + `project-browser-v1`); path changes require `inactive` and clear the old session atomically; full reset affects only agent state; delete requires a confirmed stop. Shared roots constrain web disclosure/binding — explicitly not a runtime sandbox
- [x] **Edit agent profile syncs the workspace `MEMORY.md`**: `PATCH /api/agents/:id` changing `displayName`/`description` broadcasts `agent:profile` → the daemon surgically rewrites the MEMORY.md H1 + `## Role` while preserving the agent-owned sections (admin-wins)
- [x] **Agent workspace file browser** (Profile › Workspace tab): file tree + file viewer; `.md` Preview (rendered, default) / Raw toggle; root path bar showing the daemon's real on-disk dir + Copy; dotfiles hidden by default with an eye toggle
- [x] **Profile fields + header alignment + per-runtime skills**: member profile shows the full join date and the email to any co-workspace member; agent profile shows its creation date; the profile header sits on one centered row; skills are resolved per the agent's runtime (`skillRootsFor`) instead of always reading Claude's `~/.claude/skills`
- [x] **Member profile description**: human profile detail page (`/human/:uid` — description/role/joinedAt/Created Agents; self-editable) with a 3000-char limit enforced both ends; `open-tag server info` lists teammates with descriptions (pull model — the standing prompt teaches discovery, nothing is pushed into the prompt)
- [x] **Agent profile header action bar**: DM (everyone) + start/stop · restart · delete (gated `manageAgents`); backend endpoints already enforced the cap — frontend visibility aligned
- [x] **Personality.md upload/delete (web UI)**: Profile tab shows current `personality.md`; upload any `.md` → `PUT /api/agents/:id/personality` → daemon writes it and re-syncs the MEMORY.md `## Role`; Delete removes it (gated `manageAgents`)
- [x] **Skills upload/delete (web UI)**: "+ Skill" picks a `.md` (name from filename, YAML frontmatter auto-prefixed if missing) → `PUT /api/agents/:id/skills/:name`; workspace-scoped skills deletable, global skills read-only (gated `manageAgents`)
- [x] **Avatar picker (set + upload) for agents & humans, shown everywhere**: generated DiceBear faces (persisted as `avatarUrl="dicebear:<seed>"`, shuffle for a fresh batch) or an uploaded image (attachment URL); propagates to chat, sidebar/DMs, member lists, rosters, hovercards, and mention autocomplete by carrying `avatarUrl` on the list endpoints (no message-schema change). Agent avatar = `manageAgents`, human avatar = self
- [x] **Unified in-chat profile overlay (agent + human + self), thread-override**: clicking any avatar / name / `@mention` opens that teammate's profile in the right column as an overlay (priority **profile > thread > trace** — a profile opened over a thread covers it, closing reveals the thread again); humans including yourself reuse `HumanProfile` in a panel mode mirroring `AgentProfile`; full-page `/human/:uid` + `/agent/:id` routes unchanged; mobile renders the panel fullscreen
- [x] **Agent restart three modes**: Restart (keep session + workspace) / Reset Session & Restart / Full Reset & Restart (wipes workspace too)
- [x] **Profile page seven facets**: overview / permissions / DMs / reminders / workspace / integrations / activity — all real components + API, no placeholders (third-party login itself deferred — tech-debt I18)
- [x] **Members view: agents grouped by machine + online status** (`byMachine` grouping + `dot` activity/status)
- [x] **Capability-gated UI consistency + custom role picker**: Computers connect/reconnect/delete gated on `manageMachines`; channel-members modal agent ops gated on `manageChannels`; the member-role picker uses the design-system `<Select>` — closes the UI-exposure gap (the backend already 403'd all three)
- [x] **System alert center (rail notification popover)** (`web/src/alerts.tsx`): standing warnings derived from live store state — an online machine on an outdated daemon (vs `latestDaemonVersion`, read from `packages/daemon/package.json`) and an offline machine still hosting agents; each alert offers View + session-scoped Dismiss; the rail button shows only while undismissed alerts exist. Outdated online machines get an **Update daemon** guide in Computers (saved-key: stop + rerun `@latest`; lost-key: stop, wait for offline, Reconnect rotation)
- [ ] External agent (Create External Agent — remote/external agent not attached to a local daemon): not yet implemented

## P7 Advanced
- [ ] **knowledge (agent private knowledge base / memo)**: agents **create multiple** memos (`title`+`content`), full-text **search** (PG tsvector) for self-retrieval during work — audience is **the agent itself**, not humans, **not** a read-only platform manual. Schema `knowledge` table direction correct; missing endpoints + `open-tag knowledge` CLI (create/list/search) + GIN index
- [x] **messages search (human search)**: `GET /api/messages/search?q=` (scoped to the caller's readable channels, ilike + snippet + `hasMore` paging) + web Search view; **not the same as knowledge**, do not conflate
- [ ] integrations / apps, credential proxy (agents never touch plaintext keys)
- [ ] Agent wake-hints/stream (out-of-process real-time push to agents; agents already talk to the real `/agent-api` via the CLI)
- [ ] Web Push (vapid)

## Public Landing Page (open-source showcase)
- [x] **Landing page (`/`)**: warm-editorial skin, 9 sections (hero / three pillars / capability cards / engine / self-hosted architecture / CTA / footer); CSS fully `.lp-*` scoped, isolated from the app skin; copy grounded in verified capabilities (no pricing / testimonials / star counts)
- [x] **Public features page (`/features`)**: five static open-tag cases rendered with the landing skin + a product UI demo (channel message → tracked task → clickable reply-count pill → thread evidence panel), with no backend/API/live-agent calls

## Showcase (static demo page)
- [x] **Front-end static showcase page**: four case-study transcripts rendered purely client-side (`web/src/views/Showcase.tsx` + `web/src/showcaseData.ts`, sample files under `web/public/showcase/`); zero DB, zero prop agents, one shared copy for every visitor — no workspace data can leak through the demo. Replaced the former DB-backed `#showcase` channel + per-workspace seed (backend path deleted; `scripts/drop-showcase.mjs` cleans pre-existing rows)

## Web UI Polish
- [x] **Icon micro-interactions (Amicro-style, pure CSS)**: semantic hover motion on interactive icons via one shared overshoot curve (`--im-spring`) and a two-class API (`im` trigger + `im-*` effect), zero JS/deps (`web/src/iconMotion.css`); hover-capable devices only + `prefers-reduced-motion` respected; principles + usage rules in `docs/icon-motion.md`
