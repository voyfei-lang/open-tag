# Tech Debt Archive — resolved / obsolete / superseded entries

> Full historical records moved out of [`tech-debt-tracker.md`](./tech-debt-tracker.md) so the hot tracker stays scannable. **Nothing here is deleted or summarized** — each row is the complete original entry, and many carry load-bearing rationale (e.g. I32's "do not remove the esbuild override", I67's Railway XFF model). Grep this file when a doc says "see tech-debt <ID>" and the tracker doesn't have it.
> IDs are never reused; the next free ID accounts for both files.

| # | Symptom | Evidence | Action | Status |
|---|---|---|---|---|
| I99 | **The `docs.getopentag.com` custom domain was temporarily unavailable during the suspension-era repository migration, so public links fell back to the bot account's default GitHub Pages URL.** | Live browser checks on 2026-07-28 confirmed that both `https://docs.getopentag.com/` and the restored main account's Pages route serve the open-tag docs successfully. | Restored `DOCS_SITE_URL` and Astro `site` to `https://docs.getopentag.com/`, removed the bot-only `/open-tag` Pages base, and returned repository links to `fancyboi999/open-tag`. | ✅ Resolved (main account and custom-domain Pages restored, 2026-07-28) |
| D9 | **Design spec `docs/superpowers/specs/2026-06-25-railway-preproduction-design.md` described `drizzle-kit push --force`** — the actual entrypoint (`scripts/docker-entrypoint.sh`) was already updated (PR-E / D8) to run `npx drizzle-kit push` without `--force`; spec also said first deploy used `railway up` from a CLI worktree (predates the GitHub-connected CI/CD path) | Cross-read spec vs. `scripts/docker-entrypoint.sh` line 15 + `docs/self-host.md` § migration note | Updated spec in same PR as Railway deploy config: removed `--force`, replaced `railway up` CLI path with GitHub-connected auto-deploy description, upgraded Storage section (R2 now first-class). `docs/self-host.md` gained a Railway pointer at the top | ✅ Fixed (this PR) |
| D1 | CLAUDE.md described the `src/` TS migration, React frontend, idle-sleep, multi-channel/tasks/threads **all as "PoC limitations / TODO"** | `package.json` main stack is `src/`; `agentManager.ts` has `IDLE_MS`; web fully implemented | Slim CLAUDE.md to a directory + correct current state | ✅ Fixed this session |
| D2 | CLAUDE.md "Data Model" section was partial and inaccurate (task enums, activity_log, sidebar_prefs, mentions table, lastReadSeq missing) | Compared against `schema.ts` | Redirect to `docs/generated/db-schema.md` | ✅ Fixed this session |
| D3 | CLAUDE.md Runtime table said claude "MCP tools or open-tag CLI", only listed three runtimes; "SSE is lightweight notifications only" | `claudeRuntime.ts` has no `--mcp-config`; `detectRuntimes` lists 5+; `socketio.ts` emits full message:new | Use `ARCHITECTURE.md` as source of truth | ✅ Fixed this session |
| D4 | `FEATURES.md` P4 (tasks/threads), P6 (Agent Profile seven facets/member grouping) marked `[ ]` incomplete, actually implemented | `Chat.tsx`/`Members.tsx`/`TaskBoard.tsx` + corresponding `/api/tasks`, `/api/channels/:id/threads` | **P4 corrected** (slice03 doc-sync: boxes ticked + endpoints documented + DELETE); P6 facet check pending | ✅ Done — P4 corrected (slice03); P6 facets verified implemented (FEATURES P6 all checked); the remaining FEATURES stale-marker sweep completed in the doc-convergence PR (2026-07-06) |
| D5 | ~~"`DESIGN.md` is external material, not this app's design doc"~~ **Corrected (mis-judgment)**: `DESIGN.md` = **the project's own visual system standard** — a warm-editorial design system, the project's own visual language; a deliberate design decision, **not debt** | `styles.css` implements a relevant subset (design systems are normally broader than any single app's usage, which is normal) | Keep as frontend design spec; use `/impeccable:impeccable` at build/finish time to enforce craft/a11y/interaction quality per DESIGN.md | ✅ Clarified (not debt) |
| D6 | `functional-matrix.md` open-tag current-state column (✅/🟡/❌) **severely stale overall**: profile seven facets, reactions, and many human-side endpoints (create/archive channel, join/leave/read, DM, search, full-server task board, task realtime) are actually implemented, matrix still shows ❌/🟡 | Discovered stale ❌ for profile-facet + reactions while selecting work items; code audit showed they were already done | **Do not use the matrix as a work queue** (MISSION covered this); needs a one-time sweep against current `src/` + `web/`. Real remaining gaps: message edit PATCH | ❌ Obsolete (2026-07-06) — `functional-matrix.md` was never committed to this repo (pre-publish workspace artifact); there is nothing to sweep. `FEATURES.md` is the status list. The one real gap it surfaced (message edit PATCH) remains a candidate feature |
| D8 | **No self-hosting deployment guide** — `README.md` Quick Start and `docs/docker.md` were two conflicting first-run paths, neither covering HTTPS/systemd/backup/secrets reference; `docker-entrypoint.sh` used `drizzle-kit push --force` (silent data-destruction risk); `docs/docker.md` still showed dev-mode daemon connect command (`npx tsx src/daemon/index.ts`); compose lacked `restart` policies for postgres/redis; image versions used mutable tags | Reported by user; verified by cross-reading all deployment files | Added `docs/self-host.md` (Docker Compose as recommended path, HTTPS, systemd, backup, env reference); fixed entrypoint (`--force` removed); updated `docs/docker.md` (npx daemon command, self-host.md link); added `restart: unless-stopped` + locked image versions in `docker-compose.yml`; added `docs/systemd/` units | ✅ Fixed (this PR) |
| I1 | ~~Not a git repo~~ **Git initialized** | `git log` shows full commit history; `.gitignore` in place | ~~git init + initial commit~~ | ✅ Done |
| I2 | @mentions: backend stores structured `message_mentions`; **frontend both uses text regex (allowlist) rather than consuming the structured array** | Render side `web/src/messageRender.tsx` `processMessageContent` (slice04 replaced and removed old `mentionize()`); completion side `Chat.tsx` `onInput`; both char-sets unified in slice05 to `[\p{L}\p{N}_-]/u` | Unify: frontend highlights from `mentions[]` dropping regex, or confirm "regex + allowlist" is final state | ✅ Fixed — `processMessageContent` now resolves `@` from the message's own `mentions[]` (server-recorded, channel-scoped) instead of a workspace-wide name map; a non-member `@` renders as plain text. Pairs with Slack-style mention auto-join in `core.createMessage` (public channels). |
| I7 | **Task status enum inconsistent across three locations**: `schema.ts` comment + `docs/generated/db-schema.md` said `open/claimed/in_progress/done/cancelled`; but web TaskBoard + `prompt.ts` both use `todo/in_progress/in_review/done/closed` (claim is a separate operation) | Cross-validation across three files + `core.ts` writes `todo`/`in_progress` | ✅ Fixed: `schema.ts` comment + `db-schema.md` aligned to `todo/in_progress/in_review/done/closed`; also fixed latent bug in `routes-api.ts:666` PATCH defaulting to illegal `"open"` → changed to `"todo"` | ✅ Done |
| I8 | API surface documentation overlap risk between two files listing REST/socket endpoints | Both files list APIs | Designate one file as authoritative API surface reference, the other only tracks open-tag implementation status | ✅ Done (doc-convergence PR 2026-07-06) — `ARCHITECTURE.md` §II is the authoritative route/API surface; `FEATURES.md` tracks feature status only (stated in both files' headers) |
| I9 | Dev mode messages/trajectories/AI replies **appear doubled** | React StrictMode + async socket cleanup doesn't close late-arriving connections → two sockets each receive once (`web/src/store.tsx` socket useEffect) | Add `cancelled` flag: close late-arriving connection immediately if component already unmounted | ✅ Done (commit f2b0251; browser verified single copy on send) |
| I10 | Composer **Chinese IME Enter key sends prematurely** | onKeyDown doesn't check IME composition state (`web/src/views/Chat.tsx` main composer + thread textarea, slice05 switched input→textarea, IME guard retained) | Add `e.nativeEvent.isComposing` check | ✅ Done (f2b0251; simulated composition Enter does not send, normal Enter sends) |
| I11 | ~~Message row **task pill has inline hardcoded colors**~~ | Original `Chat.tsx` task-pill `style={{...}}` | slice05 §6 extracted to `.task-pill` CSS class (`--success` 1px border + transparent background + pill, editorial style, no hardcoded colors); browser computed style verified | ✅ Done (slice05) |
| I13 | ~~**`styles.css` many off-scale border-radii / hardcoded colors**~~ | ~30 places with border-radius (3/5/7/9/10/14px) + scattered hardcoded colors (error red, inbox glyph blue/purple/rose tint, white #fff, mention yellow #fdf2d6, modal overlay, ref-chan hover) | slice05 tokenization sweep: border-radius snapped to DESIGN.md scale (3/5→4, 7/9→8, 10→12, 14→16); hardcoded colors promoted to `:root` sidecar tokens (`--error*` aligned with DESIGN.md semantic-error + `--tint-blue/lav/rose/green` + `--on-ink` + `--mention` + `--scrim`); `grep` verified zero hardcoded hex outside `:root`, all radii on scale. **Decision**: `box-shadow` `rgba(0,0,0/12,10,9, α)` kept as-is — these are per-element elevation conventions (not brand color drift); forcing tokens would flatten depth hierarchy; acceptable | ✅ Done (slice05; shadows kept by convention) |
| I14 | **`daemonHub` global `Set` broadcast, no serverId routing** → multiple daemons on the same server process (connected to different servers) would **cross-contaminate**: every `agent:start/deliver/stop/reset` fans out to all daemons | Original `daemonHub.ts` `Set<WebSocket>` + `broadcastToDaemons` with no filter | Changed to `Map<ws,serverId>`, `broadcastToDaemons(serverId,…)`/`requestDaemon(serverId,…)`/`daemonCount(serverId)` route by serverId | ✅ Done (this session; bare two-daemon two-server end-to-end verified: A triggers → A daemon receives agent:start×2+deliver, B gets zero) |
| I15 | ~~**`agentConfig.machineKey` hardcoded to `BOOTSTRAP_KEY`** + `auth.ts:35` `token===BOOTSTRAP_KEY` universal passthrough → agent plane backdoor~~ | — | **Fixed (slice10, per-agent independent token)**: `agentConfig` mints `sk_agent_*` stored as `agentTokenHash`, injected as `OPEN_TAG_AGENT_TOKEN`; `resolveAgent` reverse-resolves by hash, removed universal passthrough + machine key as agent credential. curl 4 assertions (old key 401 / real token 200 / cross-agent 401 / fabricated 401) + E2E (daemon spawn real agent message/check 200) verified. **Also added zombie reconciliation** (`ws.ts onReady` uses daemon `runningAgents` to reconcile: stale `active` agents set inactive after server+daemon both restart; daemon survives server restart → reports live agents → skips zero-desync. Live verified: manufactured active zombie → restart daemon → `reconciled stale-active agent → inactive` → back to inactive). **Remaining hardening** (low priority): token-file hot-rotation / per-task short-lived tokens with expiry | ✅ Done (slice10 + zombie reconciliation) |
| I19 | **Action cards (B-mode quick-commit action card)** | `action:prepare` scope declared but not implemented. Full contract extracted + live-verified. Card = message with `actionMetadata{kind:"action-card",state,action}`; agent sends via `open-tag action prepare --target <ch>` + stdin; 4 types `channel:create`/`agent:create`/`channel:add_member`/`integration:approve_agent_login`; 3 endpoints `/actions/{id}/event\|execute\|mark-executed` | **Implemented + verified** (slice09): agent-api `action/prepare` (scope action:prepare) + human `POST /api/actions/:id/mark-executed` + frontend card render / pre-filled modal (DESIGN.md skin). createMessage accepts actionMetadata (schema zero-migration, field already existed). execute-as-human security: mark-executed only marks; actual creation goes through existing /api/channels\|/api/agents (as human), no new privileged path. curl + browser end-to-end verified. **Deferred**: channel:add_member, integration:approve_agent_login (= I18), /actions/:id/event (telemetry), /execute (integration only) | ✅ Done (slice09). **Note: mention-actions (`/messages/mention-actions/execute`) is a separate mechanism from action cards; not implemented** |
| I20 | **`BOOTSTRAP_KEY` defaults to hardcoded `poc-secret-key` (machine layer connection key)** | `auth.ts:7` (`process.env.DAEMON_BOOTSTRAP_KEY ?? "poc-secret-key"`); `ws.ts` daemon WS `/daemon/connect?key=` uses it to authenticate the default workspace. **No longer an agent credential** (I15 fixed `resolveAgent` to not touch it) → risk far lower than old agent universal passthrough (machine layer has serverId isolation as backstop: even with the key, it only connects to the default workspace) | Before production deployment, harden by either: ① requiring `DAEMON_BOOTSTRAP_KEY` with no built-in default, or ② removing the bootstrap path so every machine uses a real `sk_machine_*` key | ✅ Fixed (sec-authhardening PR): both JWT_SECRET and DAEMON_BOOTSTRAP_KEY now use `requireEnv()` — fail-fast at module-load time if unset, no weak fallback. Tested with subprocess-based unit tests. **Residual**: `package.json` `daemon` script still has hardcoded `poc-secret-key` in a `--api-key` arg — follow-up PR to align with worktree `.env` |
| I21 | **Settings sub-page routing entirely unreachable**: `main.tsx` only had `Route path="settings"` (exact match to `/s/:server/settings`), no `settings/:section`; Settings sidebar buttons navigate to `/settings/{server\|notifications\|machines}` → fall through to `path="*"` RootRedirect → bounce to channel. Result: workspace profile/notifications/machines sub-pages **always bounced on click, never actually opened** (`Settings` component reads `useParams().section` which is always undefined → only shows account) | Discovered while implementing workspace avatar: avatar UI is in ServerSettings but `/settings/server` was unreachable | Added one line to `main.tsx`: `<Route path="settings/:section" element={<Settings />} />` (account default page retains `path="settings"`) | ✅ Fixed (this session, with workspace avatar; browser verified `/settings/server` no longer redirects, ServerSettings renders) |
| I22 | **Write endpoints have no human role permission checks**: `POST /api/channels`, `POST/PATCH/DELETE /api/agents`, agent start/stop/reset etc. **any server member can call** (member can freely create/delete channels and agents); only `PATCH /servers/:id` and avatar had manual owner/admin checks | Discovered when adding member role management: needed permissions foundation first | Created `src/server/capabilities.ts` (8 caps × owner/admin/member) + `can`/`requireCap`; integrated into create/delete channel·agent·server settings·avatar; `GET /api/servers` returns `role`+`capabilities` for frontend visibility control. **Frontend shows/hides by cap + member role management endpoints (PATCH/DELETE members) = S1/S3** | ✅ Done (P0 backend enforced: curl member→403/owner→200/all 8 caps; S1 frontend: member does not see create channel·agent buttons, browser verified) |
| I26 | **`agents.name` has no `UNIQUE` constraint** although it is the @mention handle (and `dm:@<name>` lookup key) — two agents on the same server can share a name, making `@<name>`/`dm:@<name>` ambiguous | `schema.ts`: `users.name` is `.notNull().unique()`, `agents.name` is only `.notNull()`; `resolveTarget` (`core.ts`) does a plain `eq(agents.name, peer)` and silently picks the first match on collision | Add a per-server `UNIQUE(serverId, name)` index on `agents` (+ drizzle migration + `docs/generated/db-schema.md`); requires a plan for any existing duplicates before the migration can apply | ✅ Fixed (this session): **partial** unique index `agents_name_uniq` `(serverId, name) WHERE deleted_at IS NULL` (`schema.ts`) — unique among live agents only, so soft-deleted names are reusable (better than a full-table `UNIQUE`, which would permanently lock a deleted name); `POST /api/agents` uses `ON CONFLICT` against the index → friendly **409**, race-proof (no SELECT-then-INSERT gap; verified 0×500 under concurrent double-create). Existing-duplicate plan (the "plan for existing duplicates" this entry asked for): `scripts/heal-dup-agent-names.ts` — dry-run report by default, `--apply` renames all-but-newest live dupe to `<name>-<id8>` so none is lost and shadowed agents become addressable, then `npm run db:push`. Verified: real curl chain (create 200 / dup 409 / delete→profile gone / recreate-after-delete 200) + raw `INSERT` blocked by the index |
| I28 | **No test harness → validators like `invalidAgentName` have no CI regression guard** | PR #4 review ④: the 26-case logic check for `invalidAgentName` was run by hand, not committed; the repo has no test runner (`package.json` has no `test` script, CI only typechecks + builds, `test/` is empty), so a future edit that loosens the regex/length would not fail CI | Introduce a lightweight runner (`node:test` + `tsx`) + a CI step, then land `test/agent-name.test.ts` (and similar). Repo-wide infra — related to I5 ("invariants are only prose, not mechanically enforced"); kept out of the small #3 fix | ✅ Fixed — `ci.yml` now runs `npx tsx --test --test-force-exit test/*.unit.test.ts src/daemon/*.test.ts` (99 unit + 30 daemon = 129 tests, all green locally). Also added `npm audit --omit=dev --audit-level=high` step (exits 0 — 0 prod vulnerabilities). |
| I56 | **No Dependabot** — root / web / Actions dependencies receive no automatic PR for updates | `.github/dependabot.yml` missing; stale deps accumulate silently | Add `.github/dependabot.yml` for npm root + npm web + github-actions, weekly schedule | ✅ Fixed — `.github/dependabot.yml` added (3 ecosystems: npm `/`, npm `/web`, github-actions `/`; weekly) |
| I57 | **README missing CI status badge + npm version badge** — repo health and npm release visibility invisible at a glance | Badge row had stars/license/deployment/runtimes but no CI or npm | Add CI Actions badge + npm `@fancyboi999/open-tag-daemon` version badge | ✅ Fixed — CI badge (`ci.yml`) + npm badge (`@fancyboi999/open-tag-daemon`) added to README badge row |
| I58 | **Root `package.json` missing `keywords`, `repository`, `homepage`, `bugs`, `author`** — `npm info` / GitHub dependency graph / npmjs.com all show empty/wrong metadata | Fields absent from `package.json` | Add standard npm/package metadata fields | ✅ Fixed — added `author`, `homepage`, `repository`, `bugs`, `keywords` to root `package.json` |
| I29 | **A message whose body is raw HTML rendered as an empty bubble** (content delivered + persisted, but invisible) | `web/src/messageRender.tsx` ran react-markdown with no `rehype-raw`; react-markdown's default silently DROPS mdast `html` nodes, so an all-HTML body (e.g. an agent posting a `<div class="head head-agent">…` component, 909 chars) produced zero visible output. Confirmed against the running app + DB: content length 909, rendered `.mbody .md` empty | Added `remarkHtmlAsText` (remark plugin) downgrading every `html` node to a literal `text` node → raw HTML shows its escaped source (React escapes on render — no XSS, no new HTML-render surface). Unit test `test/messageRender.html.unit.test.ts` + browser-verified (HTML message shows source; `**bold**` / `#channel` links unaffected) | ✅ Fixed (this session) |
| I30 | **A reply threaded onto a system message becomes UI-unreachable** | `resolveTarget` (`core.ts`) matched a `#channel:<shortid>` parent by id-prefix with **no `senderType` filter**, so a shortid landing on a system message ("X claimed / created task") created a thread off it via `getOrCreateThread`; system messages render with no "open thread" affordance → the reply is delivered + persisted but invisible. Observed live: `message send --target "#…:27abf892"` (27abf892 = a system "claimed" message) buried the reply in `thread-27abf892` | `resolveTarget` now returns `null` when the resolved parent is a system message → `/agent-api` surfaces `404 TARGET_FAILED` instead of silently burying it. Integration test `test/threadTarget.integration.ts` (real pg: real message → thread OK; system message → null + no orphan thread) | ✅ Fixed (this session) |
| I32 | **`drizzle-kit` (even latest `0.31.10`) hard-depends on the deprecated `@esbuild-kit/esm-loader` → `@esbuild-kit/core-utils` → old `esbuild` chain (GHSA-67mh-4wv8-2f99, moderate)** — no drizzle-kit release sheds it; `npm audit fix --force` only "fixes" it by *downgrading* drizzle-kit to `0.18.1` (wrong — breaks schema push) | `package.json` → `overrides["@esbuild-kit/core-utils"].esbuild = "^0.25.0"` forces the patched esbuild into that subtree (a fresh `npm install` is required for the override to fully resolve; an incremental install leaves the nested copy `invalid`) | **Do not remove the override** or the moderate vuln returns. The advisory is esbuild's *dev-server* CORS leak, which is **not on any path we run** (drizzle-kit uses @esbuild-kit only to transpile `drizzle.config.ts`, it never starts a serve) — so it was non-exploitable even before; the override just keeps `npm audit` at 0. Drop it once drizzle-kit ships without @esbuild-kit | ✅ Done (closes #22; verified `drizzle-kit push` still loads config + applies schema after the override, full test suite + socket WS smoke green) |
| I34 | **Task status mutators promoted a plain message into a numberless task (`#-`/`#null`)**, and **DM tasks shared the workspace counter** | `claimTask`/`unclaimTask`/`setTaskStatus` did an unconditional `UPDATE … SET taskStatus=…` with no `isNotNull(taskStatus)` guard and minted no number; agent `task/claim`·`task/update` resolve *any* message id → an agent claiming/updating a plain **DM** message turned it into a task with `taskNumber=null` (audit trail literally read `#null`). Separately every task number came from one per-server counter, so DM and channel tasks were indistinguishable | **Invariant `taskStatus ⟺ taskNumber`**: added `isNotNull(taskStatus)` to the three mutators (a plain message → 404, no promotion); agent claim/update auto-convert via `convertMessageToTask` first (canonical mint). **Per-DM numbering**: `taskNumberKey` routes DMs to `tasknum:dm:<channelId>`; `reconcileCounters` reconciles every scope on boot; frontend labels DM tasks `@peer` + gives DMs a `chat\|tasks` board; status popover switched to `Select.tsx`'s fixed positioning (was trapped under the next card's transform stacking context). `scripts/heal-task-numbers.ts` backfills legacy numberless rows. Verified: real HTTP agent-api chain + browser. ⚠️ **Introduced**: a bare `task #N` is now unique only within its scope — inline `task #N` resolution (`Chat.tsx navToken` → `/api/tasks/server` first-match) can mis-resolve across a DM/channel #N collision. Acceptable (DM scopes are 1:1 + the in-channel local match wins first); revisit if it bites | ✅ Fixed (this session) |
| I35 | **In a thread, `@`-mentioning a teammate who hadn't replied in that thread yet was silently dropped — mention unrecorded, agent never woken** | `core.createMessage` gated mention auto-join on `ch?.type === "channel"`, lumping `thread` with `private`/`dm` as excluded. A thread's `channelMembers` is only "who has replied so far", and `parseMentions` matches `@name` **only against that set**, so `@an-outsider` resolved to nobody → no `message_mentions` row, no wake delivery, and the frontend rendered the `@` as plain text (an `@` the server never recorded). Reproduced live in the prod DB: thread `a879e694` had `@claudecode-dev` in message bodies (seq 32/35/45) yet **empty** `message_mentions` every time; `claudecode-dev` was a member of the parent channel but never of the thread | **A thread now inherits its parent channel's @-reach** (`core.mentionAutoJoinPool`, the same parent-channel inheritance `socketio.canReadChannel` uses): public-parent thread → workspace pool, private-parent thread → parent members only, dm-parent → none; `autoJoinMentioned` takes the candidate pool as a param instead of always `workspaceMembers`, and the gate is now `ch && content.includes("@")`. Unit `test/mention.unit.test.ts` pins the security boundary (a public space pulls any teammate in; a members-only space never pulls an outsider) + integration `test/mention.integration.ts` cases [5]/[6] exercise the real thread→parent-channel path against Postgres (public-parent thread auto-joins + wakes a non-member; private-parent thread stays a no-op). The orphaned-thread fallback (parent message/channel deleted → `mentionAutoJoinPool` degrades to thread-own members) now logs a `warn` so a silently-dropped `@` stays debuggable. Real-run verified on an isolated stack: public-channel thread `@agentbravo` (non-member) → mention recorded + auto-joined + `wakeAgents:["agentbravo(@)"]`; private-channel thread `@agentbravo` (non-parent-member) → no mention, no auto-join, no wake (no leak). ARCHITECTURE.md mention-auto-join invariant rewritten | ✅ Fixed (this session) |
| I36 | **`thread` channels still slip past a few `type === "channel"`-only guards** (thread-as-channel audit, follow-up to I35) | A full sweep of `type ===` branches while fixing I35 found three spots that mishandle a thread but are **independent of the @-wake bug**: ① `routes-agent.ts` `server/info` filters `c.type !== "dm"`, so thread channels (internal `thread-xxxx` names) appear in the agent's channel list, polluting its context; ② `routes-api.ts` PATCH channel `visibility` has no `type !== "thread"` guard → a thread's `type` can be flipped to `channel`/`private`, breaking the `parentMessageId` invariant (it'd surface as a stray fake channel in the sidebar); ③ archive + user-side `join` endpoints have no thread guard (low impact — thread ids aren't user-enumerable) | **Fixed** (three surgical changes, all gated by a failing integration test `test/threadChannelGuards.integration.ts`): ① `routes-agent.ts:237` `server/info` filter adds `&& c.type !== "thread"` — thread channels never leak into agent context; ② `routes-api/channels.ts` PATCH **and DELETE** handler fetches the channel type first and returns 403 if `type === "thread"` (guard fires before the PATCH/DELETE branch split, protecting both — DELETE is correct to block since thread lifecycle follows parent message); ③ join and leave handlers add `if (ch.type === "thread") return 403`; archive handler adds the same guard after the `manageChannels` capability check. Regression: public channel join/archive/unarchive unaffected (still 200). All 18 checks pass (test [6] covers DELETE → 403; 8 fail on origin/main, 0 fail after fix). Typecheck clean. | ✅ Fixed (follow-up PR to I35) |
| I37 | **Agent Live Trace panel: traces vanished on channel/DM switch + `agent_activity_log` grew unbounded + long lines forced horizontal scroll** | `web/src/views/Chat.tsx` kept `traj` in local state and wiped it (`setTraj([])`) on channel switch; `agent_activity_log` was insert-only (no retention) in `ws.ts logActivity`; `.traj` lines had no wrap and `traj-col` had `overflow:auto` | **Fixed (task #2)**: (1) lifted the trace feed into a bounded ring buffer in `store.tsx` (new `web/src/trajBuffer.ts`, cap 300, drop-oldest) so it survives channel/DM switches; (2) `logActivity` now prunes to the newest 500 rows per agent on insert (`pruneAgentActivityLog` / `ACTIVITY_LOG_CAP` in `ws.ts`); (3) CSS `.traj{overflow-wrap:anywhere}` + `traj-col{overflow-x:hidden}`. Verified: `trajBuffer` unit 6/6, prune integration 6/6 (isolated DB), CSS no-h-scroll + wrap in a real browser. **Remaining trade-offs**: (a) prune runs on *every* insert — under very high trace volume that is a delete-per-insert; candidate for batching / probabilistic pruning if it shows up under load; (b) refresh-restore deferred per @fancy — could later rehydrate from the existing `GET /api/agents/:id/activity-log`, which for the *global* feed would need a new cross-agent recent-activity endpoint | ✅ Done (task #2) |
| I38 | **UI advertised runtimes the daemon couldn't run** (gemini, opencode/kimi/pi/cursor stubs) | `detectRuntimes()` (`runtimes.ts`) probed `claude/codex/copilot/kimi/gemini/opencode` and `RUNTIMES` (`Members.tsx`) listed `gemini`/`opencode`, but `REG` only implemented `claude`/`codex`/`copilot`. Selecting an un-implemented one created an agent that immediately went `offline` with `no runtime: <x>`. Surfaced while adding copilot | **gemini purged** (Google retired the standalone Gemini CLI 2026-06-18, folded into Antigravity — removed everywhere). **opencode + kimi + pi + cursor now implemented** (`opencodeRuntime.ts` / `kimiRuntime.ts` / `piRuntime.ts` / `cursorRuntime.ts`, in `REG`). Every runtime `detectRuntimes()` probes now has an adapter | ✅ Resolved — drift closed (gemini removed; copilot/opencode/kimi/pi/cursor implemented) |
| I51 | **claude effort levels applied globally, not per-model** | `listModels.ts` probed `claude --help` once for the global `--effort` superset and attached it to *every* claude model, so Sonnet/Haiku advertised `xhigh` (Opus-only) and Haiku advertised `max` — selecting an unsupported level fails at agent runtime (`error` activity), defeating discovery's whole point | **Fixed**: `listModels.ts` now parses the `--effort` superset and **projects it through a per-model allow-list** (`CLAUDE_MODEL_EFFORT_ALLOW` — multica's `claudeModelEffortAllow` collapsed to our short ids: opus=low/med/high/xhigh/max, sonnet=low/med/high/max, haiku=low/med/high) + friendly labels (`xhigh`→"Extra high"). Result is `superset ∩ allow-list`, so a CLI bump still narrows correctly. Unit-tested (6 cases in `listModels.unit.test.ts`); live-verified on an isolated stack (sonnet no longer offers xhigh, haiku drops xhigh+max). **Caveat — the allow-list is hand-maintained**: a new Anthropic model absent from the map keeps the full superset (defensive) until the table is updated. | ✅ Fixed (this PR) |
| I52 | **First runtime-models probe (up to ~8s timeout) has no loading indicator in the create-agent modal** | `runtimeModels.ts` fires a `probe-models` WS-RPC on cache miss; the timeout is intentionally generous (~8s) to survive a slow shell spawn. During that window the model dropdown in the create-agent modal is populated with a static fallback list (or empty), with no spinner/skeleton. A user who picks a model before the probe resolves gets the stale static list | **Fixed** (`web/src/views/Members.tsx`): added `modelsLoading` state; set true before fetch, false after (finally + cancel flag for in-flight cleanup). Model Select wrapped in `<fieldset disabled>` during probe (opacity 0.6 + "Detecting models…" placeholder). Selection preserved if the previous `model` id still appears in the new list; otherwise falls back to `ms[0]`. | ✅ Fixed (web-polish PR) |
| I40 | **Landing reset defeats per-element `margin-top` on `<h1>/<h2>/<h3>/<p>`** | `landing.css` `.lp-root h1,h2,h3,p { margin: 0 }` has specificity (0,1,1), which beats single-class rules like `.lp-hero__sub { margin-top: var(--lp-space-8) }` (0,1,0) → those margins silently collapse to 0. Caused the hero `<h1>` title and `<p>` subtitle to overlap (verified: text-ink gap was −2px). DIV/SPAN elements (e.g. `.lp-hero__actions`) are unaffected, which is why only the text elements collided | **Fully fixed** (`web/src/landing/landing.css`): changed reset to `:where(.lp-root) :where(h1,h2,h3,p) { margin:0 }` (specificity 0,0,0), so every single-class rule wins without needing compound-selector workarounds. `.lp-section-lead` and `.lp-engine__desc` `margin-top` now resolve correctly. Hero section's existing `.lp-hero__intro`-prefixed selectors (0,2,0) remain valid and unaffected. | ✅ Fixed (web-polish PR) |
| I43 | **`POST`/`DELETE /servers/:id/machines` lack a `manageMachines` capability check** — any workspace member (not just owner/admin) can create or delete machines | `routes-api.ts`: the create + delete machine endpoints gate only on server membership (the ~line 227 `not a member` check), never `requireCap(…, "manageMachines")`, even though that capability exists (`capabilities.ts`, owner/admin-only) and the frontend already hides machine management from members. The new `POST …/machines/:id/reconnect` (this PR) **does** enforce `manageMachines`, establishing the pattern — create/delete still need it for parity | Add `if (!await requireCap(serverId, userId, "manageMachines")) return 403` to the create + delete machine endpoints | ✅ Fixed — both endpoints now enforce `manageMachines`; owner-create regression-verified (200) in the authorization-hardening PR |
| I45 | **Reconnect catch-up: a hand-kept cross-file mirror + an out-of-scope case + 3 logic-only verifications** | `reconnectCatchup.ts` (+ `core.ts` createMessage wake branch) | (a) Fixed: `agentWakePolicy.ts` now holds the shared wake criterion used by live delivery and reconnect catch-up. It also closes the multi-agent chatter loop class: agent-authored ambient messages do not wake peer agents, while DM and explicit @ still do. (b) Agents with `machineId = null` remain out of scope — no machine-reconnect event fires for them; recovery falls back to the next @. (c) Not separately E2E'd (all share the verified `onReady` path): the soft-offline `agent:deliver` path, the 30s per-machine cooldown guard (`OPEN_TAG_CATCHUP_COOLDOWN_MS`), and the server-restart trigger. | Add targeted E2E only if a reconnect path proves fragile; keep machineId-null behavior @-driven unless a separate design says otherwise. | ✅ Partially fixed — shared policy + loop prevention landed; remaining items are explicit deferred scope |
| I46 | **Member-profile "Created Agents" card leaked soft-deleted agents** (+ a note on the dup-name race window) | The profile endpoint `GET /servers/:sid/members/:uid/profile` (`routes-api.ts`) queried `agents` by `(serverId, creatorId)` **without** `isNull(deletedAt)`, unlike every other list path (`GET /api/agents`, `core.workspaceMembers`, agent-api `server/info`), so a soft-deleted agent stayed visible in the human-profile card — its status dot reads straight from `agents.status`, which soft-delete sets to `inactive`. Surfaced with I26: the user's visible duplicate `codex-codereview` pair was a deleted-then-recreated same-name pair leaking through this hole | Added `isNull(schema.agents.deletedAt)` to the profile query, matching the global list. **Race-proofed (this session):** dup-name rejection uses `onConflictDoNothing` against the partial index (single path, no SELECT-then-INSERT gap) → a duplicate live name returns 409 with no 500 race window (verified: 8 concurrent same-name POSTs → 1×200 / 7×409 / 0×500, one live row) | ✅ Fixed (profile leak, curl-verified delete→card empty; dup-name guard race-proofed via onConflict — 0×500 under concurrency) |
| I79 | **The `target=` shown to agents was unresolvable for thread-anchor messages** → reply silently misfired/404 | `fmt` (`src/server/routes-agent.ts`) rendered a message with `m.threadId` (a task/thread-anchor) as `#chan:<threadChannelId8>`, but `resolveTarget` (`core.ts`) parses the `:suffix` as a **parent message id** prefix (`messages.id LIKE suffix%`). A thread *channel* id never matches a *message* id → `null` → `404 TARGET_FAILED`. Since the standing prompt tells agents to "reuse the `target=` value", reusing the shown target for any threaded message failed — observed live (an agent's replies landed in a task thread / `#chan:<threadId>` reads 404'd) and surfaced during task #3 bug-hunt | **Fixed**: `fmt` now emits `#chan:<m.id8>` (the anchor message's own id = the thread's parent), matching `addressableTarget`'s convention so the shown target round-trips through `resolveTarget`. Regression test `test/threadTargetRoundtrip.integration.ts` (real DB: task message's shown target resolves to its thread; plain message resolves to its channel) — RED before fix, GREEN after; existing `threadTarget.integration.ts` still passes. Related to I30 (thread/target resolution). **Not yet live-verified** (would require restarting the running server, which would disrupt active agents) | ✅ Done (task #3) |
| I47 | **Two conflicting `.seg` rules in `styles.css`** — `.seg` is defined twice: one pill-shaped (`border-radius:9999px`, `.seg-opt` children, for the landing/language picker) and one rounded-rect (`border-radius:8px`, `button` children, for the task toolbar). Same specificity → the later (8px) rule wins for *every* `.seg`, so the pill variant's `9999px` is silently clobbered. Pre-existing; surfaced by code-review of the board-layout-toggle PR, which added a `.seg-icon` modifier on the 8px (task-toolbar) variant | `web/src/styles.css` two `.seg{…}` blocks (`border-radius:9999px` vs `:8px`); new `.seg-icon` correctly extends the 8px one but inherits the ambiguity | **Fixed** (`web/src/styles.css` + `web/src/views/misc.tsx`): renamed first `.seg` (pill) to `.seg-pill`; updated the one JSX callsite in `misc.tsx`. `TaskBoard.tsx` keeps `.seg` (rect) unchanged — no collateral touch. No more duplicate `.seg` rule. | ✅ Fixed (web-polish PR) |
| I48 | **npm package `@fancyboi999/open-tag-daemon` lagged `main` → prod ran a daemon with no `copilot` runtime** (`offline · no runtime: copilot`). `0.1.0` was published 2026-06-24 19:45, *before* #44 (copilot/opencode/kimi/pi/cursor, merged 23:57) — and #45–#54's daemon changes never shipped either. Prod/self-host machines run `npx @fancyboi999/open-tag-daemon`, so merging to `main` never reached them; CI was green and docs synced, masking it. | `packages/daemon/package.json` (was `0.1.0`); `publish-daemon.yml` fires only on a *published GitHub Release*; nothing flagged "daemon change ⇒ cut a release". Also: npm **Trusted Publisher was never actually registered** (I33 marked OIDC "Done" but it had never published — first OIDC run E404'd). | Added a **Release discipline** section + doc-sync row in `AGENTS.md` (merged ≠ shipped) + this entry; bumped `packages/daemon` → `0.2.0`, cut the `v0.2.0` Release. Registered the npm Trusted Publisher (owner `fancyboi999` / repo `open-tag` / workflow `publish-daemon.yml`), reran the workflow → OIDC publish succeeded. Restarted mymac's daemon on `npx …@0.2.0`. | ✅ Fixed (v0.2.0 live on npm via OIDC; mymac daemon @0.2.0 ready with 7 runtimes incl. `copilot`; `copilot-dev-codereview` started `runtime=copilot`, **0× "no runtime"** in the new daemon log) |
| I49 | **`daemonVersion` was hard-coded `"0.1.0"` in `src/daemon/index.ts`** → every machine reported a fake version (mymac showed `0.1.0` while running the 0.2.0 bundle with all 7 runtimes) | `index.ts:59` put a literal `"0.1.0"` in the `ready` payload regardless of the published package version; surfaced while verifying I48 (runtimes updated to include copilot but the version field stayed 0.1.0) | `index.ts` now reads `process.env.DAEMON_VERSION ?? "dev"`; `build-daemon-pkg.mjs` injects the real `packages/daemon/package.json` version via esbuild `define` at bundle time (verified: `dist/cli.mjs` compiles `daemonVersion: "0.2.1"`); source/tsx runs report `"dev"` | ✅ Fixed (bundle inlines the package version; typecheck green; ships in 0.2.1) |
| I80 | **Empty states unified into a two-tier system** | A shared `<PaneEmpty>` (`web/src/PaneEmpty.tsx` — icon + EB Garamond serif headline + optional `--muted` sub-line, vertically centered) now backs every **primary full content-pane** empty: Inbox, Saved, the Tasks board, an empty channel (`Chat.tsx`, gated on a `loaded` flag so it never flashes mid-fetch), and the no-machine onboarding hint. **Secondary sub-section** empties that sit under a section header inside a profile/detail tab (Skills / Apps / DMs / Reminders / Workspace / Activity, plus channel Files) deliberately keep the inline `.empty` — a giant serif headline under a "DMs" header (or in the narrow `traj-col` profile panel) would over-dramatize a minor state — but `.empty` (and `.sel-empty` / `.msg-sys`) now use `--muted` (clears WCAG AA on the off-white canvas; the prior `--muted-soft` ≈2.4:1 failed). Tertiary metadata (counts, timestamps, file sizes, sub-eyebrows) stays `--muted-soft` by design. **Search "no results" stays inline** (renders below a persistent search input; centering would float it far from the box). | n/a — the remaining inline empties are an intentional altitude choice, not pending work | ✅ Done |
| D7 | **Repo lacked all community-trust signal files** — no CONTRIBUTING, CODE_OF_CONDUCT, SECURITY, CHANGELOG, issue templates, PR template, or FUNDING. First-time contributors had no guidance; security reporters had no private channel; GitHub Community Profile was empty. | `gh api repos/fancyboi999/open-tag/community/profile` showed 0% completion | Added `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md` (Contributor Covenant 2.1), `SECURITY.md` (GitHub private vuln reporting + placeholder contact), `CHANGELOG.md` (Keep-a-Changelog, back-filled v0.1.0–v0.4.0 from real release notes), `.github/ISSUE_TEMPLATE/{bug_report,feature_request}.yml` + `config.yml`, `.github/PULL_REQUEST_TEMPLATE.md`, `.github/FUNDING.yml`. Placeholder contact fields `<INSERT CONTACT>` and `<security contact — fill me>` must be filled by the maintainer before publishing. | ✅ Done (PR #gov-files) |
| S1 | **SEO / discoverability: `web/index.html` had bare `<title>open-tag</title>` + no meta/OG/favicon/robots/sitemap** — crawlers got zero signal | Confirmed by inspecting `web/index.html` pre-PR; Google rich-results preview empty | ✅ Fixed (this PR): descriptive title + meta description + full OG + Twitter Card + canonical + theme-color + SoftwareApplication JSON-LD in `web/index.html`; `favicon.svg` + `og-image.png` (hero screenshot, 4800×2520, 1.91:1) + `robots.txt` + `sitemap.xml` in `web/public/`. **Remaining follow-up** (outside this PR): ① register a real domain and replace `https://open-tag.dev` placeholder in all four files (canonical/OG/robots sitemap ref/sitemap loc); ② design a polished brand og-image (current image is the product screenshot — functional but not designed); ③ body pre-rendering / SSG for `<body>` content crawlability (SPA still returns empty `<div id="root">` to crawlers; `<head>` SEO is immediate win). **Update 2026-07-06:** ① done — getopentag.com live, `open-tag.dev` placeholders replaced; ③ done — see S3 (SSR prerender); ② og-image polish still open as a cosmetic item |
| I54 | **Docs site scaffolded but content migration + deployment are pending** | `docs-site/` Astro Starlight scaffold added (PR #69); build passes, 6 core pages with curated overviews + repo links as the single source of truth. Three items remain: (1) replace `hero-placeholder.svg` with a real workspace screenshot; (2) add `public/og.png` (1200×630) for the Twitter card; (3) deploy to a real domain (Vercel/Netlify) and set `site` in `astro.config.mjs` to the production URL. Additional content pages (Runtimes reference, Agent Collaboration guide, CLI reference) are deferred for a follow-up. | Complete before public site launch | ❌ Obsolete (2026-07-06) — the Starlight scaffold this entry describes was replaced by the single custom Astro page (`docs-site/src/pages/index.astro`, `verify:onepage` guard); GitHub Pages deploy to docs.getopentag.com shipped (`.github/workflows/docs-deploy.yml`); `og.png` + real workspace screenshot exist in `docs-site/public/` |
| I59 | **Demo runtime — daemon npm release required before `npx` self-hosters get it** | `demoRuntime.ts` was added in `src/daemon/`; Railway demo builds from source and picks it up immediately. But self-hosted machines run `npx @fancyboi999/open-tag-daemon@latest` — that package is only updated when a GitHub Release is cut (per Release discipline in `AGENTS.md`). Until then, `npx` machines won't have the demo runtime in their `detectRuntimes()` list | Bump `packages/daemon/package.json` minor version → cut a GitHub Release. The `publish-daemon.yml` CI workflow handles the npm publish via OIDC Trusted Publishing | ❌ Obsolete — the demo runtime was **removed entirely** (rm-demo-runtime PR): UI option, i18n, registry, `demoRuntime.ts`/test, the `listModels` case, and the `servers.ts` model fallback are all deleted; `detectRuntimes()` no longer appends `demo`. No demo-specific release is needed anymore. (`packages/daemon/package.json` still bumped 0.5.1→0.6.0 so the removal itself reaches `npx` self-hosters on the next GitHub Release.) |
| I55 | **`npm run build --prefix web` emitted a Vite 500 kB chunk warning** — the entire client (react-dom, react-markdown+plugins, dnd-kit, dicebear, i18n, socket.io-client, app code) landed in a single `index-*.js` at ~1,065 kB (gzip 330 kB). Vite's default `chunkSizeWarningLimit` is 500 kB. No split = longer first-load parse time and no HTTP caching granularity. | `web/vite.config.ts` had `build: { outDir: "dist", emptyOutDir: true }` and no `rollupOptions`. | **Fixed** (`web/vite.config.ts`): added `rollupOptions.output.manualChunks` — 6 named chunks: `react-vendor` 161 kB, `markdown` 163 kB, `avatars` (dicebear) 380 kB, `i18n` 50 kB, `dnd` 43 kB, `socket` 42 kB; app `index` 227 kB. Largest chunk (avatars) is 380 kB — under the 500 kB threshold. Warning gone. | ✅ Fixed (web-polish PR) |
| S2 | **MIME injection → same-origin XSS via inline attachment download** | `src/server/routes-api/attachments.ts` used `a.mimeType` verbatim (client-controlled via multipart `Content-Type`) as the HTTP `content-type` header with `Content-Disposition: inline`. An attacker uploading an HTML file declared as `text/html` received that type back on download → browser inline-executed the script on the same origin → could steal localStorage JWT. | **Fixed** (this PR): `sanitizeMimeType()` normalises/strips params before storage; `safeDownloadHeaders()` enforces a safe-inline whitelist at serve time — only images/audio/video/PDF; `text/html`, `image/svg+xml`, `text/javascript`, `application/xhtml+xml` and all unlisted types are forced to `Content-Type: application/octet-stream` + `Content-Disposition: attachment`. Download-time gate covers both new uploads and legacy DB records. 23/23 unit tests (`test/mimeXssGuard.unit.test.ts`) + real curl evidence. | ✅ Fixed (this PR) |
| I60 | **Human REST plane: `GET /api/messages/channel/:id`, `POST /api/messages`, `GET/POST /api/tasks/channel/:id` had no channel-membership check** — a same-tenant non-member who knew a private/DM channel UUID could read full message history and write to it (IDOR, invariant 3). The agent plane and the socket.io room-join already had equivalent guards (`canAgentReadChannel` / `canReadChannel`); the human REST routes were left unguarded. | `src/server/routes-api/messages.ts` (~L103-120), `src/server/routes-api/tasks.ts` (~L12-26) | Added `canUserReadChannel(serverId, channelId, userId)` in new `src/server/channelAccess.ts` (same logic as the existing agent-plane mirror: member → ok; public → ok; thread → inherits parent; private/DM non-member → 403). Applied to all four endpoints. Integration test `test/channelAccess.integration.ts` (6 non-member checks RED on main, ALL GREEN after fix, public-channel regression + DM isolation verified). `docs/authorization.md` invariant 4 + §6 updated. | ✅ Fixed (private-channel-idor PR) |
| I62 (IDOR-B2) | **Residual private-channel IDOR on three human-REST endpoints** — verifier of I60/PR #73 surfaced three uncovered endpoints: (1) `GET /api/channels/:id/members` had a server-ownership check but no channel-membership check — non-members could enumerate private/DM channel rosters; (2) `GET /api/channels/:id/files` was only `serverId`-scoped — non-members could list files in private/DM channels; (3) `POST`/`DELETE /api/messages/:id/reactions` checked message existence but not channel membership — non-members could add/remove reactions on private-channel messages. | `src/server/routes-api/channels.ts` (members + files handlers), `src/server/routes-api/messages.ts` (reactions handler) | Added `canUserReadChannel(serverId, channelId, userId)` pre-check to all three handlers, returning 404 on failure (reuses existing `channelAccess.ts` guard — no new logic). Integration test `test/channelAccessB2.integration.ts` (8 non-member cases RED on main, ALL GREEN after fix, public-channel + DM regression checks included). `docs/authorization.md` §5 + §6 updated. | ✅ Fixed (sec-idor2 PR) |
| I65 (IDOR-B3) | **Attachment download + thread list/create lacked channel-membership check** — `GET /api/attachments/:id` ran at Gate 0 (before the member-gate middleware) and served the file to any authenticated user who knew the attachment UUID, regardless of channel membership. `GET /api/channels/:id/threads` and `POST /api/channels/:id/threads` had no membership check — non-members could enumerate thread metadata (reply count, last-reply timestamps) and create threads in private/DM channels, effectively joining conversation threads in channels they cannot see. | `src/server/routes-api/attachments.ts` (`handlePublicAttachmentGet`), `src/server/routes-api/channels.ts` (GET threads handler, POST threads handler) | Added `canUserReadChannel` gate to all three handlers: attachment → 404 on denial (existence-hiding; if `channelId` null, falls back to serverMembers check for avatar-type uploads); threads GET → 403 using URL channel id; threads POST → 403 using `parent.channelId` (from DB-fetched parent message, more robust than URL). Integration test `test/channelAccessB3.integration.ts`: 4 non-member cases RED on main (attachment: 200; threads GET: 200+data; threads POST: 200), ALL GREEN after fix; public-channel + avatar regression checks included. Typecheck clean. `docs/authorization.md` §5 Slice 4 + §6 IDOR-B3 Fixed entry added. | ✅ Fixed (sec-idor3 PR) |
| S3 | **Landing-page `<body>` was empty `<div id="root">` — crawlers got no text content** (S1 follow-up ③) | `curl https://getopentag.com/` returned `<div id="root"></div>`; Googlebot renders only as much JS as its time-box allows, so the full landing copy was invisible to search engines | ✅ Fixed (`web-seo2` PR): added Vite SSR build step (`web/src/entry-server.tsx` + `web/scripts/prerender.js`). At the end of `npm run build --prefix web`, `prerender.js` builds an isolated SSR bundle via Vite's Node API (CSS imports stripped automatically), calls `renderToString(<StaticRouter><StoreProvider><Landing/></StoreProvider></StaticRouter>)`, and patches `dist/index.html` — replacing `<div id="root"></div>` with the fully rendered HTML. Client-side: `ReactDOM.createRoot().render()` (not `hydrateRoot`) replaces the prerendered HTML cleanly on load — no hydration mismatch. Verified: `grep "Where your team and its AI agents work as one" web/dist/index.html` exits 0; `npm run build --prefix web` clean with zero new warnings. SPA routing unaffected (React replaces entire root on mount). **Caveats not verified**: prod smoke test (live `curl getopentag.com` after deploy); "Enter workspace" button behaviour in real browser post-hydration (unit-tested at SSR level only); og-image design (still the screenshot placeholder) |
| I64 | **`clientIp()` took the leftmost XFF value — client-forgeable, rate-limit bypass** | `src/server/ratelimit.ts` (pre-fix): `Array.isArray(xff) ? xff[0] : xff` then `fwd.split(",")[0]`. When `TRUST_PROXY=true` and a reverse proxy appends the real client IP, the chain is `"<forged>, <real>"`. Taking `[0]` returned the client-controlled forged value, letting an attacker rotate XFF on every request to bypass per-IP rate-limiting. Bug class: XFF leftmost vs. rightmost (standard 1-hop proxy trust model). | **Fixed** (this PR): `clientIp()` now joins array XFF headers, splits on `,`, trims, filters blanks, and returns `parts[parts.length - 1]` (rightmost = the hop the trusted proxy appended; unforgeable). Falls back to socket address when XFF is absent or TRUST_PROXY is unset. Assumes exactly one trusted proxy hop; multi-hop deployments (CDN → nginx → app) are documented as out-of-scope in the code comment + AGENTS.md. Unit test `test/clientIp.unit.test.ts` (7 cases) — RED on main for cases [2]/[3]/[4] (forgery / three-hop / array), all 7 GREEN after fix; no regression on `registerRateLimit` (6/6). **Operator action for Railway**: set `TRUST_PROXY=true` in env vars to activate the corrected guard. | ⚠️ Superseded by I67 — "rightmost" was wrong for Railway (see I67) |
| I67 | **I64's "rightmost XFF" fix was wrong for Railway — rate limit still didn't work in prod** — after deploying I64 + setting `TRUST_PROXY=true`, a live probe of getopentag.com showed `clientIp()` never produced a stable IP, so registration rate-limiting never triggered (14 requests, no 429). Root cause: I64 assumed the proxy *appends* the real client (real = rightmost, nginx-style). Railway's Envoy does the opposite — it *prepends* the real client and *appends* its own **rotating** edge hop, so `X-Forwarded-For` is `"<realClient>, <edgeIP>"` and the rightmost is the per-request-varying edge IP → every request got a fresh bucket. | `src/server/ratelimit.ts` `clientIp()`. Ground truth from a temporary prod echo endpoint: forged `X-Real-IP: 6.6.6.6` + `X-Forwarded-For: 7.7.7.7, 8.8.8.8` arrived as `x-real-ip=<realClient>`, `xff="<realClient>, 152.233.33.x"` — both client values stripped/overwritten by Railway. | **Fixed** (this PR): `clientIp()` now prefers **`X-Real-IP`** (single clean value, overwritten by the proxy) and falls back to the **first** `X-Forwarded-For` hop (real client, prepended). Unit test `test/clientIp.unit.test.ts` rewritten to the correct model (9 cases incl. "first hop not rightmost", X-Real-IP precedence, empty-header fallback) — RED on main (rightmost impl) for the precedence cases, GREEN after. Verified on prod after redeploy: register requests with rotating forged XFF still share the real-IP bucket → 429 (forgery ineffective). The anti-spoof guarantee comes from Railway overwriting the headers (probe-proven), not the parser. Possible future hardening: a hop-count-aware lib (`proxy-addr`) for multi-hop chains. | ✅ Fixed (this PR) |
| I66 | **Remote daemon injected the server's `localhost` address into agents** → every agent on a daemon running on a *different host* than the server got `OPEN_TAG_SERVER_URL=http://localhost:PORT`, so every `open-tag` CLI call failed `fetch failed` (live incident: getopentag.com server + local mac daemon; the agent reported it as "5XX / service down", but cli.log showed `fetch failed` — connection, not HTTP error). Root cause: `core.ts:13` `SELF_URL = http://localhost:${PORT}` (no env override) was sent verbatim as the `agent:start` `config.serverUrl` and injected as the agent's `OPEN_TAG_SERVER_URL`. Correct only when daemon and server share a host (the original self-host assumption); wrong for split deploys (cloud server + remote daemon). | `src/server/core.ts:13` (`SELF_URL`), `src/daemon/index.ts:44` (`agent:start` handler), `src/daemon/agentManager.ts:103` (injects `OPEN_TAG_SERVER_URL`) | **Fixed** (daemon side, single source of truth): `index.ts:44` now overrides `config.serverUrl` with the URL **this daemon connected with** (CLI `--server-url`, already proven reachable) before spawning — the agent always dials the same server the daemon did. End-to-end verified: a source daemon connected to `https://getopentag.com` spawned an agent whose `OPEN_TAG_SERVER_URL=https://getopentag.com`, and cli.log `message/check`+`message/send` went back to `200`. Server-side `SELF_URL` left as-is (now harmless — the daemon overrides it). **Residual debt: `SELF_URL` is now vestigial/misleading; remove it or make it env-configurable in a follow-up.** Shipped in daemon `0.5.1`. | ✅ Fixed (daemon 0.5.1) |
| I69 | **Showcase demo agents reserve the agent name namespace — a user can't create a real agent named after a prop (Pat/Cody/Alice/…)** — the 13 `creatorType="system"` props are live `agents` rows (`deletedAt IS NULL`), so they hold the `agents_name_uniq` `(serverId, name)` partial unique index; `POST /api/agents` with such a name returns 409 "already exists", but the user can't see who holds it (props are hidden from the roster by `visibleAgents`). | `src/server/showcaseSeed.ts` (`SHOWCASE_AGENTS`), `src/db/schema.ts` `agents_name_uniq`, `src/server/routes-api/agents.ts` 409 path | Low-probability (prop names aren't common agent names) + benign (no security impact) → deliberately NOT fixed in the showcase-feedback PR. If it becomes a real annoyance: make `agents_name_uniq` also exclude `creator_type = 'system'` (props stop reserving names); `@`-resolution already prefers the real agent (`workspaceMembers` excludes props). Needs a schema/index change + prod migration. | ✅ Resolved — the prop agents were removed entirely when the showcase became a front-end static page (`showcaseSeed.ts` deleted; existing `creatorType="system"` rows dropped by `scripts/drop-showcase.mjs`), so they no longer reserve any name. |
| I74 (IDOR-B4) | **Human-plane task/action-card write endpoints lacked the channel-membership check** — `POST /api/tasks/convert-message`, `PATCH /api/tasks/:id/(claim\|unclaim\|status)`, `DELETE /api/tasks/:id`, and `POST /api/actions/:id/mark-executed` mutated a task / action card identified by a message UUID with only `serverId` scoping — no `canUserReadChannel`. A same-tenant non-member who had obtained a private/DM channel's message UUID (e.g. before being removed) could promote / claim / unclaim / re-status / delete its tasks or mark its action cards executed. Lower exploitability than B1/B2/B3 (UUIDs only come from already-gated reads), hence LOW-MED; the last open human-plane IDOR in `docs/authorization.md` §6. | `src/server/routes-api/tasks.ts` (convert-message, claim/unclaim/status, DELETE handlers), `src/server/routes-api/messages.ts` (mark-executed handler) | Each handler now fetches the message by `(id, serverId)` and calls `canUserReadChannel(serverId, m.channelId, userId)` before the core mutator, returning **404** on denial (existence-hiding, matches reactions B2/I62). Guard at the route layer, not the shared `core.ts` mutators (agent plane gates those via `resolveMessageId` → `canAgentReadChannel`) to avoid double-gating the agent path. Integration test `test/channelAccessB4.integration.ts`: 6 non-member breach cases RED on main (all 200), ALL GREEN after fix; 2 regressions (non-member→public 200, owner→private 200) green. Typecheck clean. Not in CI (manual guard, like the B2/B3 siblings). `docs/authorization.md` §6 IDOR-B4 moved Pending→Fixed. | ✅ Fixed (sec-idor4 PR) |
| I75 (IDOR-B5) | **Saved-message bookmark endpoint leaked private/DM channel content** — `POST /api/channels/saved` fetched the message by `(id, serverId)` but skipped `canUserReadChannel`; a same-tenant non-member holding a private/DM channel's message UUID could bookmark it and then read its full `content` via `GET /api/channels/saved` (`core.listSaved` returns `content`) — a write endpoint that became a content-read of an invisible channel. Surfaced by the IDOR-B4 (sec-idor4 / I74) verifier as the same by-message-UUID-without-channel-check pattern. | `src/server/routes-api/messages.ts` (`POST /api/channels/saved` handler) | Added `canUserReadChannel(serverId, m.channelId, userId)` after the message fetch, **404** on denial (existence-hiding, matches B2/B4). Route layer only — `core.ts` `saveMessage`/`listSaved` are shared with the agent plane and untouched. Integration test `test/channelAccessB5.integration.ts`: 3 non-member breach cases RED on main (private save 200, content leak into GET /saved, DM save 200), ALL GREEN after fix; 2 regressions (public 200, owner own-private 200 + content) green. Typecheck clean. Not in CI (manual guard). `docs/authorization.md` §6 IDOR-B5 Fixed entry added. (I74 = sibling IDOR-B4, parallel PR #142.) **Residual CLOSED (sec-idor6):** the read path is now gated too — `listSaved` re-checks channel access per-plane (`canUserReadChannel`/`canAgentReadChannel`) at read time and drops saved messages the caller can't currently read, closing both the lost-access snapshot leak and the pre-fix illegitimate-row leak in one stroke with **no migration** (read-time gate is agnostic to how the row was created). Hides (not deletes); server pagination keeps the `limit+1` probe (filtered rows occupy a slot, `hasMore` stays correct), and the web Saved view (`web/src/views/misc.tsx`) was updated in the same change to paginate by DB-row offset (not `items.length`) — else filtering would cause duplicate bookmarks / a stuck "load more". Test `test/channelAccessB6.integration.ts`: 3 breach RED on main → ALL GREEN; still-accessible saved stay visible. Browser-verified (chrome-devtools): 25-saved / 3-inaccessible list renders 22 across two pages, no duplicates, no leaked content. | ✅ Fixed — write-gate (B5 / PR #143) + read-gate + web pagination (sec-idor6) |
| I94 | **`web/src/styles.css` failed the impeccable detector deep pass with a wall of legacy findings (~24-95 per run)** — mostly `design-system-font-size` (13px×45 / 11px×34 / 10px×6 literals off the marketing-born DESIGN.md ramp) plus 2 `side-tab` hits; every styles.css edit re-triggered the wall. | detector deep pass on any session touching `web/src/styles.css`; DESIGN.md typography ramp | **Resolved (design-ramp-sync)**: audited every off-ramp value instead of mass-suppressing. ① The de-facto app-shell steps are now **documented** in DESIGN.md (frontmatter + Hierarchy table): `ui-md` 13px / `ui-sm` 11px / `ui-xs` 10px / `display-xs` 22px / `mono-code` 12.5px / `mono-micro` 10.5px — dense operational UI legitimately sits below the marketing ramp. ② Four genuine near-miss drifts **fixed**: `.pe-title` 23→24px, `.avatar-edit-ph` 26→24px, `.ctx-rx button` 17→18px (aligns with `.rx-pop` 18px), `.msg-act-event` 12.5→12px (Inter row; half-steps reserved for mono). ③ The 2 `side-tab` hits are `.md`/`.ws-md` **markdown blockquote + GitHub-alert left borders** — the standard md affordance, false positive → file-scoped `side-tab` ignore in `.impeccable/config.json` with reason. Landing page audited on the way: its small sizes live in the ProductMock miniature UI + avatar initials (intentional), its one 3px ink left-border is the features-page outcome callout (intentional) — landing unchanged. | ✅ Resolved (audit + doc + 4 fixes + 1 scoped false-positive ignore) |
