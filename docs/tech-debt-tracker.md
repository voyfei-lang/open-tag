# Tech Debt & Doc Drift Tracker

> The "garbage collection" ledger for this codebase (tech debt compounds like high-interest loans — pay in small installments continuously rather than deferring to one big payoff).
> Each entry: symptom / evidence / recommended action / status. Append new drift when found.
> **This file holds only open items** (⬜ pending / 🟡 in progress / 🅿️ deferred-by-decision / 🟢 done-with-residue). When an entry is fully resolved (✅ / ❌ obsolete / ⚠️ superseded), **move its full row to [`tech-debt-archive.md`](./tech-debt-archive.md)** — the record is kept, the hot file stays scannable.
> **IDs are append-only and never reused** (D = doc drift, I = implementation, S = site/SEO + security one-offs). The next free ID = 1 + the highest ID across **both** this file and the archive. Cross-doc references like "see tech-debt I35" may resolve to either file — grep both.

## Open items

| # | Symptom | Evidence | Action | Status |
|---|---|---|---|---|
| I3 | `/api/messages/sync?since=` (seq incremental) endpoint exists but **web does not use it** — steady state uses socket.io full message:new + initial `GET /messages/channel/:id` | `web/src/store.tsx` | Clarify sync endpoint role (offline catch-up only?) and align in docs; evaluate before horizontal scale | ⬜ Pending |
| I5 | Architecture invariants (file ≤1000 lines, cross-plane import direction, boundary parse-don't-validate) **are only prose, not mechanically enforced** | No corresponding lint/CI | Add custom lint + structural tests, errors include fix instructions | ⬜ Pending |
| I6 | Single-instance socket.io emits directly; horizontal scale requires `@socket.io/redis-adapter` | `realtime.ts` has TODO comment | Add redis-adapter before scaling | ⬜ Pending (not blocking) |
| I12 | **Dual icon source**: `web/src/icons.tsx` custom SVGs coexist with `lucide-react` | `icons.tsx` still has IconWrench/IconFile/IconExternalLink/IconDownload in use; new UI defaults to lucide (slice05 composer uses ImagePlus/Paperclip/Send, message row toolbar uses Smile/MessageCircle/MoreHorizontal) | Progressively migrate icons.tsx remainder to lucide-react then delete | ⬜ Pending (progressive) |
| I16 | **`machine:status` has no `statusVersion` ordering protection** (agents may receive status events out of order) | `socketio.ts`/`ws.ts`/`store.tsx` (currently online/offline each publish once; frontend reloads machine list on event) | Add monotonic `statusVersion` + frontend drops stale versions | ⬜ Pending (low priority; current "reload on event" behavior is functionally correct) |
| I17 | **Daemon standing prompt missing several behavioral sections** | Diff against known correct standing prompt (345 lines): command model/threads/naming already aligned (slice07); **missing**: Credential hygiene (redact `sk_*_<redacted>`), Compaction safety (CRITICAL), Conversation etiquette, ~~Channel awareness~~ (✅ this session), ~~Discovering people/channels~~ (✅ this session), Formatting (Mentions/Channel Refs, non-ASCII URLs), Third-party integrations (`integration list/login/env`), `action prepare`, `inbox check` alias, `reminder log/update`, explicit Startup sequence, Splitting tasks for parallel | Fill in per confirmed ground truth (skin = our `open-tag` CLI, structure = standard behavior); credential redaction first (security) | 🟡 In progress (gaps G-H remain: Formatting/CJK-URL/historical ref/private channel confidentiality/task splitting parallel; `Channel awareness` + `Discovering people/channels` added this session in `prompt.ts` under `## Channels & people` + `### Channel awareness`, open-tag CLI skin, relies on existing `server info` capability; private/role markers deferred — depend on `server info` returning `visibility`/`role` fields, which `routes-agent.ts:190` currently only returns name/description/joined) |
| I18 | **Third-party integration login not implemented — intentionally deferred** | Agent detail Apps tab "CONNECTED APPS / Apps this agent has logged into". CLI `integration list/login/env`; endpoints `/integrations/clients`/`/integrations/marketplace`/`/integrations/agents/{id}`/`/integrations/grants/{grantId}/revoke`. Essentially: OAuth provider pattern for logging into external third-party apps (GitHub/Gitea etc.) | **Not building now**: self-hosted OSS has no real external services to connect to; building it now produces only an empty stub (violates real-verification principle). Build the specific integration when there is a real external service to connect to | 🅿️ Deferred (user decision 2026-06-17: documented, do when needed). **Do not proactively build empty stubs** |
| I23 | **Workspace tab deep-link reload returns `x-server-id header required`** + no Refresh button | Direct page reload to `/s/:server/agent/:id?agentTab=workspace` → `GET /agents/:id/workspace-files` missing `x-server-id` header (store/sidRef not ready when request fires; on reload agent detail data also partially broken Session/Status empty); **in-SPA navigation works fine**. Also: workspace panel is missing a Refresh button (the equivalent panel in similar products has one) | ① Don't fire serverId-dependent requests before store init (sidRef ready); or request layer waits for sidRef; ② add Refresh button to workspace tab | ⬜ Pending (low priority; only triggers on deep-link reload, SPA navigation unaffected) |
| I24 | **Several polish items from UX audit intentionally deferred** | Remaining non-critical items from a full-site persona review | Deferred (not omissions): ① trajectory column collapse — cross-component `Layout`/`Chat` grid coordination, high regression risk given recent scrim grid fix, and resizer already provides a workaround; ② reactions picker + mention menu full keyboard accessibility — mention menu now supports ↑/↓ + Enter/Tab selection with a highlighted-index + Esc to close (`Chat.tsx`); reactions picker keyboard access still pending (trigger is hover, needs focus management refactor); ③ j/k message navigation — not verified in comparable products, do not invent; ④ Help onboarding tour — already have empty-state guidance (add-computer onboarding modal/empty copy), full tour is over-engineering. **High-value items done this round**: Cmd+K global jump, site-wide :focus-visible, 7 modal Esc handlers, agent status dot aria, ConfirmModal (replaces native confirm×4), rail labels, DM uuid fallback, wake-state visualization, shadow elevation token into design system | 🅿️ Deferred (do when user requests) |
| I25 | **codex runtime has no per-agent `CODEX_HOME` / MCP isolation injection** | `src/daemon/codexRuntime.ts` current comments and implementation use user default `~/.codex`; `ARCHITECTURE.md` codex section marks this as a target spec not current implementation | Future: generate per-agent `CODEX_HOME/config.toml` (0o600, includes MCP server/env) for each agent, and handle auth/plugin inheritance strategy | ⬜ Pending |
| I27 | **Pre-existing agent names with spaces/illegal chars are not backfilled** | Creation-time validation (#3) only guards new agents via `POST /api/agents`; rows created earlier (e.g. an agent whose `name` is `Code-review master`) keep their space-bearing handle and stay hard to `@`/`dm:@` by text | Decide a backfill: rename offending handles (accepting that text-based historical message/DM resolution keyed on the old `name` may shift — uuid-keyed resolution is unaffected), or leave as-is. Kept out of #3 to keep the fix surgical | ⬜ Pending (follow-up to #3; intentionally deferred) |
| I31 | **Oversized uploads are silently truncated, not rejected** | `attachments.ts` busboy `limits.fileSize: 25MB`: on overflow busboy ends the file stream early; `saveObject` persists the truncated bytes in **both** local and S3 drivers, and the upload still returns `200` (no `file.truncated`/`limit` check). A >25MB file is stored corrupt-but-looks-fine. Pre-existing, not S3-specific; surfaced while building the S3 E2E | Detect the busboy `limit` event per file → reject the upload (or mark the attachment) instead of silently storing a truncated object; covers both drivers at the one `attachments.ts` boundary | ⬜ Pending (deferred — kept the S3 close-loop diff surgical) |
| I33 | **`@fancyboi999/open-tag-daemon` published to npm + onboarding wired to npx** (was: built but unpublished + UI showed `npm run daemon`) | `packages/daemon/` (esbuild bundle of `src/daemon` + `src/cli`); `.github/workflows/publish-daemon.yml`; `web/src/views/misc.tsx` + `connectModal*` locales. | ✅ ① Published `v0.1.0` (user scope, public, provenance). Publish auth moved to npm **Trusted Publishing (OIDC, token-less)** — workflow drops `NODE_AUTH_TOKEN`, upgrades npm ≥11.5.1, relies on `id-token: write` + the npm-side trusted-publisher config (owner `fancyboi999` / repo `open-tag` / `publish-daemon.yml`). ✅ ② Connect-a-computer modal + empty-state hint now show `npx @fancyboi999/open-tag-daemon …` (#33). ⚠️ **Remaining:** revoke the first publish token (it was pasted in chat) + delete the now-unused `NPM_TOKEN` repo secret. | 🟢 Done (published + npx onboarding + Trusted Publishing); only token-revocation cleanup remains |
| I39 | **Copilot + kimi model lists are static, not discovered from the user's actual account** | Dynamic model discovery landed for **opencode / cursor / pi** (model list via shell command) and **claude / codex** (thinking/effort levels probed; `listModels.ts`, `runtimeModels.ts`). **copilot and kimi are still static**: copilot's only programmatic source is the `copilot --acp` `availableModels` handshake (ACP / JSON-RPC over stdio); kimi similarly requires its own JSON-RPC-over-stdio session setup — neither fits the daemon's current one-shot shell-command adapter. multica already has a complete ACP handshake sequence that could be adapted. Picking a model the account lacks still fails at runtime (stderr → `error` activity), but cannot be pre-warned | Adapt multica's ACP handshake pattern: a lightweight probe subprocess that does the ACP/JSON-RPC init, reads `availableModels`, and exits — reuse the existing `probe-models` / `models` WS-RPC so no new daemon→server channel is needed. Add copilot and kimi to `DYNAMIC_RUNTIMES` once the probe is built | ⬜ Pending (daemon→server channel now exists; only the ACP probe subprocess remains) |
| I50 | **opencode `--verbose` `reasoning.variants` not consumed** | `listModels.ts` calls `opencode models --verbose` and parses the JSON; model names are extracted but the per-model `reasoning.variants` (thinking capability flags, analogous to claude `--effort` levels) are silently dropped. As a result, opencode models that support thinking are treated as plain text-only models in the UI | Parse `reasoning.variants` from the verbose JSON and include them in the returned model list as thinking-level descriptors, mirroring the claude/codex path | ⬜ Pending |
| I53 | **`listModels` inherits daemon `process.env` minus `NODE_OPTIONS`; agent-specific env not injected** | `listModels.ts` spawns runtime binaries with the daemon's own environment (`process.env`, stripping `NODE_OPTIONS` to match the per-runtime pattern in the runtime adapters). It does **not** inject any per-agent env (e.g. a custom `OPENAI_API_BASE` or provider key stored per agent). Some runtimes that read config from env rather than a config file (e.g. an opencode setup pointing at a non-default provider) may therefore enumerate a different — possibly empty — model list than what the actual agent session would see | Decide whether `probe-models` should carry an `agentId` so `listModels` can look up that agent's runtime config and inject it; or document that model discovery uses the daemon's ambient env and agents relying on per-agent provider overrides may see partial lists | ⬜ Pending |
| I42 | **`activityDetail` can show a stale tool string after an activity transition** | `store.tsx` `agent:activity` handler keeps the old detail when a new event carries none: `activityDetail: p.detail ?? a.activityDetail`. When an agent goes `working`("Running command X") → `thinking`(no detail), the old "Running command X" lingers. Pre-existing store logic, **not introduced by the live-agent-bar PR**, but that bar (`LiveAgentBar.tsx labelOf`) is the first UI to surface `activityDetail` prominently and persistently, so it amplifies the staleness | Decide the intended semantic: either clear detail on a detail-less event (`p.detail ?? null`) — affects the per-DM dot tooltip too, so verify both consumers — or have producers always send a detail. Keep the `?? a.activityDetail` for now (changing it is its own cross-consumer change); `labelOf` already falls back to a generic "Working/Thinking" label when detail is empty | ⬜ Pending (pre-existing; surfaced by code-review of live-agent-bar) |
| I41 | **Runtime adapters — deferred hardening (low priority)** | Code-review of the one-shot runtime adapters (copilot/opencode/kimi/pi/cursor plus experimental Hermes) identified a set of improvements that are correct-but-not-urgent: exit-75 transient retry (kimi), content-as-array support (kimi multi-modal), token-usage capture (all), MCP server injection (all), cursor thinking-block trajectory kind, pi `auto_retry_end` signal, errTail pollution by tool stdout mixed with CLI errors, unbounded `buf` on pathological output (no `MAX_BUF_BYTES` guard), `writeFileSync` → async fs.writeFile (avoids main-thread block on slow network FS), `onExit(null)` → `onExit(1)` coercion (signal-killed processes emit `null`), Hermes prompt passed through argv, and Hermes resending the full OpenTag system prompt on every resumed turn | Address per-runtime as follow-up slices when the relevant CLIs stabilize | ⬜ Pending (post-v1; none of these affect the happy path, only edge cases) |
| I44 | **Authorization layer — systemic access-control gaps (two-plane security audit)** | A human-plane (`routes-api`/`capabilities`) + agent-plane (`routes-agent`/`scopes`/`core.ts resolveTarget`) audit found ~20 gaps in two families: (a) cross-tenant IDOR — `:id` lookups not scoped by the auth-derived `serverId`; (b) missing resource-access checks — scope/role pass but no channel-membership/ownership/capability check on the specific resource. Canonical model + full prioritized register: **`docs/authorization.md`** | Slices shipped: (1) cross-tenant IDOR batch F4/F6/F7/F9/F10 + agent C9 + machine F1/F2 `manageMachines` + C11; (2) **agent-plane channel-access layer C1/C2/C3/C6/C7/C8 + server/info** via `canAgentReadChannel`; (3) **human capability gates F3/F5/F8** (`manageAgents`/`manageChannels` + channel-ownership pre-check; real E2E member→403/owner→200) **+ C4** deleted-agent token (`resolveAgent` `isNull(deletedAt)` + clear hash on soft-delete; E2E token rejected after delete). **Open items live solely in `docs/authorization.md` §6** — do not mirror per-item states here (a mirrored list kept C10 marked "remaining" after `safeEqual` shipped in PR #75) | 🟢 Slices done; check `docs/authorization.md` §6 for what's open (C5 task-ownership, C12 token TTL at last audit 2026-07-06) |
| I81 | **Per-runtime skills dirs: kimi + project-local (workspace) dirs are best-effort** | `skillRootsFor` (`src/daemon/workspace.ts`) ports multica's `localSkillRootsForProvider` for the **home** provider dirs (claude/codex/copilot/opencode/cursor/pi — authoritative from multica) + universal `~/.agents/skills`. Two soft spots: (a) **kimi** has no established skills dir, so it falls back to the universal dir only — if Kimi Code ships a skills convention, add it; (b) the **workspace** (cwd-relative) provider dir mirrors the home leaf (`.claude` / `.codex` / `.opencode` …), which is verified only for claude (`.claude/skills`); for other runtimes a wrong project-local path just yields an empty scan (harmless), not a load of the wrong dir | Confirm kimi's skills path and each runtime's project-local skills convention; until then the home provider dir (the one agents actually load, since they run with the real `$HOME`) is correct and unit-tested (`test/skillRoots.unit.test.ts`) | ⬜ Pending (low priority; home dirs correct, workspace dirs harmless-if-wrong) |
| I82 | **`TaskBoard.tsx` defines `Card` / `StatusPill` / `DraggableCard` / `DroppableCol` INSIDE the component** → new function identities every render → React **remounts** them on any `TaskBoard` state change. Costs: cards lose DOM/transition continuity, dnd-kit re-registers each draggable/droppable per render, and component-local state is lost (this is why the status menu's open state had to be **hoisted to `TaskBoard` + portaled** in the board-move-ux PR rather than living in `StatusPill`). | `web/src/TaskBoard.tsx` (the four inner components); surfaced while root-causing a "click the status pill, menu won't open until you move the mouse" bug — the menu flag was being reset by remounts (`MeasuringStrategy.Always` amplified the churn, since removed). | **Proper fix:** extract `Card`/`StatusPill`/`DraggableCard`/`DroppableCol` to module scope (or a `TaskCard.tsx`), passing the handful of needed props/handlers, so they stop remounting. The move-ux PR neutralized the acute symptom (hoisted menu state, dropped `Always`) but left the components inline. | ⬜ Pending (workaround in place; extraction is the real fix) |
| I83 | **`.task-col-body{overflow-y:auto}` makes each Kanban column its own scroll container** — fine today (modest card counts, no overflow), but on a column with many cards dnd-kit's auto-scroll may scroll a non-target column near a drag edge. The droppable is the full-height `.task-col` (not the scrolled body), so drop-target *measuring* is unaffected; only auto-scroll behaviour is the open question. | `web/src/styles.css` `.task-col-body`; `DndContext` has no `autoScroll` override. | If a many-card column ever shows scroll-jump during drag, configure `DndContext autoScroll` (or scope the scroll container). Not worth pre-building. | ⬜ Pending (edge-case; no repro on current boards) |
| I61 | **Showcase channel — `POST /api/channels/:id/join` route uses the URL param `:id` only for path-matching (the regex captures it but the code uses `b.parentMessageId` from the body for threads; join uses a separate guard)** — no functional bug, but the channel-id capture in `cthreads[1]` is unused in the thread POST handler (security-irrelevant: the parentMessageId in the body is validated against the DB). | `src/server/routes-api/channels.ts` ~L46-58 | No functional fix needed; cleanup: either use the captured id to validate `parent.channelId === cthreads[1]` (cross-check URL vs body) or remove the capture. Low priority. | ⬜ Pending (note added) |
| I63 | **`POST /api/auth/register` rate limit is in-memory and per-process** — the fixed-window limiter (`src/server/ratelimit.ts`) is intentionally single-process: it does not coordinate across multiple server instances and resets on every restart. `REGISTER_RATE_LIMIT=5/hour` (tightened from the default 10/min in this PR) blocks bulk scripted registration effectively on a single-node deployment (the common self-hosted case), but a horizontally-scaled or frequently-restarted deployment could allow substantially more than 5 attempts per hour per IP. The `clientIp()` function is safe from XFF spoofing when `TRUST_PROXY` is not set (default). | `src/server/ratelimit.ts` comment § "per-process and in-memory only"; `REGISTER_RATE_LIMIT = 5`, `REGISTER_RATE_WINDOW_MS = 3 600 000`. | For multi-instance deployments: replace the in-memory `Map` with a Redis-backed atomic counter (use `INCR`/`EXPIRE`), or front-load with an edge/WAF rate limit. Single-instance self-hosting is the current primary target and this guard is sufficient there. | ⬜ Pending (accepted for now; revisit before horizontal scale) |
| I68 | **`.modal` retains an identity `matrix()` transform at rest (from `animation: modal-enter … both`, introduced #96) → it silently becomes the containing block of any `position:fixed`/`absolute` descendant.** This drifted the custom `<Select>` dropdown menu (`.sel-menu`, position:fixed, positioned via viewport coords from `getBoundingClientRect`) ~370px off its trigger inside the Create-Agent modal: the menu opened but landed on empty space, so clicks did nothing ("下拉没反应"). Subtle root cause: `fill-mode: both` keeps the `to` frame after the animation, and although that frame is `transform: none`, the retained value serializes to `matrix(1,0,0,1,0,0)` — NOT the string `none` — and ANY non-none transform establishes a fixed-descendant containing block. | `web/src/styles.css` `.modal,.qs{animation:modal-enter … both}` (~L745); victim was `web/src/Select.tsx` `.sel-menu`. | **Worked around** (sel-menu-fix PR): the Select menu is now `createPortal`-ed to `<body>`, so it lives outside `.modal`'s subtree and is immune to the ancestor transform (verified in-browser: menu↔trigger delta 370px→0). Regression locked by `test/selectMenuPortal.unit.test.ts`. **Residual hazard (not fixed here):** `.modal` still carries the matrix transform at rest — any *future* `position:fixed`/`absolute` element placed inside a modal and expecting viewport/page anchoring will hit the same trap. Full root-cause fix (deferred to avoid changing every modal's enter-animation feel): drop the retained transform at rest — e.g. animate opacity only, or use a fill-mode that doesn't pin the `to`-frame transform. | ⬜ Worked around (residual CSS hazard documented) |
| I70 | **`creatorType="system"` prop-agent filters are now permanent no-ops** — once the showcase became a front-end static page, the backend never creates `creatorType="system"` agents again, so the defensive filters introduced in #116 (`ne(creatorType,"system")` in `core.workspaceMembers` + `routes-agent.ts` `server/info` teammate roster; `creatorType` returned by `GET /api/agents` for web store `visibleAgents`) can never match a row. They were **deliberately kept** rather than removed — they are harmless (the predicate just never fires), and deleting them would be a larger, riskier diff than the surgical showcase-removal warranted. | `src/server/core.ts` `workspaceMembers`, `src/server/routes-agent.ts` `server/info`, `src/server/routes-api/agents.ts` (`creatorType` field), `web/src/store.tsx` `visibleAgents` | Low priority cleanup: drop the `ne(creatorType,"system")` filters + the `creatorType` field/`visibleAgents` plumbing in a dedicated follow-up if the dead code becomes confusing. No functional or security impact either way. | ⬜ Pending (intentional no-op; cleanup deferred) |
| I71 | **`POST /api/channels/:id/read` recomputes the full per-user unread map (`unreadMapForUser`, N+1: 1 membership query + 1 channels query + 1 `COUNT(*)` per membership + 1 parent lookup per followed thread) on every call** — fine for channel-entry / thread-open (infrequent), but `web/src/views/Chat.tsx` fires `markRead(cur.id)` on **every incoming message** in the viewed channel (un-debounced), so a busy/agent-streaming channel turns each message into O(memberships) server queries. Before the honest-badge fix `POST /read` was a single `UPDATE`; the read endpoint now shares the same scan that the debounced `GET /channels/unread` already does, but on a hotter, un-debounced path. Correctness is unaffected (verified ship-ready); this is throughput amplification only. | `src/server/routes-api/channels.ts` (`unreadMapForUser`, `read` action) + `web/src/views/Chat.tsx` (`markRead` on `message` event) | Keep the single source of truth (`unreadMapForUser` is shared by GET /unread + POST /read — do **not** fork a near-duplicate per-channel helper). Cheapest mitigations: (a) client-side debounce/coalesce `markRead` on bursts; (b) replace the per-channel `COUNT` loop with one `GROUP BY channelId` aggregate query so the map is O(1) round-trips; (c) only recompute the affected parent's aggregate. Accepted for now at self-hosted scale; revisit if a deployment shows DB pressure on hot channels. | ⬜ Pending (perf only; flagged by the unread-resurrection PR verifier) |
| I72 | **A machine *returning* online leaves its previously-sleeping agents showing `inactive` (grey) instead of `sleeping` (blue) until they're next @-woken** — the machine-offline-agents fix mutates a down-machine agent's stored `status` `sleeping`→`inactive` (the deliberately-chosen "DB is the single source of truth" approach the user picked over a read-time, machine-gated derive). `sessionId` is kept so the agent still `--resumes` on wake; only the dot colour/label is affected. Also a ≤`STALE_MS` (~90s) lag between the machine showing offline and its sleeping agents following (the sweep needs the heartbeat to age past the cutoff — which is exactly what makes it blip-proof). | `src/server/machineLiveness.ts reconcileOfflineMachineAgents`; chosen in the machine-offline-agents PR over the frontend-derive alternative | Accepted by-design. If grey-on-return ever reads as wrong: either (a) have `onReady` restore `sleeping` for resumable agents on reconnect, or (b) switch to read-time machine-gated presence in the serializers — **don't do both** (that's the satisfies-both hybrid the code-quality rules forbid). | ⬜ Accepted (by-design tradeoff) |
| I73 | **Prod daemon WS flaps with close code `1006` (abnormal) every few minutes**, auto-reconnecting each time (`~/.open-tag/logs/daemon.log`: e.g. 06:46:22, 06:49:53 disconnect→reconnect). Each flap flickers `machines.status` offline→online; the stale-heartbeat gate added in the machine-offline-agents PR keeps it from dragging live agents offline during the few-second window. `3264cd6` already added watchdog reconnects + a stale-close guard so it self-heals, but the underlying flap remains — most likely a reverse-proxy/edge WS idle timeout on `getopentag.com` shorter than the server's 30s `ping`. | `~/.open-tag/logs/daemon.log` 1006 cadence; server pings every 30s in `src/server/ws.ts` | Check the prod proxy/edge WS idle-timeout vs the 30s ping; shorten the ping interval or set proxy WS keepalive if it's a timeout. **Out of scope** for the machine-offline-agents fix (that fix only stops the flap from corrupting agent status). | ⬜ Pending (observed on prod; separate issue) |
| I76 | **Agent Profile › Overview tab still shows a hardcoded `~/.open-tag/agents/<id>/` workspace path** — the same hardcoded template this PR fixes in the Workspace tab. The Overview path row renders `~/.open-tag/agents/{a.id}` from the agent-detail API, which is a pure DB row carrying no daemon-side path (the real dir lives on the daemon machine under its `OPEN_TAG_HOME`, wrong under worktree isolation / custom data dir). Fixing it means exposing the daemon workspace root on `GET /api/agents/:id` — either an extra `requestDaemon` per detail fetch, or a persisted column populated by the daemon ready/status upsert — a larger surface than this PR's one-field-on-workspace-files fix, so deferred. | `web/src/views/Members.tsx` Profile › Overview path row (the Workspace tab next door is now correct) | Extend the agent-detail API to return `workspaceRoot` (prefer piggybacking on the existing daemon `ready`/`agent:status` upsert so there's no per-fetch daemon RPC), then render it in the Overview path row with the same hardcoded fallback as the Workspace tab. | ⬜ Pending (this PR fixes the Workspace tab only) |
| I77 | **`agents.machineId` still has no rebind API after creation** — `PATCH /api/agents/:id` whitelist is only `displayName/description/model/runtime/avatarUrl`, so an agent's machine can never be changed after creation (delete + recreate is the only escape). Create-time is now closed (see residue note), but an agent already bound to a machine that gets deleted, or any pre-existing unbound row from before this fix, still has no way back except recreation. | `src/server/routes-api/agents.ts` (PATCH whitelist) | Make `machineId` patchable (manageAgents cap; validate the machine row belongs to the server; stop/restart the agent process across the move), and add a rebind affordance in the agent profile UI. | 🟢 Done-with-residue (create-time gap closed: `POST /api/agents` now requires `machineId` and 404s on a cross-tenant/bogus id — `machineId ?? null` is gone; web create modal marks Computer required + disables Create until picked. Residue = no PATCH rebind path yet, and any pre-existing `machineId=null` row — e.g. `seed-dev.ts`'s `@dev-bot`, which inserts directly via `db.insert` and bypasses this API validation — still depends on the `broadcastToDaemons` fallback in `core.ts`.) |
| I78 | **Outdated self-hosted daemons (≤0.7.1) crash-loop on codex-agent wake — server can only observe, not prevent** — prod logs (getopentag.com, 2026-07-05) show an external user's machine (`daemonVersion: 0.7.1`, runtimes `["codex"]`) connecting → "machine ready" → dying 44–140s later, reconnecting every ~10 min, all day (20+ cycles). The server never terminates daemon sockets (30s app-level ping, no missed-pong kill — `src/server/ws.ts`), so the disconnects are daemon-side crashes — consistent with the pre-#164 unguarded Codex runtime startup (fixed in daemon 0.8.0) + the 0.7.2 workspace-path fix. Each cycle triggers a reconnect catch-up wake → crash → repeat. | Railway deploy logs (`daemon connected/disconnected` pairs, ~10-min cadence); `src/server/ws.ts`; the runtime image already ships `packages/daemon/package.json` for the "outdated daemon" system-alert (Dockerfile) | The outdated-daemon system alert already covers notification. If crash-looping old daemons become a real support burden: add a **min-daemon-version gate at the WS handshake** (reject `< MIN_DAEMON_VERSION` with a clear "please update: `npx @fancyboi999/open-tag-daemon@latest`" close reason). That hard-breaks old daemons — a product decision, deliberately not bundled into the log-driven-fixes PR. | ⬜ Pending (observed on prod; alert exists, hard gate is a product decision) |
| I84 | **`docs/motion-charter.md` promises distillation into a `DESIGN.md` Motion chapter — the chapter doesn't exist** | The charter header says the tokens land in `web/src/styles.css :root` (done — `--ease-quint`/`--ease-expo`/`--ease-decel` are there) and the charter itself gets distilled into a `DESIGN.md` Motion chapter; `DESIGN.md` has no `## Motion` section (chapters run Overview…Known Gaps). Until distilled, the charter is the only motion spec and DESIGN.md readers miss it | Write the `DESIGN.md` Motion chapter from the charter (tokens + usage rules + `prefers-reduced-motion` policy), or amend the charter to declare itself the standing motion spec | ⬜ Pending (found by the 2026-07-06 doc-convergence audit) |
| I85 | **Windows deployment not covered + runtime detection + CLI spawn broken on Windows** — `docs/self-host.md` assumes Ubuntu + systemd + cron + openssl, none of which apply to Windows. `src/daemon/runtimes.ts` used `command -v` (POSIX-only) so detection always returned `[]`. All 8 runtime adapters used direct `spawn("cli-name", args)` which fails on Windows + nvm4w symlink `.cmd` files (Node v24 bug: `spawn("name")` → ENOENT, `spawn("name.cmd")` → EINVAL). Plus `agentManager.ts` used hardcoded `:` PATH separator (Windows uses `;`), and `openTagBin.ts` didn't generate a `.cmd` wrapper for Windows. | `docs/self-host.md` (no Windows section), `scripts/docker-entrypoint.sh` (CRLF hazard), `src/daemon/runtimes.ts` (POSIX-only), all `*Runtime.ts` (raw `spawn` on Windows), `src/daemon/agentManager.ts` (PATH separator), `src/daemon/openTagBin.ts` (no `.cmd` wrapper) | Windows companion doc; `runtimes.ts` `has()` uses `powershell Get-Command` on win32; `.gitattributes` entrypoint CRLF fix; **`spawnSafe.ts`** uses `cross-spawn` instead of `cmd.exe` wrapper; **`killTree.ts`** — process tree termination via `taskkill /T /F` on Windows; **`agentManager.ts`** uses `path.delimiter` instead of `:`; **`openTagBin.ts`** generates `open-tag.cmd` on Windows; **`index.ts`** registers `SIGBREAK`; **`listModels.ts`** uses no-signal `kill()` on Windows; all 8 `*Runtime.ts` stop() patched to use `killTree`. | 🟡 Partial — code PR'd (#169), awaiting 0.9.0 release |
| I86 | **Generic 500 response echoes `e.message` as `detail` to any authenticated client** — for DB errors the message contains the raw SQL text + bind values (live-reproduced by the uuid-boundary PR's verifier: `{"error":"internal","detail":"Failed query: select \"id\" from \"channels\" where …"}`). The 22P02→400 backstop (same PR) removed the biggest source (malformed-id probes), but **any other throw class still round-trips internals** (constraint violations, connection errors, code bugs). | `src/server/index.ts` top-level catch: `sendErr(res, 500, "internal", { detail: String(e?.message ?? e) })` | Decide a policy: gate `detail` by `NODE_ENV !== "production"`, or replace with an opaque error id correlated to the server log line. **Check consumers first**: the daemon/CLI surfaces `detail` from error responses for agent self-debugging (`~/.open-tag/logs/cli.log`) — stripping it in prod makes remote-daemon debugging harder, so this is a deliberate product/ops tradeoff, not a one-liner. | ⬜ Pending (security hygiene; found by the reaction-500 PR verifier 2026-07-14) |
| I87 | **Agent-plane id resolution still uses a `length >= 32` heuristic instead of the canonical uuid check** — `routes-agent.ts` `findParent` (~L366) and `/agent-api/message/resolve` (~L430) route "long" ids to a bare `eq(uuid_column, v)`: a ≥32-char non-uuid (agent-hallucinated/truncated id) used to 500; since the 22P02 backstop it returns a generic `400 invalid id` instead of the route's own 404 semantics, and never gets the prefix-match fallback. | `src/server/routes-agent.ts` (`v.length >= 32 ? eq(...) : like(...)`) vs `core.ts resolveIdOrPrefix` (`UUID_RE`-gated, shared by messages + attachments) | Route both call sites through `resolveIdOrPrefix` (or at least gate the `eq` branch with `isUuid`) so the agent plane has one id convention. Low severity: authenticated agents only, self-inflicted, no longer a 500. | ⬜ Pending (found by the reaction-500 PR verifier 2026-07-14) |
| I88 | **Pre-0.10.0 daemons silently drop unknown WS-RPC types — the server surfaces no version-mismatch error** — live-hit on local prod (2026-07-19): a machine ran `npx @fancyboi999/open-tag-daemon@latest` while npm `latest` was still 0.9.0; the 0.10.0 server routed `agent:workspace:write` (personality upload) to it, the old daemon logged `recv` and ignored the message, and the upload appeared to succeed-then-vanish (next `workspace:read` → ENOENT). No error reached the UI; the user read it as a broken feature. Sibling of I78 (crash-looping old daemons) — same root: no protocol/version gate at the WS handshake, and `requestDaemon` has no "unsupported RPC" reply path. | `src/server/ws.ts` (handshake carries `daemonVersion` but nothing gates on it); daemon message dispatcher drops unknown `type` without a NACK; observed via `~/.open-tag/logs` + npm dist-tags timeline | Either of: (a) the I78 min-daemon-version WS gate (reject with "please update: `npx @fancyboi999/open-tag-daemon@latest`"), or (b) a generic RPC NACK — daemon replies `{error:"unsupported: <type>", daemonVersion}` for unknown types so `requestDaemon` can surface "daemon too old for this feature" instead of timing out/succeeding silently. (b) is forward-proof for every future RPC addition; do (b) at minimum. | ⬜ Pending (UX/fail-loud gap; bit a real user 2026-07-19) |

> Maintenance: run the `/doc-sync` skill (`.agents/skills/doc-sync/`) Mode 2 periodically to scan this table against code, append new drift, and archive what's been resolved.
